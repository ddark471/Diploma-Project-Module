#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=isgreater(a,a);
b=isless(c,a);
a=isgreaterequal(a,c);
while(islessgreater(c,c)){
a=atan2(b,d);
c=isgreater(b,c);
b=pow(a,b);
}
d=acos(a);
c=log10(c);
d=sin(c);
d=islessequal(c,c);
}
