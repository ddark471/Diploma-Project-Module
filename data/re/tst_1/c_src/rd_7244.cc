#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=log(a);
f=asin(c);
g=acos(f);
f=log10(d);
b=islessequal(a,g);
while(islessgreater(c,a)){
e=isgreaterequal(b,d);
c=fmax(g,a);
}
d=atan2(e,b);
d=fmin(b,b);
}
