#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=fmax(c,c);
a=acos(d);
while(islessgreater(d,c)){
b=fmax(e,b);
c=cos(a);
a=atan2(a,d);
}
g=islessequal(h,c);
a=sin(c);
if(islessgreater(g,b)){
a=sqrt(d);
b=fmin(a,f);
}
else{
b=exp(e);
e=asin(b);
c=sin(a);
c=asin(c);
}
h=fmin(b,a);
a=fmin(d,a);
}
