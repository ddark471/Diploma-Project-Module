#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=pow(b,c);
c=fdim(e,a);
f=isgreaterequal(b,f);
e=isgreater(e,g);
while(islessgreater(f,f)){
b=pow(b,b);
d=sqrt(f);
c=ceil(d);
b=islessgreater(g,a);
a=ceil(e);
}
f=ceil(d);
g=log(a);
}
