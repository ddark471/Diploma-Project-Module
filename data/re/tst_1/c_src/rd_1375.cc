#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=isgreater(b,a);
d=isgreaterequal(b,a);
while(islessgreater(b,a)){
b=tan(c);
a=cos(c);
c=sqrt(d);
}
d=log(d);
f=exp(e);
}
