#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=cos(b);
b=fmax(d,d);
d=islessequal(e,d);
while(isgreaterequal(f,e)){
a=islessequal(a,b);
d=isless(e,f);
b=fmax(b,a);
}
e=atan(a);
c=sqrt(f);
a=sqrt(c);
d=isgreaterequal(e,d);
f=pow(a,f);
}
