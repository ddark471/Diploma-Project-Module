#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fmax(d,c);
d=isless(e,e);
c=fmax(b,g);
while(islessequal(a,a)){
f=islessequal(c,g);
c=fmax(g,g);
b=isgreater(a,b);
c=isgreaterequal(g,d);
}
a=asin(c);
d=log10(c);
e=atan2(e,e);
}
