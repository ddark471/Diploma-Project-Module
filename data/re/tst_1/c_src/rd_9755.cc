#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=cos(a);
b=pow(d,b);
c=ceil(d);
d=fmax(d,a);
while(isless(b,b)){
b=exp(c);
b=fdim(d,d);
c=isgreaterequal(a,c);
}
b=log(a);
a=exp(c);
c=pow(d,c);
}
