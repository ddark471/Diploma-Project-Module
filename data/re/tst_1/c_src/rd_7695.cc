#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=sqrt(b);
b=pow(b,b);
d=ceil(d);
d=log(c);
while(islessgreater(e,b)){
e=islessequal(a,a);
e=ceil(a);
}
a=cos(b);
d=exp(d);
if(isgreaterequal(b,d)){
b=pow(b,b);
d=isgreaterequal(c,d);
b=atan(b);
}
a=sin(b);
a=ceil(d);
b=sin(c);
c=isgreater(c,b);
}
