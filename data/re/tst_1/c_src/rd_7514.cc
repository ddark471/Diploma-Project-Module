#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=tan(g);
f=atan2(c,c);
c=fdim(b,c);
h=fmax(b,h);
g=cos(b);
while(islessequal(f,g)){
c=isgreaterequal(e,h);
f=log10(a);
h=fmax(d,f);
}
d=exp(e);
e=pow(b,d);
a=isgreater(g,g);
e=sin(b);
}
