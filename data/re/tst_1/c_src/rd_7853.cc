#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=exp(b);
a=ceil(g);
while(islessequal(h,e)){
h=isless(b,d);
a=exp(e);
}
f=log10(a);
g=exp(b);
e=atan(b);
b=ceil(a);
a=islessgreater(f,h);
}
