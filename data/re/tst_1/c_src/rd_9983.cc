#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=cos(e);
c=log(c);
while(isgreaterequal(d,g)){
c=isgreater(g,g);
g=asin(c);
d=ceil(g);
}
e=log(e);
a=islessequal(c,a);
if(islessgreater(f,c)){
f=isless(a,d);
e=sqrt(b);
b=exp(b);
d=ceil(d);
b=islessequal(e,d);
}
f=isgreater(f,f);
c=isgreater(f,a);
}
