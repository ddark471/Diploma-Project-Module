#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fmax(c,c);
d=fmax(d,c);
c=pow(a,b);
while(isless(b,c)){
b=fdim(a,c);
a=islessequal(b,d);
b=islessgreater(c,b);
a=exp(b);
}
g=isless(g,g);
e=ceil(g);
c=fmin(e,e);
if(islessequal(g,b)){
d=asin(e);
f=log10(d);
f=fmax(g,b);
c=tan(c);
}
else{
a=islessgreater(c,f);
b=exp(g);
f=asin(a);
}
g=acos(g);
g=fmin(c,c);
}
