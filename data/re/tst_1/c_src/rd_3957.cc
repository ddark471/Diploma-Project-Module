#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fmax(c,a);
d=pow(a,c);
b=atan2(b,b);
while(isless(d,b)){
b=cos(d);
d=sin(d);
b=isless(a,a);
}
b=sqrt(b);
c=sqrt(d);
a=fmax(d,b);
f=acos(f);
a=isless(e,g);
if(islessgreater(g,b)){
c=asin(g);
d=atan2(c,b);
}
else{
e=floor(f);
a=tan(e);
d=isgreater(g,f);
e=log10(g);
}
f=sqrt(f);
g=log10(b);
}
