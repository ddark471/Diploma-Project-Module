#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=fmin(g,b);
a=ceil(a);
while(isgreaterequal(a,g)){
e=islessgreater(e,f);
e=log(a);
c=fmax(a,e);
b=sin(b);
}
c=fmax(b,g);
f=log(b);
e=islessgreater(a,g);
g=fmax(g,c);
}
