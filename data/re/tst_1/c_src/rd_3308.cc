#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=pow(a,b);
a=isless(c,d);
b=isless(c,d);
a=acos(a);
a=pow(a,c);
while(isless(b,c)){
d=islessgreater(a,b);
d=ceil(b);
c=exp(a);
c=pow(a,c);
d=pow(d,d);
}
d=floor(d);
b=isless(c,d);
b=pow(b,c);
a=islessequal(c,b);
c=cos(c);
}
