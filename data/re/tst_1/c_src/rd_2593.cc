#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=atan2(d,a);
c=exp(d);
c=atan(a);
d=isgreaterequal(a,c);
while(islessequal(b,a)){
c=sin(a);
b=pow(c,c);
c=atan2(a,d);
c=isgreater(b,b);
a=fmax(a,c);
}
a=atan(a);
b=asin(b);
c=isless(d,c);
d=cos(a);
a=floor(a);
}
