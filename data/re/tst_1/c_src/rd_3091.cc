#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=pow(f,c);
g=exp(e);
while(isless(b,b)){
d=islessgreater(a,a);
h=islessequal(b,a);
}
e=tan(f);
f=fmax(a,c);
d=atan2(b,e);
d=isgreaterequal(g,h);
}
