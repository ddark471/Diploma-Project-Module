#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=log10(d);
e=log10(b);
c=exp(e);
b=atan(g);
if(isgreaterequal(c,g)){
c=islessgreater(a,f);
d=sqrt(b);
}
g=log10(e);
d=pow(c,b);
a=cos(c);
while(islessgreater(f,c)){
e=log(c);
c=fmax(g,g);
d=islessgreater(c,d);
d=pow(a,g);
}
h=log(f);
b=floor(e);
a=fmax(e,b);
a=ceil(b);
if(islessequal(g,h)){
d=isgreater(g,g);
a=isgreater(h,h);
e=islessequal(h,a);
g=pow(e,b);
}
else{
f=isgreaterequal(d,h);
e=islessgreater(d,c);
d=atan(e);
}
h=log(g);
c=sin(a);
c=pow(b,c);
}
