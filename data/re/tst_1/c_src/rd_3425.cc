#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=isgreater(d,c);
d=isless(a,b);
d=sqrt(a);
c=isgreater(d,a);
while(islessequal(c,b)){
d=isgreater(b,b);
a=fdim(c,d);
a=sqrt(a);
d=isgreaterequal(a,c);
}
d=fdim(a,b);
b=log(a);
}
