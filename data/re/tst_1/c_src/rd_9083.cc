#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=floor(a);
d=asin(b);
d=floor(b);
while(islessequal(b,d)){
c=log(d);
d=pow(b,a);
b=log(a);
}
c=log10(d);
c=cos(c);
}
