#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=atan2(c,d);
b=log10(d);
a=islessequal(d,b);
while(islessequal(c,a)){
b=isgreater(b,d);
a=sqrt(b);
b=tan(e);
c=sqrt(c);
c=acos(a);
}
d=sqrt(a);
g=islessgreater(c,g);
e=fmax(f,f);
if(islessgreater(g,h)){
g=atan2(h,g);
h=isless(a,h);
c=log10(e);
d=tan(g);
g=tan(b);
}
g=isless(g,g);
d=atan2(b,g);
h=islessgreater(e,a);
}
