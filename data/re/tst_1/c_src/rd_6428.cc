#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=log(b);
e=fmin(a,b);
d=log10(a);
while(isless(e,b)){
d=ceil(f);
f=fmin(d,d);
e=floor(a);
}
e=islessequal(a,f);
a=atan2(b,a);
}
