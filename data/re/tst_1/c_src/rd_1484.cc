#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=floor(b);
f=sqrt(e);
e=exp(f);
while(islessgreater(c,d)){
c=pow(b,e);
c=fmin(c,f);
}
c=isgreater(d,b);
a=log10(f);
c=ceil(f);
c=sqrt(d);
}
