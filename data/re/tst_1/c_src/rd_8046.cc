#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=floor(b);
g=exp(g);
a=sqrt(b);
g=pow(a,f);
d=atan2(c,e);
if(isgreaterequal(e,f)){
f=sqrt(b);
f=log10(b);
f=isgreater(d,b);
}
e=islessequal(d,g);
e=atan2(f,c);
f=fdim(d,d);
g=fmax(b,b);
while(islessgreater(c,g)){
b=pow(d,e);
b=pow(a,b);
}
e=islessequal(f,d);
d=atan2(c,a);
a=floor(b);
f=islessequal(d,c);
}
