#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fmax(a,d);
g=log10(e);
while(islessequal(c,d)){
d=acos(f);
g=isgreater(g,b);
d=pow(a,a);
a=ceil(d);
}
e=log(g);
c=atan(f);
g=floor(b);
f=ceil(e);
}
