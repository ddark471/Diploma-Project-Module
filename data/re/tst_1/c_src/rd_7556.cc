#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=pow(g,a);
g=atan(b);
d=islessgreater(c,a);
f=fmax(c,b);
while(islessgreater(a,g)){
d=tan(b);
a=fmax(d,f);
f=islessgreater(f,e);
f=exp(c);
f=isless(b,g);
}
f=cos(b);
g=log(f);
}
