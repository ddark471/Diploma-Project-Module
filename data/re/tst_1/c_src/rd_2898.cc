#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=atan2(d,b);
c=floor(a);
while(islessequal(a,d)){
d=exp(c);
a=isless(b,c);
b=floor(d);
}
b=fmax(c,a);
d=isgreater(a,b);
}
