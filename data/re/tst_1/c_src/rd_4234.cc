#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=islessgreater(d,b);
b=cos(c);
d=islessequal(c,a);
a=fmax(a,a);
while(isless(b,d)){
b=exp(a);
b=isgreaterequal(d,b);
c=sqrt(d);
}
a=log(d);
c=fmin(a,c);
d=fmax(d,b);
}
