#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=pow(a,a);
d=fmin(e,c);
a=pow(d,f);
g=ceil(f);
if(islessgreater(d,a)){
a=fmin(b,f);
b=fmax(g,d);
}
d=cos(e);
c=fdim(e,b);
e=isgreaterequal(d,c);
while(isgreaterequal(g,b)){
g=ceil(b);
d=fmax(a,c);
g=fmin(g,b);
}
b=sin(c);
e=exp(d);
a=pow(d,a);
d=isless(c,a);
e=isless(b,c);
}
