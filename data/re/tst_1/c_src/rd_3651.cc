#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=log10(a);
h=tan(h);
h=isgreaterequal(e,d);
f=log(c);
h=sqrt(e);
while(islessgreater(c,h)){
c=tan(c);
f=isgreater(h,b);
g=log(f);
c=islessequal(f,b);
}
b=floor(g);
b=isgreater(d,a);
}
