#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=isgreaterequal(c,c);
c=pow(b,b);
d=sqrt(b);
a=isgreater(d,c);
d=sin(b);
while(islessgreater(c,b)){
d=fdim(b,a);
b=exp(a);
c=tan(d);
}
c=sqrt(c);
d=atan(d);
d=log(d);
c=fmax(d,c);
b=isless(c,b);
}
