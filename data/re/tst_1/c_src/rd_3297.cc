#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=ceil(g);
d=cos(d);
f=fmax(d,b);
d=exp(a);
while(islessequal(a,g)){
f=islessequal(f,c);
g=atan(b);
g=isgreater(b,c);
}
g=fdim(g,e);
c=isgreater(f,g);
}
