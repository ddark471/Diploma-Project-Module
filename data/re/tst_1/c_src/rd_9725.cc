#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=atan2(b,b);
d=islessequal(d,b);
a=fmax(e,d);
while(isgreaterequal(e,e)){
a=isless(a,b);
b=cos(e);
}
e=atan2(e,a);
b=log(b);
}
