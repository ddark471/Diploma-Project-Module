#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=fdim(b,d);
f=fmin(g,b);
while(isless(d,d)){
e=fmax(g,b);
a=isgreaterequal(b,c);
a=islessgreater(d,e);
}
e=sin(b);
c=islessgreater(d,c);
g=fmin(f,c);
if(isgreaterequal(b,f)){
c=isless(a,a);
d=exp(c);
}
else{
h=atan2(g,d);
c=cos(d);
}
h=sqrt(c);
g=atan(b);
a=fmin(h,c);
}
