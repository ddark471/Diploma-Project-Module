#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=isgreaterequal(h,a);
b=floor(h);
f=exp(c);
g=islessequal(a,a);
f=isgreaterequal(h,g);
while(islessequal(f,g)){
h=ceil(f);
d=log(b);
e=atan(f);
a=sin(g);
d=tan(b);
}
f=fmin(g,h);
a=fmax(d,e);
d=fmin(f,f);
}
