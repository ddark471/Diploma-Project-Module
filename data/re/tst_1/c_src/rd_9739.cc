#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=pow(c,a);
d=fdim(b,d);
a=sin(b);
while(isless(d,b)){
a=atan(c);
c=fmax(d,c);
c=ceil(c);
a=log10(d);
d=pow(a,d);
}
d=sin(a);
d=exp(c);
a=pow(b,c);
}
