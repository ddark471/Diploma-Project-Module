#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fmax(a,b);
c=sin(c);
b=log(b);
while(isgreaterequal(a,c)){
c=fmax(d,b);
a=sin(b);
b=isgreaterequal(b,c);
a=floor(d);
d=atan(b);
}
d=floor(c);
a=fmin(c,a);
}
