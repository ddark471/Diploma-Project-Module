#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fmax(e,d);
c=isless(b,c);
d=islessequal(d,d);
c=isgreaterequal(c,c);
while(isless(b,a)){
c=isless(c,c);
b=islessgreater(b,a);
e=fmax(a,d);
}
b=fdim(c,d);
a=isgreater(e,a);
a=floor(b);
}
