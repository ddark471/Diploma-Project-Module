#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=atan2(f,b);
a=pow(b,a);
a=asin(c);
d=sin(f);
f=isgreater(c,d);
while(isless(e,c)){
b=log10(b);
e=floor(d);
f=log(c);
a=atan(c);
}
e=isgreaterequal(e,f);
c=fmax(d,c);
}
