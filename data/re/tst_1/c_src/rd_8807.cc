#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=floor(b);
e=fmax(b,c);
c=fmax(e,a);
while(islessequal(d,b)){
d=atan2(e,b);
d=sin(d);
b=pow(e,b);
}
c=tan(d);
d=acos(e);
d=floor(a);
c=atan2(e,b);
}
