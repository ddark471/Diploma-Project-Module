#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=pow(b,b);
e=isless(d,b);
d=isless(a,g);
while(islessgreater(c,f)){
a=isless(b,g);
b=atan(a);
g=log(c);
g=ceil(f);
}
b=islessgreater(c,b);
a=log(c);
if(isgreaterequal(c,b)){
a=atan2(b,a);
c=asin(d);
c=atan2(b,c);
a=cos(d);
a=atan2(b,b);
}
else{
b=isgreater(d,c);
a=asin(d);
a=atan(c);
b=islessgreater(a,d);
}
c=cos(c);
a=fmax(c,b);
c=pow(b,a);
}
