#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=log(f);
g=isless(c,c);
h=islessgreater(h,c);
b=isless(a,f);
g=fmax(h,g);
if(isless(d,f)){
h=tan(f);
b=floor(c);
g=tan(b);
d=fmax(h,e);
h=fdim(e,g);
}
c=islessequal(d,h);
b=acos(c);
g=islessequal(b,a);
b=isless(c,h);
h=fmax(e,c);
while(isgreaterequal(a,e)){
c=islessgreater(h,c);
c=isless(b,f);
}
c=fmax(c,b);
e=fmin(h,h);
if(islessequal(g,a)){
f=exp(h);
d=sqrt(h);
}
else{
a=sin(f);
d=floor(f);
}
b=sin(f);
a=atan(a);
a=cos(g);
}
