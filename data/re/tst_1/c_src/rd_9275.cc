#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=atan(c);
c=log(a);
while(isless(c,d)){
e=tan(c);
d=exp(a);
c=isgreater(e,c);
}
c=fmin(b,a);
b=sqrt(b);
d=sqrt(b);
c=tan(a);
}
