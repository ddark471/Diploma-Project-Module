#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=islessequal(b,b);
d=fmax(c,c);
c=cos(d);
a=fmax(a,a);
while(isless(a,a)){
c=fdim(c,b);
b=floor(c);
}
a=pow(c,a);
a=islessequal(f,c);
if(islessgreater(f,b)){
b=fmin(f,c);
a=isless(e,c);
a=fmax(b,c);
}
b=atan(b);
e=fdim(e,b);
a=isgreater(c,c);
e=isgreater(d,c);
c=acos(b);
}
