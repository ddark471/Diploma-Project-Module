#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=log10(d);
a=cos(c);
while(isgreaterequal(d,b)){
a=sqrt(b);
a=fmax(a,c);
}
c=islessequal(b,c);
c=asin(b);
a=exp(b);
a=log10(d);
a=isgreater(b,a);
}
