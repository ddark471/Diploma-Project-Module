#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=sin(d);
a=fmax(a,c);
d=sin(b);
while(isgreaterequal(c,b)){
c=atan2(b,d);
a=asin(b);
}
b=fmax(b,d);
c=atan(a);
if(isless(c,d)){
d=sqrt(b);
a=asin(a);
b=atan2(c,d);
d=isgreaterequal(b,d);
}
else{
a=fmax(b,b);
b=isgreaterequal(c,d);
d=islessgreater(d,a);
}
d=fmax(d,b);
b=atan2(c,a);
}
