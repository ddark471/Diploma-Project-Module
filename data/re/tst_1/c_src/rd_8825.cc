#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=ceil(c);
b=islessequal(b,b);
while(islessequal(b,a)){
d=pow(c,b);
d=pow(c,b);
}
b=exp(a);
b=sin(d);
a=cos(d);
}
