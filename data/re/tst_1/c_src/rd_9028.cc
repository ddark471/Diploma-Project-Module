#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=fmin(b,a);
d=log10(c);
c=atan2(c,b);
d=sin(a);
if(islessequal(d,b)){
a=exp(d);
b=fmin(d,b);
a=islessequal(a,a);
c=fmin(b,b);
}
else{
a=asin(b);
a=exp(d);
d=floor(b);
}
d=log(a);
a=fmin(b,d);
while(isless(b,d)){
d=islessequal(d,a);
c=isless(d,d);
}
f=atan(b);
f=isless(a,b);
c=exp(f);
c=pow(f,a);
d=atan(f);
if(islessgreater(e,b)){
f=atan(e);
c=log10(g);
c=islessequal(a,b);
}
else{
a=exp(c);
a=atan(d);
}
a=exp(d);
g=fmax(e,f);
d=atan(c);
a=pow(g,g);
}
