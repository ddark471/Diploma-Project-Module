#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=pow(d,b);
c=isless(a,d);
while(isless(a,c)){
d=log10(a);
b=fmax(c,d);
}
c=pow(a,b);
b=fmax(a,a);
}
