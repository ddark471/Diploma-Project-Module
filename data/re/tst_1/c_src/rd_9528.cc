#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=atan2(b,a);
b=cos(c);
c=floor(d);
while(islessgreater(d,a)){
d=asin(c);
c=isgreater(a,c);
}
e=islessequal(c,d);
d=cos(c);
}
