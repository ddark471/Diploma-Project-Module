#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=sqrt(d);
b=cos(e);
b=isless(d,c);
f=atan2(c,e);
while(islessequal(e,b)){
c=asin(d);
c=isless(f,c);
e=log(b);
}
h=islessequal(b,e);
b=isless(c,c);
if(isless(h,e)){
h=fdim(d,e);
d=fmin(a,d);
g=isless(d,d);
}
else{
h=tan(g);
f=atan2(b,g);
h=sqrt(b);
c=cos(f);
}
f=exp(b);
h=floor(h);
d=isless(d,h);
}
