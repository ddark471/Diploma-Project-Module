#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=isless(a,c);
h=cos(a);
while(isless(g,e)){
a=fmax(f,b);
f=atan2(f,h);
}
a=log10(a);
c=fmax(b,a);
c=log10(b);
a=tan(d);
d=log(b);
if(isless(a,a)){
a=islessequal(a,b);
d=atan(c);
b=fdim(d,a);
a=atan2(b,c);
}
a=cos(d);
a=islessequal(a,a);
}
