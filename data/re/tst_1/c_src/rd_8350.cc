#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=pow(c,a);
b=fmin(a,a);
d=cos(c);
d=fdim(d,c);
c=sin(d);
while(isgreaterequal(b,b)){
b=tan(d);
c=ceil(c);
}
a=isgreaterequal(d,a);
d=sqrt(b);
d=fdim(a,b);
a=ceil(b);
}
