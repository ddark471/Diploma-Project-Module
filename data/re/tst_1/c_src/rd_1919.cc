#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=log10(a);
a=sqrt(b);
c=isgreater(c,a);
while(isless(d,b)){
b=atan(c);
b=log10(c);
b=log10(c);
}
d=exp(d);
b=acos(b);
if(isless(b,c)){
d=log10(b);
d=isgreaterequal(d,a);
}
else{
d=fmax(a,d);
d=sqrt(a);
}
a=pow(b,c);
d=exp(d);
}
