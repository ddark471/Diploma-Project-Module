#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=exp(d);
f=tan(c);
a=atan(b);
d=atan2(a,c);
if(isgreaterequal(f,c)){
d=exp(c);
a=atan2(a,c);
}
b=acos(f);
b=pow(d,d);
while(islessgreater(d,c)){
a=fdim(f,f);
a=exp(d);
b=isgreater(a,c);
}
a=fmax(b,c);
a=fdim(a,d);
c=islessequal(b,d);
if(islessequal(b,a)){
e=isgreater(e,c);
c=islessgreater(b,b);
c=cos(c);
a=fdim(c,e);
}
b=atan2(e,d);
d=isless(a,d);
d=sqrt(b);
e=atan(e);
}
