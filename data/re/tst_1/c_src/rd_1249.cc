#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=log(f);
e=atan(a);
while(isgreaterequal(c,d)){
c=isgreater(c,a);
c=fmax(f,e);
e=isless(c,a);
c=fmax(a,b);
b=pow(f,d);
}
c=islessequal(c,a);
c=ceil(c);
d=isgreaterequal(d,c);
}
