#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=log(a);
h=exp(e);
e=islessequal(f,c);
while(islessequal(d,f)){
e=fmax(h,c);
h=acos(h);
f=isless(f,c);
}
f=cos(b);
d=fmin(b,a);
b=islessgreater(g,c);
if(isgreaterequal(b,f)){
e=fmax(f,d);
g=pow(f,f);
a=atan2(e,d);
d=log(g);
}
else{
b=atan2(a,e);
a=atan2(f,g);
g=cos(g);
}
c=fmax(f,a);
b=fmax(e,e);
f=fmin(b,b);
}
