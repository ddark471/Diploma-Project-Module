#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=atan2(c,a);
c=isgreater(a,c);
c=isgreater(d,c);
c=islessequal(b,a);
c=isless(a,b);
while(isgreaterequal(d,b)){
a=log(a);
d=ceil(c);
b=pow(b,c);
a=fmin(b,d);
c=isless(b,a);
}
b=fdim(b,d);
c=isgreater(d,d);
a=log10(a);
c=acos(a);
}
