#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=isgreater(b,d);
e=isgreater(d,c);
e=sin(a);
d=sqrt(h);
while(isless(e,g)){
d=sqrt(h);
c=log10(h);
c=fmax(h,e);
c=isgreaterequal(f,a);
b=asin(g);
}
b=islessgreater(b,c);
d=pow(c,a);
g=fmax(c,e);
f=isgreaterequal(g,f);
if(isless(d,e)){
g=isgreater(e,f);
f=islessgreater(a,d);
}
else{
d=pow(e,c);
f=asin(c);
a=islessequal(a,d);
g=log10(b);
b=fdim(e,b);
}
c=atan(e);
b=exp(a);
g=isgreater(a,a);
g=islessgreater(a,g);
}
