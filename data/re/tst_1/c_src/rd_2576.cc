#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=atan2(c,e);
d=fmin(e,a);
a=asin(c);
a=islessequal(b,a);
b=fmax(d,c);
while(islessequal(b,c)){
c=islessequal(d,a);
d=atan(b);
e=sin(a);
c=atan2(b,b);
}
b=atan2(b,c);
d=cos(e);
a=islessequal(a,d);
}
