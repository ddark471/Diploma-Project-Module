#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=sqrt(e);
a=exp(b);
e=isless(g,e);
while(isless(b,b)){
c=atan(b);
g=tan(c);
a=pow(c,b);
}
e=floor(g);
e=ceil(a);
}
