#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=cos(h);
b=isless(b,b);
f=exp(b);
f=asin(d);
while(isless(e,c)){
e=islessgreater(e,c);
b=cos(c);
h=log(f);
}
b=isgreater(f,c);
d=ceil(f);
e=ceil(h);
g=log(h);
g=tan(d);
}
