#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=log(c);
c=isgreater(b,c);
d=islessequal(a,c);
while(isgreaterequal(b,b)){
c=isless(b,a);
c=exp(d);
d=fdim(a,b);
d=exp(a);
c=acos(b);
}
b=atan(b);
f=sqrt(a);
b=cos(b);
}
