#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=log10(a);
d=fdim(a,c);
c=acos(b);
d=sin(a);
while(isless(b,b)){
a=exp(d);
d=isless(c,b);
}
b=isgreater(b,a);
a=fdim(b,a);
}
