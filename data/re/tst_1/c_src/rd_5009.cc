#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=cos(e);
a=islessequal(c,f);
while(isless(d,f)){
g=asin(h);
h=exp(c);
d=islessgreater(d,f);
g=sqrt(a);
}
d=islessgreater(a,d);
d=acos(c);
b=isless(a,e);
d=islessequal(d,b);
e=islessgreater(a,a);
if(isless(d,c)){
a=fmax(e,a);
a=pow(d,c);
}
c=fmax(d,d);
a=islessgreater(a,a);
}
