#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fmax(c,b);
c=isgreater(d,c);
c=fmax(b,b);
d=islessgreater(d,a);
while(islessgreater(c,d)){
b=isgreater(d,a);
d=acos(b);
}
c=ceil(a);
a=exp(a);
b=pow(c,a);
}
