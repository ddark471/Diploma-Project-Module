#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=islessequal(f,b);
b=pow(d,c);
while(islessgreater(d,c)){
f=pow(e,e);
e=isless(a,e);
d=floor(b);
}
a=log10(b);
g=log(c);
}
