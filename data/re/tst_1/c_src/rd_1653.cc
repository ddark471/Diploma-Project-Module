#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=isless(f,g);
g=tan(g);
if(isgreaterequal(d,d)){
e=tan(g);
a=isless(g,d);
g=log10(g);
d=isgreater(f,a);
}
else{
g=atan2(a,g);
e=isless(e,d);
}
d=islessgreater(d,b);
d=atan2(d,a);
c=floor(f);
f=atan(c);
while(isless(d,d)){
e=islessequal(c,a);
d=pow(f,a);
}
b=tan(e);
a=log(b);
}
