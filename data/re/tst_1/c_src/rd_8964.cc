#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=floor(c);
h=isgreater(b,c);
g=cos(g);
while(isless(g,h)){
g=islessequal(e,g);
d=tan(h);
h=fmax(f,c);
}
c=pow(a,d);
d=fmin(e,b);
e=cos(a);
}
