#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=islessequal(a,e);
e=log10(f);
g=atan(f);
g=atan2(c,f);
c=log10(c);
while(isless(b,b)){
e=atan2(b,f);
b=log10(c);
c=atan(d);
}
b=atan2(a,d);
f=sqrt(a);
d=atan2(d,e);
g=log(g);
}
