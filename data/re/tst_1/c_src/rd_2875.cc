#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=log(d);
d=exp(a);
b=acos(a);
b=atan(c);
d=atan(d);
while(isless(b,c)){
e=islessgreater(c,a);
d=atan(f);
d=fdim(e,a);
}
d=pow(a,e);
c=sin(b);
}
