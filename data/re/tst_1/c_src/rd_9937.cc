#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=islessgreater(b,d);
a=pow(c,a);
c=islessgreater(d,c);
c=asin(a);
while(islessequal(d,c)){
d=cos(b);
a=fmax(d,c);
}
c=atan(a);
d=pow(c,b);
c=isgreaterequal(d,b);
d=pow(d,c);
c=isgreaterequal(c,b);
}
