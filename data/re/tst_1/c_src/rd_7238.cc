#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=ceil(g);
b=isgreater(c,e);
e=fmin(c,h);
a=islessgreater(d,f);
while(islessgreater(g,h)){
f=log10(d);
g=fmax(b,e);
b=fmax(a,a);
a=floor(e);
}
h=isgreater(c,g);
f=floor(g);
g=atan2(f,a);
c=acos(a);
}
