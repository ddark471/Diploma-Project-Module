#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=log10(b);
f=asin(f);
e=islessequal(d,e);
b=fmin(c,e);
while(islessequal(d,b)){
a=log10(b);
e=sin(d);
c=atan2(a,f);
}
h=fmin(g,b);
h=sin(g);
if(isgreaterequal(a,b)){
b=atan(h);
a=islessequal(d,f);
g=cos(e);
a=isgreater(h,g);
}
g=atan(c);
g=islessgreater(f,c);
d=exp(a);
b=sqrt(h);
e=ceil(g);
}
