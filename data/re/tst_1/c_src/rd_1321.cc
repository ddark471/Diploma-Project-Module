#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=atan2(a,b);
a=cos(d);
b=isless(c,b);
b=atan2(d,c);
while(isless(d,b)){
c=cos(c);
a=fmax(a,d);
}
d=atan(a);
c=isless(b,a);
a=islessgreater(a,d);
}
