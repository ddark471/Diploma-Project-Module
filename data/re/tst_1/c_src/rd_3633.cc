#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=log(e);
h=isgreater(d,h);
g=atan(d);
g=isgreater(c,g);
d=atan(c);
while(islessgreater(b,e)){
c=cos(a);
d=islessequal(c,a);
b=atan2(f,a);
}
b=ceil(e);
a=isgreaterequal(b,d);
a=fmax(c,b);
}
