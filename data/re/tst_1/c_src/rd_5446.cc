#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=atan2(e,d);
f=isgreater(b,b);
if(islessequal(b,c)){
f=exp(e);
c=fmax(e,h);
}
else{
d=islessgreater(e,f);
a=pow(f,b);
f=asin(h);
}
b=log(d);
g=pow(b,h);
e=pow(f,d);
while(islessequal(c,d)){
d=isgreaterequal(d,g);
g=isgreater(b,f);
e=ceil(a);
d=isless(e,c);
}
g=pow(g,d);
b=fmin(g,d);
}
