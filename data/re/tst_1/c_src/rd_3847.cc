#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fdim(a,d);
d=asin(a);
if(isless(a,c)){
c=fmax(c,d);
d=atan(a);
d=fdim(c,b);
d=sqrt(d);
}
d=fmax(a,c);
a=floor(d);
a=acos(c);
a=cos(b);
while(isless(c,d)){
d=exp(a);
d=log(a);
a=islessgreater(a,d);
d=fmax(c,d);
}
d=islessequal(b,a);
d=log10(e);
h=ceil(h);
}
