#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=ceil(d);
a=sin(b);
while(isless(b,c)){
a=floor(b);
c=exp(b);
}
b=exp(b);
c=fmin(d,d);
}
