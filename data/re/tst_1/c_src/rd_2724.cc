#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=isless(a,b);
c=floor(d);
a=floor(d);
while(isgreaterequal(d,a)){
c=pow(c,b);
d=atan2(a,d);
}
c=fmax(c,a);
d=islessgreater(a,d);
a=islessgreater(d,b);
if(islessgreater(c,a)){
a=fdim(d,c);
b=fmin(d,b);
d=log(a);
a=fmin(b,b);
}
a=isless(b,d);
d=sqrt(a);
d=pow(c,c);
}
