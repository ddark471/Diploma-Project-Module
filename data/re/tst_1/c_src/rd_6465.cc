#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=acos(h);
g=tan(a);
d=exp(h);
g=sqrt(d);
while(isless(b,f)){
a=fmax(e,a);
a=fmax(f,g);
d=tan(h);
a=atan2(a,g);
g=islessequal(d,c);
}
d=tan(a);
b=isgreater(a,d);
}
