#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=exp(f);
a=isless(c,b);
a=log(g);
f=pow(b,b);
while(isless(f,a)){
f=tan(d);
c=exp(e);
d=atan(d);
e=asin(f);
}
d=fmax(c,g);
f=islessequal(d,g);
d=islessgreater(e,f);
g=exp(e);
b=islessequal(e,b);
}
