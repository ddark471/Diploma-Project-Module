#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=tan(a);
c=fmin(d,b);
while(isgreaterequal(b,a)){
e=fdim(c,b);
c=exp(e);
c=isgreaterequal(b,b);
}
a=exp(g);
e=fmax(h,a);
if(islessequal(d,f)){
d=floor(g);
a=cos(f);
f=sqrt(b);
}
g=pow(h,g);
a=pow(h,h);
d=fmin(h,c);
c=tan(h);
}
