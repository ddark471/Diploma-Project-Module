#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=sqrt(b);
a=isless(d,b);
d=sqrt(d);
c=sqrt(a);
d=fmax(d,e);
while(isless(a,b)){
b=pow(a,c);
a=fmin(b,c);
}
b=cos(a);
b=islessgreater(a,e);
d=isgreater(a,b);
}
