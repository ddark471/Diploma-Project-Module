#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=sin(c);
d=fmax(b,a);
while(islessequal(a,d)){
a=fmin(c,d);
d=fdim(d,a);
}
c=islessequal(d,b);
a=isless(d,d);
c=tan(d);
}
