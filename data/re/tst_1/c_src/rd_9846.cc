#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fmax(d,a);
d=islessequal(b,c);
a=cos(d);
d=sqrt(d);
b=floor(b);
while(islessgreater(b,a)){
b=isless(d,d);
d=atan(b);
b=sqrt(d);
d=isgreater(a,a);
}
b=pow(f,e);
a=sin(a);
f=fmax(b,b);
a=fmax(f,a);
if(islessequal(f,e)){
c=atan(d);
f=atan(e);
f=log10(f);
d=sin(d);
}
d=log(f);
d=atan(a);
f=exp(d);
f=log10(b);
}
