#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fdim(c,c);
d=isgreaterequal(b,b);
while(islessgreater(c,a)){
c=islessgreater(a,a);
a=floor(d);
}
c=islessequal(b,b);
c=sqrt(b);
b=isgreater(c,b);
}
