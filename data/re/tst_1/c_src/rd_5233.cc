#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=atan2(d,a);
a=log10(d);
while(islessgreater(c,a)){
a=fmax(a,a);
d=atan2(c,d);
b=floor(c);
c=tan(b);
}
b=floor(b);
c=islessgreater(c,a);
d=fdim(c,d);
c=log(b);
}
