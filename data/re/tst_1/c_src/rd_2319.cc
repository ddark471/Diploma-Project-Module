#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=sqrt(g);
d=fmax(g,f);
b=fmax(a,e);
while(isless(g,a)){
e=cos(d);
f=pow(c,b);
a=fmax(a,a);
c=pow(d,e);
}
e=pow(c,e);
g=isgreaterequal(f,e);
}
