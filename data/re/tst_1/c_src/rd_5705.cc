#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=fmax(e,b);
e=log(d);
while(isless(d,d)){
e=isgreater(c,c);
a=pow(d,b);
d=fmax(e,d);
}
a=acos(b);
d=asin(a);
d=isgreater(a,b);
}
