#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=pow(f,f);
c=isgreaterequal(g,g);
f=islessgreater(d,f);
while(islessequal(a,g)){
h=floor(d);
f=acos(h);
g=ceil(b);
b=ceil(f);
}
b=log(d);
b=exp(b);
if(islessgreater(d,c)){
a=fmin(a,b);
b=isgreater(a,d);
c=exp(d);
d=floor(a);
d=pow(a,a);
}
else{
c=isgreater(c,d);
c=tan(d);
a=isless(b,d);
}
c=log(d);
c=fmin(a,b);
a=ceil(b);
d=fdim(b,c);
}
