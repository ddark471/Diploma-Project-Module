#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=floor(a);
d=acos(c);
b=atan2(c,d);
while(isgreaterequal(a,b)){
b=fmax(a,b);
d=log(b);
}
b=fmax(a,a);
e=log10(b);
if(islessequal(b,b)){
d=fdim(b,a);
d=atan(d);
}
else{
c=fmax(a,e);
b=ceil(d);
a=isless(b,c);
}
c=exp(c);
e=atan2(b,b);
c=log10(e);
c=log(a);
}
