#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=cos(c);
d=isgreaterequal(d,a);
a=cos(a);
d=exp(a);
while(isless(a,c)){
c=fmax(b,c);
c=acos(c);
a=isgreater(a,a);
c=atan2(c,b);
}
c=atan(d);
b=tan(d);
c=log10(b);
}
