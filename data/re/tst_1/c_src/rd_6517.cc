#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=pow(d,a);
g=cos(h);
while(islessequal(c,g)){
g=isgreater(c,c);
c=log10(a);
c=atan(a);
f=isgreater(d,e);
}
d=exp(d);
a=fmax(b,d);
b=exp(a);
h=exp(d);
}
