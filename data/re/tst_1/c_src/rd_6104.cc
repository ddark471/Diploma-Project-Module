#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=sin(f);
f=islessgreater(a,a);
b=fmax(a,c);
e=islessequal(b,a);
c=pow(b,b);
while(isless(c,f)){
e=islessequal(b,c);
e=fmax(b,e);
b=tan(d);
}
e=pow(b,b);
a=cos(f);
f=islessequal(d,d);
d=atan2(f,f);
}
