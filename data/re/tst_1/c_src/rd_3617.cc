#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=islessequal(a,e);
c=pow(a,g);
while(islessequal(g,b)){
e=fmax(e,d);
g=pow(c,a);
c=isgreater(b,b);
g=acos(b);
}
a=ceil(c);
b=tan(b);
}
