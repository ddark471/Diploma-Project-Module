#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=ceil(a);
e=fmax(b,f);
h=fmin(g,a);
e=atan(h);
g=sin(g);
while(isgreaterequal(c,g)){
h=floor(b);
f=sqrt(d);
c=atan(e);
}
d=sqrt(g);
g=floor(f);
d=fmax(a,h);
b=isgreaterequal(b,f);
}
