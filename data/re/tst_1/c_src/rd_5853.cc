#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fmax(d,e);
f=islessgreater(e,g);
g=fmax(g,c);
h=islessequal(a,h);
c=islessgreater(d,a);
while(isgreaterequal(a,b)){
d=pow(c,d);
d=pow(g,f);
a=tan(e);
c=pow(f,h);
}
a=fmin(b,c);
c=exp(b);
c=exp(d);
}
