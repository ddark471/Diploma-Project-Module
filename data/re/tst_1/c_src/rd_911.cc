#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=fmax(c,e);
d=log10(c);
c=sqrt(e);
while(isgreaterequal(b,e)){
b=sqrt(c);
e=fmax(b,c);
}
a=log(b);
c=acos(c);
c=sqrt(c);
b=isgreater(b,a);
}
