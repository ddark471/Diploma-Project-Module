#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fmax(c,b);
d=cos(d);
d=islessgreater(b,a);
while(islessequal(b,a)){
c=islessgreater(c,d);
a=isgreater(d,a);
c=fmax(a,d);
b=log(a);
}
c=atan(d);
d=ceil(a);
d=fmin(c,a);
b=islessequal(a,c);
a=sin(d);
}
