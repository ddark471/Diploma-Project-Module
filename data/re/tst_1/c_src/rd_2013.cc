#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=sin(c);
b=sin(g);
c=floor(b);
while(islessequal(c,d)){
e=isless(d,a);
d=fdim(c,g);
}
a=exp(d);
a=isgreaterequal(a,d);
c=fmax(d,c);
d=fmin(b,d);
}
