#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=islessgreater(a,e);
b=log(c);
f=isgreater(c,a);
f=fmax(e,b);
a=fmin(e,c);
while(islessgreater(b,b)){
a=exp(f);
d=fmax(b,f);
}
e=sin(c);
b=fmin(c,c);
d=fdim(a,a);
}
