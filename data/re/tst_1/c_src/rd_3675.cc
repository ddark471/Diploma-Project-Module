#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=exp(a);
d=fmax(d,c);
c=sqrt(e);
if(isless(a,a)){
f=log10(c);
a=islessgreater(d,a);
f=isgreater(d,b);
}
else{
d=ceil(e);
b=ceil(f);
}
b=fmax(d,e);
e=pow(a,d);
d=isgreater(a,d);
while(islessgreater(e,b)){
e=islessgreater(f,f);
a=sin(b);
}
d=pow(d,b);
e=isless(b,b);
e=fmax(a,b);
e=islessequal(d,e);
a=atan2(a,e);
}
