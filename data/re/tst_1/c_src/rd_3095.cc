#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=tan(b);
c=log10(a);
c=isless(a,a);
b=log(a);
d=isless(d,d);
while(islessequal(c,a)){
d=fmin(c,d);
d=ceil(b);
}
a=isless(c,b);
c=isgreater(c,d);
}
