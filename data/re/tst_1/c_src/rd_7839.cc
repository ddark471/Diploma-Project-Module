#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=cos(a);
b=fmax(d,c);
a=pow(b,d);
while(isless(c,b)){
d=fmax(c,d);
b=islessgreater(b,c);
}
b=sqrt(c);
a=isgreater(c,a);
d=islessgreater(b,d);
c=log(c);
a=log(c);
}
