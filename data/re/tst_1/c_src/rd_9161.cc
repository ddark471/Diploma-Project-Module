#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=pow(f,d);
f=fmax(c,b);
d=ceil(f);
while(islessgreater(a,f)){
f=fmin(c,e);
b=pow(e,f);
}
b=sqrt(e);
a=islessequal(b,e);
e=isgreaterequal(g,b);
a=atan(d);
if(islessequal(e,a)){
f=log10(b);
a=islessgreater(c,d);
f=acos(g);
}
else{
g=islessequal(f,e);
c=sin(c);
}
b=fmax(f,e);
g=isless(g,f);
}
