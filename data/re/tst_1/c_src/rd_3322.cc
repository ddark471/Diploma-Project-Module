#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=atan(b);
g=asin(c);
g=isless(d,d);
if(islessgreater(d,h)){
h=fmax(c,e);
h=isgreaterequal(f,a);
}
else{
f=exp(c);
e=pow(f,f);
}
a=isgreater(g,c);
f=cos(a);
e=sqrt(c);
d=islessgreater(e,c);
h=ceil(h);
while(isless(d,g)){
g=asin(d);
f=floor(a);
e=fmax(f,b);
e=log10(f);
}
a=isgreater(d,d);
d=asin(c);
d=isless(c,a);
c=log10(d);
}
