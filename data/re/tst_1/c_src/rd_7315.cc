#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=islessequal(b,c);
b=islessequal(c,b);
while(islessequal(a,a)){
b=isgreater(a,a);
d=isless(b,b);
c=pow(a,b);
a=floor(d);
b=floor(b);
}
c=floor(c);
a=islessequal(b,b);
c=atan(a);
c=acos(b);
d=atan2(a,b);
}
