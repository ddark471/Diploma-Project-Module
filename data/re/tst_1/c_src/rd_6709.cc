#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=atan2(f,g);
b=isgreater(g,b);
while(isgreaterequal(b,d)){
d=atan(d);
e=ceil(a);
g=cos(g);
}
f=isgreater(e,e);
f=islessequal(d,e);
a=acos(g);
f=fmax(a,e);
}
