#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fmin(b,a);
b=ceil(c);
while(islessequal(c,d)){
c=atan(d);
c=exp(d);
b=islessequal(b,b);
d=sin(b);
d=isgreater(b,b);
}
c=floor(b);
b=sin(a);
a=exp(a);
c=exp(d);
}
