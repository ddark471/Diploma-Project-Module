#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=isless(c,g);
b=atan(d);
g=islessgreater(h,f);
b=exp(h);
f=fmin(h,b);
while(isless(d,b)){
g=pow(f,e);
c=islessgreater(b,h);
e=atan(c);
}
a=floor(c);
h=exp(h);
f=islessgreater(d,a);
}
