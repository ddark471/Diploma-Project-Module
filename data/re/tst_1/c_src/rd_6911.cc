#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=acos(f);
a=exp(d);
h=pow(h,d);
d=cos(g);
d=exp(b);
while(islessequal(f,d)){
e=ceil(h);
g=islessequal(e,a);
d=atan(a);
h=sqrt(a);
b=isless(g,g);
}
c=fmax(f,e);
a=log(g);
d=log10(c);
e=islessequal(d,c);
}
