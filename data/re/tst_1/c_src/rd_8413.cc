#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=atan2(b,a);
a=islessgreater(g,d);
while(isless(f,a)){
d=fmax(e,b);
g=isgreater(f,g);
}
c=fdim(d,e);
g=cos(d);
b=islessgreater(d,f);
f=isgreaterequal(g,d);
d=log10(c);
}
