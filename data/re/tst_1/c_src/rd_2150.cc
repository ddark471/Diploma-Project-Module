#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=isgreater(b,d);
a=fmax(d,c);
while(isless(b,c)){
c=isgreaterequal(a,b);
b=atan2(b,a);
a=atan(c);
}
c=exp(c);
a=isgreater(a,a);
}
