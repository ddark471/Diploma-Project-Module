#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=sin(c);
g=sin(b);
d=fdim(a,g);
if(isless(f,a)){
b=sin(f);
d=fdim(g,a);
e=atan(c);
e=cos(b);
}
e=pow(a,f);
d=isless(a,a);
while(islessgreater(f,e)){
a=isgreater(b,e);
b=fmin(f,b);
c=islessequal(a,a);
e=log(b);
b=atan(e);
}
b=fmax(c,b);
d=ceil(a);
c=atan(b);
a=fdim(c,a);
if(isgreaterequal(b,a)){
c=log(b);
a=fmax(b,d);
}
d=floor(a);
b=isless(d,b);
c=pow(c,a);
a=fmax(c,b);
}
