#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=isless(f,a);
a=atan(b);
a=atan(b);
e=isless(h,b);
while(islessequal(h,g)){
b=isgreater(e,f);
d=fmax(d,d);
b=islessgreater(d,f);
}
f=fmin(f,a);
b=fmax(e,d);
}
