#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=exp(d);
c=ceil(c);
while(isless(b,b)){
d=log10(a);
a=cos(d);
a=islessgreater(b,a);
d=islessequal(a,a);
a=fmax(d,d);
}
f=exp(c);
e=isgreater(b,f);
g=sqrt(b);
b=sin(b);
f=exp(a);
}
