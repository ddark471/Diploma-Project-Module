#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=isgreater(a,c);
a=islessequal(a,d);
c=acos(d);
a=exp(c);
while(islessgreater(d,b)){
d=atan2(a,d);
b=pow(d,b);
c=log10(c);
}
c=floor(d);
c=sin(d);
}
