#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fmax(f,d);
d=cos(c);
while(islessgreater(c,g)){
g=pow(c,d);
f=tan(a);
c=cos(g);
e=exp(g);
}
h=floor(c);
a=tan(g);
}
