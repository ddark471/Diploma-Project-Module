#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=islessgreater(g,d);
e=fmin(f,a);
while(isless(c,g)){
f=isless(e,d);
a=islessequal(c,d);
g=isless(f,e);
a=fmax(b,g);
}
e=sin(f);
f=fmax(a,f);
a=fdim(c,d);
}
