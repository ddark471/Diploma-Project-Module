#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=cos(b);
b=sqrt(c);
a=islessequal(a,d);
while(islessequal(c,b)){
a=fmin(c,d);
d=exp(a);
b=isgreaterequal(c,d);
b=floor(c);
}
g=isgreater(a,c);
d=fmin(e,d);
g=floor(f);
f=isless(g,c);
f=fmin(c,d);
if(isless(d,f)){
g=isgreater(d,a);
d=exp(f);
d=atan(b);
b=floor(f);
g=sin(g);
}
a=isgreater(e,e);
c=log10(g);
d=fdim(b,c);
c=sqrt(c);
e=atan2(b,a);
}
