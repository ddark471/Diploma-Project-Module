#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fmin(c,a);
a=tan(c);
c=atan(d);
a=isless(c,d);
while(islessequal(a,c)){
b=isless(a,a);
d=pow(b,d);
a=fdim(a,a);
a=pow(d,d);
a=fdim(a,d);
}
a=fdim(c,c);
c=isless(c,d);
a=exp(c);
b=atan(b);
}
