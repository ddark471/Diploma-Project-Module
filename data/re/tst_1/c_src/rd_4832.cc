#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=asin(a);
b=sqrt(b);
while(islessgreater(d,c)){
d=exp(a);
a=ceil(b);
}
a=exp(b);
a=ceil(b);
}
