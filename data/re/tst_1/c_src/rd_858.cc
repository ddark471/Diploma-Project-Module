#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=isless(d,d);
c=islessequal(d,c);
e=isgreater(a,c);
a=atan(d);
f=atan(c);
while(isless(a,a)){
f=isgreater(c,a);
b=log(a);
c=isgreaterequal(e,b);
}
b=exp(c);
d=isless(f,a);
f=islessequal(e,f);
}
