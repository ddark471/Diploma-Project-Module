#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=tan(c);
d=ceil(b);
d=tan(b);
d=isgreater(d,b);
while(isgreaterequal(d,b)){
c=acos(c);
d=fmax(c,b);
c=log(a);
a=isgreater(b,d);
}
b=isgreater(a,b);
c=fmax(d,a);
c=fmin(a,c);
}
