#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=pow(e,c);
a=isgreater(e,b);
while(isgreaterequal(b,a)){
c=fmin(c,a);
e=log(e);
}
a=log10(a);
d=isgreater(e,e);
a=fmax(e,e);
}
