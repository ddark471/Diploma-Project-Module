#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=acos(e);
e=log(f);
c=acos(g);
while(islessequal(h,f)){
d=fmin(d,c);
d=islessgreater(a,h);
d=log10(b);
a=islessequal(c,f);
f=islessequal(c,b);
}
d=pow(a,b);
h=floor(a);
c=exp(g);
e=acos(b);
}
