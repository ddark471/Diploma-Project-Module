#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=asin(c);
e=fmax(g,h);
if(isless(g,b)){
c=pow(e,h);
c=fdim(a,c);
}
b=pow(d,g);
a=log(f);
d=isgreaterequal(c,e);
while(islessgreater(a,c)){
g=asin(f);
b=pow(a,d);
b=fmax(h,g);
}
f=fmin(c,c);
e=pow(c,d);
d=fdim(a,d);
f=atan(e);
b=fdim(b,f);
}
