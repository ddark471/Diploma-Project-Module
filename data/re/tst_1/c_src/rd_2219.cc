#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=log(a);
f=tan(e);
while(isgreaterequal(a,b)){
g=sqrt(g);
g=fmax(h,e);
f=log(c);
d=fmin(b,g);
c=floor(e);
}
f=isgreaterequal(h,b);
h=ceil(f);
h=pow(c,b);
a=floor(b);
}
