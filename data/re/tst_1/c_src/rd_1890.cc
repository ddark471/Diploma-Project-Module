#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=atan2(b,b);
c=sqrt(b);
e=isgreater(b,g);
d=tan(e);
g=asin(g);
while(islessgreater(c,b)){
a=fdim(b,a);
e=ceil(g);
}
d=atan(d);
f=sqrt(e);
}
