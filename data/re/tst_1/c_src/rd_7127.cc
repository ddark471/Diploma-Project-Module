#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=pow(c,f);
d=pow(f,b);
a=pow(c,f);
e=isgreaterequal(e,b);
while(islessequal(a,b)){
e=isgreater(a,a);
d=pow(e,a);
a=asin(c);
c=isgreater(a,e);
}
d=log10(d);
f=fdim(b,e);
}
