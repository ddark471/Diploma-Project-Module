#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=fdim(e,c);
g=atan2(e,d);
c=log10(b);
while(isless(e,a)){
d=islessgreater(b,f);
f=islessgreater(b,e);
f=islessgreater(c,e);
}
f=isless(d,d);
b=cos(g);
d=isless(e,a);
d=log(a);
f=atan(g);
}
