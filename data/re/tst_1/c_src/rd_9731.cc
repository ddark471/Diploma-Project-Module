#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=exp(d);
b=asin(c);
d=exp(b);
while(islessequal(c,c)){
b=atan2(c,b);
b=atan2(a,c);
}
b=isless(c,c);
a=isgreaterequal(a,d);
d=pow(d,b);
}
