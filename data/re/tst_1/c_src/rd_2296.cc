#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=isgreaterequal(d,e);
a=fmax(d,e);
d=islessequal(b,f);
while(isgreaterequal(b,b)){
d=log(f);
d=acos(e);
}
b=pow(a,c);
d=sqrt(a);
c=tan(c);
c=islessequal(a,a);
c=fmax(a,b);
}
