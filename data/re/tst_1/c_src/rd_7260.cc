#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=exp(b);
a=log10(d);
if(islessequal(b,a)){
a=isless(d,b);
a=isgreaterequal(b,c);
c=log10(c);
a=sqrt(a);
d=islessgreater(c,a);
}
c=floor(d);
d=floor(a);
b=islessgreater(c,c);
while(isgreaterequal(a,a)){
b=ceil(c);
d=fmax(c,d);
}
a=islessgreater(c,c);
g=atan(a);
}
