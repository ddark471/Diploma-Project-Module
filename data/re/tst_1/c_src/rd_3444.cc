#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=pow(c,b);
d=log10(c);
a=isless(b,b);
d=isgreaterequal(c,b);
while(islessequal(d,c)){
d=islessgreater(b,d);
a=isgreaterequal(d,a);
c=sqrt(b);
a=fmin(c,d);
c=atan2(c,b);
}
b=ceil(d);
c=log(d);
c=ceil(a);
d=asin(c);
a=isgreater(d,b);
}
