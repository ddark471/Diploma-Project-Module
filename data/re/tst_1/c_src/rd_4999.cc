#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=exp(b);
c=fmax(d,a);
while(isless(c,b)){
a=atan2(c,c);
a=islessgreater(a,b);
}
a=log10(d);
a=islessgreater(d,d);
}
