#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=atan2(b,e);
e=atan(a);
while(isless(a,c)){
b=sqrt(a);
c=pow(a,b);
}
a=islessgreater(c,a);
d=asin(c);
c=tan(b);
}
