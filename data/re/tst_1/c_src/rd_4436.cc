#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=sqrt(a);
a=isless(c,c);
d=isless(a,b);
b=log10(b);
d=isgreater(c,c);
while(isless(d,c)){
a=atan(a);
b=islessgreater(b,d);
}
b=isless(d,a);
a=ceil(d);
b=atan(c);
}
