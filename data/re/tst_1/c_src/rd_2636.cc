#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=atan2(a,c);
b=log(c);
d=cos(a);
c=fmax(c,b);
while(islessgreater(d,c)){
a=cos(b);
c=atan2(d,b);
}
b=pow(c,d);
c=acos(a);
}
