#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=log10(b);
e=fmax(d,g);
b=exp(d);
while(isless(g,c)){
c=log(e);
d=fdim(d,b);
}
c=fmax(g,c);
g=sqrt(g);
g=pow(d,c);
f=asin(c);
g=fdim(d,f);
}
