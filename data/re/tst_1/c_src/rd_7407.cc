#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=ceil(e);
d=isgreater(a,d);
c=islessequal(b,d);
d=floor(e);
while(isgreaterequal(d,b)){
a=atan(a);
d=islessgreater(b,c);
d=log(d);
}
d=fdim(a,d);
a=fmax(a,e);
a=islessequal(a,d);
}
