#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=log(g);
b=floor(d);
if(isgreaterequal(b,d)){
c=floor(d);
e=pow(g,a);
b=floor(b);
h=isgreaterequal(a,b);
}
f=isgreater(e,f);
c=cos(h);
e=sqrt(a);
h=atan(h);
a=ceil(c);
while(isless(g,a)){
b=fmin(f,c);
f=exp(g);
d=floor(a);
}
a=sqrt(c);
d=asin(e);
}
