#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=cos(e);
e=log(f);
g=atan2(b,b);
while(isgreaterequal(a,b)){
h=isgreater(c,h);
e=islessgreater(e,a);
c=isless(d,h);
}
f=isgreater(a,a);
e=sin(e);
e=fmax(b,c);
}
