#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=isless(d,d);
d=isgreater(b,b);
a=sqrt(d);
while(isless(c,b)){
b=isless(c,d);
a=log(a);
a=sqrt(d);
}
d=pow(d,b);
a=sqrt(b);
d=isgreaterequal(c,c);
a=log(c);
}
