#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=exp(g);
c=floor(h);
d=isgreater(h,g);
while(isgreaterequal(b,a)){
d=log(h);
f=tan(h);
e=isless(f,h);
g=isgreater(g,d);
a=sqrt(e);
}
d=sqrt(h);
c=sqrt(h);
if(isgreaterequal(e,a)){
c=sqrt(h);
c=isless(b,b);
}
else{
h=log(g);
c=log10(d);
d=fmin(b,e);
e=atan(e);
c=pow(e,d);
}
b=pow(c,a);
b=log(e);
c=isgreater(b,d);
b=log(d);
}
