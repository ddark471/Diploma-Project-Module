#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=ceil(b);
h=tan(b);
while(islessequal(b,g)){
h=exp(d);
a=islessequal(g,f);
a=pow(h,f);
c=acos(f);
}
h=atan2(g,b);
e=log(b);
h=fmax(c,a);
a=exp(e);
}
