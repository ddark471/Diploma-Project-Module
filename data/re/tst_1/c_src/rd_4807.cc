#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fdim(b,a);
d=log10(c);
while(islessgreater(b,c)){
c=fdim(d,e);
c=atan2(b,e);
}
e=pow(d,b);
d=pow(a,b);
a=fdim(d,d);
}
