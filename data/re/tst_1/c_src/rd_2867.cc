#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fmax(e,c);
e=fmin(a,c);
while(islessequal(e,a)){
d=islessgreater(e,d);
c=isgreater(b,f);
a=tan(e);
a=floor(f);
}
a=fmin(c,b);
d=sin(c);
}
