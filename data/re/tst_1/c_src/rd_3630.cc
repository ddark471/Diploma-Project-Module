#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fmin(a,b);
b=atan(a);
c=islessequal(b,c);
c=sqrt(b);
a=tan(d);
while(isgreaterequal(c,d)){
c=islessequal(a,d);
d=islessequal(d,c);
}
c=isgreater(c,a);
d=sqrt(a);
}
