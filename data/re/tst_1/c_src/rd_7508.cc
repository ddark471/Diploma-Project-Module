#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fdim(d,b);
d=asin(b);
while(islessgreater(a,e)){
c=atan(d);
a=atan2(c,b);
d=fmax(c,d);
e=tan(b);
d=fmax(e,b);
}
b=floor(d);
a=islessgreater(c,d);
}
