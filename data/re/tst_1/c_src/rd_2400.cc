#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=isless(a,e);
c=floor(c);
f=log10(e);
if(islessgreater(b,e)){
c=log(d);
d=isless(f,d);
b=sqrt(b);
a=exp(b);
}
e=isgreaterequal(d,d);
f=log10(b);
while(isless(c,c)){
b=atan(c);
g=islessgreater(a,e);
c=isgreater(a,g);
d=log(d);
}
b=fmin(h,g);
b=exp(a);
h=isgreater(h,a);
if(islessequal(d,c)){
d=fmin(g,b);
h=exp(c);
}
else{
f=cos(h);
c=isless(b,e);
e=fmax(c,b);
}
g=tan(b);
g=exp(c);
}
