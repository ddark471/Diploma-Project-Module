#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=atan(d);
b=sqrt(c);
while(isless(a,e)){
c=sin(a);
d=cos(a);
}
f=fmax(d,f);
a=isgreater(c,b);
b=isgreater(c,f);
f=asin(e);
}
