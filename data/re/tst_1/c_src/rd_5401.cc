#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=islessgreater(b,b);
b=fmax(d,d);
c=cos(b);
while(islessequal(c,a)){
c=islessequal(a,a);
c=fmax(a,d);
d=isgreater(a,c);
}
a=sqrt(c);
b=asin(a);
}
