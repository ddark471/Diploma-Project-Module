#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=exp(c);
b=asin(e);
e=atan(c);
while(isless(b,a)){
a=fmin(e,a);
d=atan2(a,a);
e=isgreater(d,f);
a=log(e);
}
f=asin(a);
d=fmin(f,b);
g=atan(a);
f=pow(e,g);
if(islessequal(f,e)){
d=fmax(a,d);
b=fdim(a,a);
}
e=fmax(d,e);
c=acos(g);
c=pow(d,d);
}
