#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=asin(d);
d=tan(a);
a=fmax(f,d);
while(islessgreater(a,a)){
c=fmax(e,d);
e=isless(e,f);
g=fmax(g,b);
}
d=log10(b);
d=isgreater(a,a);
if(islessgreater(d,b)){
d=islessgreater(b,c);
d=atan(d);
a=cos(d);
a=asin(a);
}
else{
a=cos(b);
d=sqrt(b);
a=fmax(a,d);
}
b=islessequal(b,c);
b=isgreater(b,b);
c=exp(a);
a=floor(a);
d=ceil(b);
}
