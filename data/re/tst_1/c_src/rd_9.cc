#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=isgreater(a,d);
c=islessequal(c,d);
c=isgreater(a,d);
while(isgreaterequal(d,a)){
d=ceil(a);
b=log(d);
c=floor(d);
c=fmax(d,b);
c=fmin(d,b);
}
d=isless(b,a);
c=ceil(c);
a=log10(d);
}
