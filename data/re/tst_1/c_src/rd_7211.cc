#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=log(c);
b=islessgreater(c,c);
a=sqrt(a);
if(isless(a,a)){
c=sin(d);
a=isless(d,b);
a=isgreater(a,a);
a=cos(d);
c=isless(a,a);
}
c=atan(c);
d=fmin(d,a);
d=isgreaterequal(a,b);
while(islessgreater(c,c)){
d=tan(b);
c=islessequal(b,d);
c=ceil(c);
}
b=sin(e);
g=sqrt(b);
g=isgreaterequal(d,c);
if(isless(b,f)){
e=log10(b);
c=isgreaterequal(e,g);
c=atan(c);
f=islessgreater(c,a);
}
else{
d=ceil(d);
c=log10(f);
}
a=log10(d);
b=ceil(c);
g=isgreater(f,d);
}
