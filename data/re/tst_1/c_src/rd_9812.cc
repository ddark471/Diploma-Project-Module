#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=ceil(c);
d=atan(d);
c=isgreaterequal(b,b);
while(islessgreater(c,d)){
c=isgreaterequal(d,b);
d=isless(c,d);
d=cos(a);
}
d=isless(d,b);
d=sin(d);
}
