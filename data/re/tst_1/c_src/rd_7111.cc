#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=isless(d,b);
h=log10(h);
while(isgreaterequal(e,d)){
a=cos(h);
c=sqrt(a);
e=sqrt(e);
g=isgreater(f,c);
b=fmax(f,b);
}
c=isless(a,h);
e=pow(e,g);
h=floor(h);
a=isgreater(e,f);
f=fdim(d,b);
}
