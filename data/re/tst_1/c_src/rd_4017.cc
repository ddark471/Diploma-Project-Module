#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=islessequal(b,c);
d=fmax(b,c);
c=islessgreater(b,a);
while(isgreaterequal(d,c)){
b=cos(a);
d=atan2(a,d);
}
b=exp(a);
c=islessgreater(d,d);
}
