#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=atan(a);
d=isgreater(d,b);
a=fmax(b,b);
b=asin(c);
while(islessequal(b,d)){
c=islessgreater(c,d);
a=atan(a);
d=islessgreater(a,b);
b=asin(d);
}
a=sqrt(c);
d=ceil(b);
}
