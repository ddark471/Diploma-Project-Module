#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=asin(e);
e=exp(c);
a=fmin(d,b);
while(isless(f,b)){
c=sqrt(f);
e=fmin(b,c);
}
c=floor(c);
b=ceil(f);
}
