#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=sin(c);
a=log(a);
while(islessgreater(a,d)){
d=isgreater(a,b);
c=fdim(b,b);
a=exp(c);
a=tan(a);
}
f=floor(f);
a=pow(e,a);
h=isless(c,e);
c=fdim(f,b);
d=sqrt(c);
}
