#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=ceil(c);
d=islessgreater(c,d);
d=isless(b,d);
while(islessgreater(c,c)){
d=islessgreater(a,d);
a=atan2(d,a);
a=atan(b);
c=isgreaterequal(a,c);
}
d=isless(b,b);
b=ceil(a);
}
