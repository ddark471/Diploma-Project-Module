#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=log(d);
a=pow(c,b);
a=isgreaterequal(b,d);
while(isless(c,b)){
a=isgreater(d,a);
c=fdim(d,c);
a=pow(c,d);
b=islessgreater(c,a);
}
d=islessgreater(d,b);
c=exp(a);
}
