#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fmin(b,c);
d=islessgreater(a,c);
c=islessequal(b,d);
while(islessgreater(b,b)){
a=isgreater(a,b);
b=isless(c,d);
c=log(a);
a=tan(b);
d=fmin(c,a);
}
d=isless(a,b);
c=fmax(d,b);
a=isgreaterequal(c,a);
b=fmax(a,b);
b=islessequal(a,a);
}
