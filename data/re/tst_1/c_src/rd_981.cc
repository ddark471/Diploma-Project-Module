#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=tan(g);
e=atan2(c,c);
while(islessequal(c,g)){
c=fmax(f,g);
f=fmin(h,h);
}
b=tan(a);
f=isless(d,d);
b=atan(b);
if(isless(g,g)){
d=cos(c);
a=islessequal(d,g);
f=atan(e);
}
else{
f=fmin(f,a);
d=islessgreater(e,b);
}
b=tan(f);
c=islessgreater(e,g);
}
