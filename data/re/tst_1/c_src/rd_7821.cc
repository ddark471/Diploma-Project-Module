#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=exp(b);
c=log10(e);
e=pow(b,b);
while(islessgreater(c,c)){
c=log(d);
b=sqrt(d);
d=fmax(e,e);
e=fmax(e,c);
b=islessgreater(b,b);
}
d=islessgreater(e,a);
e=asin(a);
}
