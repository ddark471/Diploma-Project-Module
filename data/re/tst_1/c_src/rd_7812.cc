#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fmax(e,a);
a=atan(b);
e=isless(c,a);
e=sqrt(c);
while(islessequal(e,c)){
e=isless(e,e);
c=log(c);
b=fmax(c,c);
}
d=isless(b,a);
c=islessequal(e,d);
d=log10(d);
}
