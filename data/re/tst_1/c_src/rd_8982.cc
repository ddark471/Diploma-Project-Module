#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=exp(c);
c=islessequal(a,d);
while(islessgreater(c,a)){
c=isless(d,c);
b=fdim(b,b);
}
c=atan(a);
b=isgreaterequal(a,c);
}
