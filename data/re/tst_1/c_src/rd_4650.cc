#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=pow(b,c);
d=atan2(a,c);
b=islessequal(a,c);
while(islessgreater(d,b)){
b=islessequal(c,d);
c=fmax(a,a);
}
b=cos(d);
d=acos(a);
}
