#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=asin(d);
b=pow(b,a);
c=atan2(b,a);
while(isless(d,a)){
a=fmin(c,d);
c=sin(b);
}
b=isgreater(a,d);
a=floor(b);
d=fdim(b,a);
d=ceil(b);
}
