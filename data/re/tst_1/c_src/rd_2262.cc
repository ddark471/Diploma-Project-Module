#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=exp(b);
d=tan(b);
while(isgreaterequal(c,d)){
a=fmin(d,b);
c=fmax(b,c);
}
c=islessgreater(d,a);
d=isgreater(c,b);
}
