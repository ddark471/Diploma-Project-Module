#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=log10(e);
b=islessgreater(c,b);
if(isgreaterequal(d,c)){
c=cos(g);
g=pow(a,a);
}
c=isgreaterequal(a,e);
d=asin(g);
while(islessequal(f,c)){
f=pow(a,d);
b=isgreater(g,g);
f=islessgreater(f,a);
d=pow(a,b);
}
g=isgreaterequal(d,g);
h=atan2(h,g);
d=floor(f);
f=tan(d);
d=isgreater(b,e);
if(isless(b,g)){
b=exp(a);
a=fmax(e,e);
}
f=isgreaterequal(b,e);
b=acos(h);
a=exp(c);
}
