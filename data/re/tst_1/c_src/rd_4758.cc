#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=log10(d);
a=fmax(b,b);
e=pow(b,e);
while(islessgreater(b,f)){
f=acos(d);
c=isless(a,b);
}
b=exp(a);
d=fmin(b,a);
}
