#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=isless(a,c);
a=pow(c,c);
b=fmax(d,d);
e=isless(b,b);
while(islessequal(a,b)){
e=fmin(c,e);
b=isless(e,a);
a=pow(a,a);
a=atan(c);
b=log(c);
}
d=pow(a,a);
a=asin(a);
}
