#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=isless(a,g);
f=exp(c);
d=isgreater(e,b);
d=isless(c,d);
c=isless(a,a);
while(isgreaterequal(c,a)){
e=log10(a);
c=exp(d);
}
b=fmax(a,d);
f=sqrt(e);
b=isless(f,f);
c=log(g);
}
