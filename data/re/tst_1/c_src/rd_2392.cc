#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=ceil(b);
c=fmin(b,d);
if(isgreaterequal(a,b)){
c=ceil(a);
a=log10(d);
d=log10(c);
}
else{
d=atan2(c,b);
d=pow(b,a);
}
c=log10(a);
c=asin(a);
while(isgreaterequal(b,c)){
a=exp(a);
c=islessequal(a,d);
a=floor(d);
d=isgreater(d,b);
a=isgreaterequal(c,d);
}
d=isgreater(d,b);
f=floor(f);
if(isgreaterequal(f,e)){
d=islessequal(e,d);
c=pow(a,a);
a=isless(b,a);
a=log(b);
}
d=cos(c);
b=log(d);
}
