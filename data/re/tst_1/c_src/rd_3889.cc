#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=pow(a,a);
a=isgreater(b,b);
a=fmin(a,d);
while(islessequal(a,a)){
a=isless(b,a);
b=isless(d,c);
}
b=fmin(b,d);
c=asin(a);
d=sin(c);
c=floor(a);
a=sin(c);
}
