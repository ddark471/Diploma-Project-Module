#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=sin(b);
c=sqrt(c);
while(isgreaterequal(d,d)){
a=fmin(a,d);
d=exp(b);
b=fmax(a,a);
c=pow(d,b);
}
a=islessgreater(c,d);
a=isless(d,c);
}
