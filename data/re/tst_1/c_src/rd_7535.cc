#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=islessequal(c,b);
b=fmax(a,d);
a=fmax(a,a);
c=islessequal(e,b);
while(islessgreater(d,b)){
e=log(b);
d=cos(d);
e=log(a);
d=sqrt(b);
}
e=fdim(c,d);
d=log(d);
c=log(c);
a=fdim(c,c);
}
