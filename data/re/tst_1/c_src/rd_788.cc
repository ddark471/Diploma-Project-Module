#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=sin(a);
e=cos(d);
while(isless(d,a)){
d=isless(a,d);
c=pow(d,c);
d=fmax(c,e);
b=fmin(b,a);
}
d=fmax(e,b);
b=fmin(b,a);
}
