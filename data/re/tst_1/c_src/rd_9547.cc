#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=isless(e,g);
f=fmax(e,b);
h=islessequal(g,e);
while(isgreaterequal(h,g)){
d=fdim(f,g);
c=acos(f);
}
c=fdim(a,h);
h=sqrt(f);
f=acos(b);
g=isless(d,b);
g=sin(h);
}
