#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=tan(c);
c=atan(d);
c=acos(h);
d=log10(f);
while(islessequal(c,b)){
a=floor(f);
b=fmax(c,e);
}
e=log10(g);
d=isgreater(h,b);
c=floor(a);
}
