#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=isgreater(c,b);
d=floor(c);
while(islessgreater(f,e)){
b=pow(c,f);
g=atan2(e,e);
f=ceil(b);
}
b=ceil(g);
g=pow(f,f);
c=atan2(b,g);
}
