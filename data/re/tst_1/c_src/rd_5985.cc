#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=pow(b,c);
c=log10(c);
while(islessequal(b,a)){
d=atan(d);
a=isless(c,a);
}
d=atan2(d,a);
a=asin(c);
b=ceil(c);
c=log10(a);
}
