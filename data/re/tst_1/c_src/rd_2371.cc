#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=atan2(b,a);
d=fdim(b,g);
while(islessequal(d,a)){
g=isgreaterequal(d,g);
a=fmax(b,c);
g=log(g);
}
b=exp(e);
f=isgreater(g,f);
}
