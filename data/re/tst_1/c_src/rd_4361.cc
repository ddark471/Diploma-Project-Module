#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=acos(f);
g=islessequal(b,d);
c=tan(g);
while(islessgreater(f,a)){
g=fmin(d,c);
g=log10(f);
f=acos(f);
a=isgreaterequal(a,f);
}
b=asin(e);
g=log10(g);
c=sqrt(c);
if(islessgreater(d,c)){
b=pow(g,a);
d=fmax(g,d);
b=log10(a);
}
g=asin(d);
d=asin(g);
}
