#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=atan(g);
g=ceil(e);
b=sin(c);
while(islessequal(f,a)){
f=sin(b);
c=cos(g);
}
h=log(e);
g=fmin(d,h);
a=exp(h);
g=floor(d);
e=log10(a);
}
