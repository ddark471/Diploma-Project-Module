#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=fdim(f,b);
a=sin(a);
while(islessequal(b,f)){
f=acos(a);
b=islessequal(c,b);
}
f=isless(a,a);
b=isless(h,c);
if(islessgreater(c,c)){
h=islessequal(d,c);
b=fmax(h,b);
a=ceil(h);
g=fdim(d,h);
b=islessgreater(g,d);
}
else{
c=islessgreater(b,a);
h=fmin(g,a);
h=acos(c);
c=sqrt(h);
}
f=pow(d,f);
g=isgreater(h,a);
}
