#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=acos(f);
e=floor(h);
h=pow(c,b);
while(isless(h,f)){
c=fmax(b,e);
g=isgreater(a,g);
}
d=cos(c);
e=fmax(h,f);
h=exp(h);
d=islessequal(d,c);
g=pow(c,f);
}
