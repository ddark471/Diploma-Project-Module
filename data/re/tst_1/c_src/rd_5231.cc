#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=floor(a);
e=atan2(e,f);
e=log10(e);
while(islessequal(b,c)){
f=exp(d);
d=atan2(e,d);
b=log(e);
}
b=islessequal(a,b);
b=isless(b,b);
if(islessequal(a,b)){
c=fmax(c,d);
a=fmin(a,c);
}
d=floor(b);
d=atan(d);
c=isgreaterequal(b,c);
d=atan2(c,b);
}
