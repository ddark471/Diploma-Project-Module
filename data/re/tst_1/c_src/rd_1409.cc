#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=acos(d);
b=fmax(b,c);
a=isless(b,a);
while(islessgreater(c,c)){
b=exp(d);
a=exp(d);
c=ceil(a);
}
c=islessgreater(b,c);
c=islessgreater(a,d);
a=exp(b);
c=sin(d);
}
