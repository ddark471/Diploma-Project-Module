#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=islessequal(d,c);
c=fdim(a,a);
d=isless(a,b);
a=log10(b);
while(islessgreater(d,a)){
d=isless(b,b);
a=isgreaterequal(b,b);
c=exp(c);
d=atan2(d,c);
d=pow(a,c);
}
c=tan(b);
d=sqrt(d);
}
