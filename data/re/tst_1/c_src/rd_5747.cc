#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=floor(c);
d=fmax(c,c);
c=isgreaterequal(c,a);
c=isgreater(c,d);
while(islessgreater(d,b)){
d=atan(b);
d=fdim(b,c);
}
a=fmax(d,a);
b=log10(a);
a=ceil(c);
c=islessgreater(d,d);
}
