#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=isgreaterequal(c,e);
e=fdim(b,d);
c=exp(c);
c=exp(b);
while(islessequal(c,c)){
f=isgreaterequal(e,b);
d=fmax(c,a);
c=isless(b,d);
}
c=isless(a,b);
a=isless(d,a);
d=cos(d);
}
