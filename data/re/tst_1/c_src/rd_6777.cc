#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=exp(c);
d=cos(b);
e=fmax(e,a);
c=exp(a);
c=fmax(c,e);
while(isless(b,a)){
c=isless(c,b);
e=isgreater(b,c);
b=isless(c,d);
a=fmin(c,e);
}
b=fdim(c,d);
a=fmin(a,a);
}
