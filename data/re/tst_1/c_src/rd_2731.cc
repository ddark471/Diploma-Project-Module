#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=pow(c,e);
a=log(b);
f=log10(e);
d=pow(f,e);
d=pow(c,d);
while(islessequal(a,c)){
g=isless(g,d);
f=isgreater(g,e);
}
b=sqrt(g);
e=atan2(c,b);
}
