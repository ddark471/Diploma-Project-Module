#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=exp(b);
c=log(d);
while(isgreaterequal(a,b)){
f=islessequal(b,d);
c=acos(a);
}
f=atan(b);
b=sin(c);
c=cos(a);
e=sqrt(c);
}
