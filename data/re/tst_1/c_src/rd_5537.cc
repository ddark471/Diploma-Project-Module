#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=isless(a,b);
d=fmin(e,f);
e=sqrt(a);
b=exp(c);
c=fmax(e,f);
while(isless(d,f)){
b=islessequal(f,g);
g=pow(a,b);
}
e=fdim(b,g);
a=islessequal(e,c);
}
