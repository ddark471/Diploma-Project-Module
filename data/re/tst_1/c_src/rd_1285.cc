#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=log10(d);
d=sqrt(c);
c=isless(f,e);
while(islessgreater(f,d)){
e=fdim(f,a);
a=isgreater(d,d);
e=isgreater(f,a);
}
a=sin(e);
d=exp(c);
f=fmax(b,b);
c=cos(c);
a=exp(f);
}
