#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=floor(a);
a=pow(a,b);
while(isgreaterequal(b,c)){
d=acos(a);
c=sqrt(c);
b=atan2(b,a);
}
b=atan(d);
c=atan2(d,a);
a=log(a);
}
