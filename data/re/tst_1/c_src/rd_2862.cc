#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=islessequal(d,a);
d=fmin(b,d);
c=sqrt(f);
e=sqrt(a);
while(islessgreater(c,e)){
f=isgreater(g,c);
h=isgreaterequal(d,c);
d=fmax(g,h);
a=fmin(e,h);
f=islessgreater(f,c);
}
f=isgreater(d,d);
a=log(a);
a=ceil(e);
f=log10(c);
}
