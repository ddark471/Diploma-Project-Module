#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=acos(b);
g=log10(e);
while(islessgreater(a,c)){
f=isgreaterequal(f,g);
a=fmax(g,b);
a=fmax(f,f);
}
d=pow(c,a);
g=log10(a);
}
