#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=islessgreater(f,e);
f=islessgreater(f,d);
while(isgreaterequal(b,b)){
c=islessgreater(f,d);
a=fmax(g,e);
e=log10(c);
g=floor(f);
}
f=pow(g,c);
a=atan2(g,e);
}
