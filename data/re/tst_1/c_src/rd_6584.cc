#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=log10(g);
g=pow(a,e);
h=tan(h);
g=asin(c);
h=ceil(d);
while(isless(b,a)){
f=sin(d);
d=log(g);
a=fmin(a,h);
h=acos(h);
b=sin(h);
}
e=fmax(c,e);
g=log(d);
}
