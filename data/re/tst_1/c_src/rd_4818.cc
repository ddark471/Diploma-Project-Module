#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=floor(c);
a=exp(b);
a=fdim(b,f);
g=log10(g);
while(isgreaterequal(d,f)){
e=log(b);
a=isgreaterequal(c,d);
}
g=sqrt(e);
g=fmax(e,a);
c=islessequal(a,b);
g=ceil(g);
}
