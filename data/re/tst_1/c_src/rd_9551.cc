#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fmax(d,a);
b=fmax(d,b);
b=isgreaterequal(a,b);
while(islessgreater(c,b)){
a=cos(c);
c=exp(b);
a=fdim(d,d);
b=pow(b,b);
}
d=fmin(a,c);
d=fmax(c,c);
}
