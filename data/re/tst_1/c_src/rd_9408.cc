#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=exp(f);
c=atan(e);
f=floor(e);
if(islessequal(d,d)){
e=islessequal(b,g);
b=isless(g,b);
}
else{
f=atan2(g,a);
g=islessgreater(b,a);
}
b=log10(g);
d=isgreater(b,c);
while(isgreaterequal(b,c)){
a=fmax(d,a);
a=sqrt(g);
}
c=floor(e);
a=fdim(b,e);
}
