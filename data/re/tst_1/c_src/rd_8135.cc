#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=log10(a);
c=floor(d);
d=islessequal(c,b);
while(islessgreater(d,b)){
a=isgreaterequal(b,b);
d=isless(b,c);
b=isgreater(c,c);
b=pow(b,c);
}
b=floor(b);
d=sin(c);
d=pow(a,d);
}
