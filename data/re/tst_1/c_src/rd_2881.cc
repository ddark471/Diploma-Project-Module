#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=isless(c,h);
h=tan(d);
e=isgreater(a,d);
g=floor(g);
if(isgreaterequal(e,h)){
b=exp(d);
d=fmin(d,a);
g=floor(b);
d=isgreaterequal(c,c);
g=ceil(b);
}
f=log(e);
a=islessequal(h,g);
f=sqrt(f);
while(islessequal(b,a)){
e=isgreater(f,e);
f=islessgreater(e,b);
}
a=ceil(a);
c=ceil(b);
if(isless(d,f)){
d=tan(f);
c=isgreaterequal(d,b);
}
d=log10(d);
e=asin(f);
}
