#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=floor(e);
f=fmax(f,f);
a=log10(e);
while(islessgreater(g,c)){
d=atan2(b,c);
f=fmin(g,f);
}
c=tan(f);
g=pow(g,b);
c=acos(g);
}
