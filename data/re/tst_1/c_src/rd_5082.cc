#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=ceil(b);
b=isless(g,g);
a=ceil(h);
b=islessequal(e,c);
h=fmin(b,c);
while(isgreaterequal(g,b)){
e=asin(g);
c=isgreaterequal(g,d);
b=exp(h);
}
c=floor(a);
d=fmin(a,d);
if(islessequal(b,d)){
d=log(d);
d=atan2(b,b);
c=islessequal(a,d);
d=sqrt(a);
c=floor(c);
}
else{
d=log10(a);
b=fmax(d,d);
d=islessgreater(d,b);
d=isless(c,d);
}
c=sqrt(d);
a=islessequal(b,b);
d=fmax(a,a);
}
