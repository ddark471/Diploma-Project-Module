#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=fmax(b,a);
d=cos(e);
while(isless(b,d)){
a=fmax(e,b);
d=sqrt(a);
a=atan(e);
}
e=islessgreater(d,b);
d=log10(b);
e=asin(c);
}
