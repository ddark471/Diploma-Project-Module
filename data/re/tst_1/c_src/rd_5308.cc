#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fmax(a,c);
d=islessequal(a,a);
a=isgreaterequal(a,d);
while(islessequal(b,b)){
d=isgreaterequal(b,a);
a=pow(b,d);
}
c=tan(d);
a=atan(c);
c=asin(a);
}
