#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=tan(f);
e=fmin(g,d);
if(islessgreater(e,g)){
c=islessgreater(g,c);
f=islessgreater(f,c);
e=islessgreater(b,c);
a=islessequal(a,g);
}
g=floor(f);
g=islessequal(a,b);
while(isgreaterequal(b,e)){
c=floor(b);
f=fmax(f,b);
b=exp(f);
e=atan2(g,b);
}
c=fmax(d,d);
d=islessequal(b,c);
b=sin(c);
b=floor(a);
c=pow(c,d);
}
