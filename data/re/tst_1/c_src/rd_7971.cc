#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=islessequal(b,g);
a=fdim(d,e);
while(isgreaterequal(a,f)){
d=islessgreater(a,d);
d=islessgreater(g,a);
}
g=fmin(d,h);
h=fmin(c,f);
if(isgreaterequal(c,d)){
g=islessgreater(c,g);
h=sin(h);
}
else{
d=fmin(e,f);
h=fdim(a,b);
g=tan(e);
}
g=tan(b);
g=pow(e,g);
g=log(f);
}
