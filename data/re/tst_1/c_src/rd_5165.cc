#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=asin(b);
a=sqrt(b);
f=log10(b);
a=atan2(a,g);
if(islessgreater(b,g)){
h=log(c);
h=atan2(e,e);
}
else{
a=isless(f,e);
e=fmax(c,d);
}
a=fmin(e,e);
e=isgreaterequal(d,b);
e=islessequal(f,c);
a=pow(b,f);
while(islessequal(h,f)){
c=tan(f);
c=fmax(e,a);
c=tan(c);
h=asin(c);
h=isless(a,a);
}
g=acos(h);
e=sin(c);
h=asin(h);
if(islessequal(h,e)){
h=isgreater(b,b);
d=pow(c,c);
a=exp(b);
e=islessequal(a,a);
g=sin(d);
}
c=tan(f);
d=isless(h,h);
b=atan(g);
c=log10(b);
}
