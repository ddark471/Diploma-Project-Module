#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=pow(d,f);
e=fmin(c,d);
b=fmax(e,d);
while(islessgreater(a,d)){
d=cos(a);
c=sin(e);
}
f=exp(f);
g=isgreater(d,d);
e=fmin(f,g);
d=log10(f);
if(islessequal(e,h)){
c=fmax(a,c);
b=isgreater(g,d);
f=log10(c);
f=fdim(a,c);
}
else{
f=fdim(h,a);
c=isgreater(f,e);
g=floor(f);
f=asin(a);
}
d=ceil(b);
h=isless(b,b);
h=atan(g);
g=atan2(h,a);
b=fmax(a,e);
}
