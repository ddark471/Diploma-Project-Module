#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=islessequal(c,a);
c=sqrt(a);
e=fdim(a,b);
a=exp(b);
c=tan(c);
while(islessgreater(c,e)){
a=fmax(a,e);
a=sqrt(a);
}
c=isgreater(e,a);
c=log10(d);
b=islessgreater(d,b);
a=atan2(b,d);
d=atan2(a,d);
}
