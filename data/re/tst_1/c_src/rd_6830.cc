#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=atan(a);
e=floor(a);
d=islessequal(d,c);
e=islessequal(d,b);
while(isless(b,e)){
c=islessgreater(b,c);
d=fdim(f,a);
b=floor(c);
d=fmax(e,c);
f=fmin(c,e);
}
d=log10(d);
a=log(a);
}
