#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=atan(a);
d=floor(b);
b=islessequal(c,c);
a=log10(a);
d=sqrt(c);
while(islessequal(a,b)){
a=pow(a,c);
c=sqrt(c);
}
b=pow(b,c);
a=floor(b);
c=islessequal(e,d);
}
