#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=islessequal(d,c);
g=atan2(d,d);
while(islessgreater(g,c)){
d=isgreaterequal(c,e);
b=islessgreater(c,g);
b=fmax(b,a);
}
d=tan(c);
a=ceil(c);
b=atan2(c,a);
if(isless(d,d)){
d=pow(a,a);
a=isless(b,a);
}
d=islessequal(a,d);
b=log10(c);
d=isgreater(a,c);
}
