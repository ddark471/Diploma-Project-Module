#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fdim(b,a);
d=isgreater(b,a);
a=asin(b);
c=tan(b);
while(isgreaterequal(d,b)){
a=floor(b);
b=sin(b);
c=sin(b);
a=fmin(b,a);
c=ceil(a);
}
d=fmax(a,d);
d=floor(d);
}
