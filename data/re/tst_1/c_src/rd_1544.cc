#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=isless(d,f);
b=fdim(f,b);
if(islessequal(g,g)){
b=isless(c,a);
f=isgreater(a,a);
g=atan2(f,c);
d=fmax(g,a);
g=cos(g);
}
f=pow(d,d);
h=exp(h);
while(isless(d,g)){
e=floor(h);
c=atan(f);
a=isless(c,h);
f=sin(a);
b=atan(b);
}
e=fmin(g,a);
e=log10(d);
a=atan2(d,d);
if(islessgreater(c,f)){
b=floor(c);
c=exp(g);
b=sin(a);
g=tan(g);
e=ceil(g);
}
else{
c=ceil(e);
a=log(b);
}
c=sin(f);
e=ceil(g);
d=acos(e);
c=fmax(f,e);
f=floor(g);
}
