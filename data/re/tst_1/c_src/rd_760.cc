#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=floor(e);
d=islessequal(a,b);
h=isless(b,e);
g=sin(f);
if(isgreaterequal(h,b)){
c=tan(b);
a=isgreater(b,h);
g=sin(a);
g=isgreater(b,d);
}
d=log(b);
e=fmin(h,e);
while(islessgreater(g,a)){
d=fdim(f,c);
b=fmax(e,e);
}
c=isless(b,h);
e=log(f);
if(isgreaterequal(g,g)){
b=isgreater(h,f);
c=isgreaterequal(b,c);
h=exp(e);
}
b=fmax(a,e);
b=atan2(f,d);
d=islessequal(h,h);
}
