#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=sqrt(d);
b=acos(d);
if(isless(c,e)){
g=atan(f);
d=atan2(f,b);
f=isgreaterequal(h,d);
c=exp(f);
}
else{
b=ceil(f);
f=isless(b,g);
a=cos(d);
d=fmin(d,c);
b=cos(d);
}
h=islessgreater(g,b);
b=sin(d);
f=atan(a);
while(islessequal(d,e)){
d=isless(c,g);
b=isgreater(h,a);
g=isless(g,c);
g=atan2(a,b);
}
h=log10(g);
e=islessequal(h,g);
if(islessequal(d,f)){
h=islessgreater(d,b);
c=cos(d);
}
d=islessequal(h,f);
c=isgreaterequal(c,d);
a=acos(h);
d=asin(d);
}
