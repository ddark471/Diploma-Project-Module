#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=islessequal(a,d);
e=floor(a);
while(islessequal(c,a)){
b=fmax(a,d);
d=ceil(b);
}
c=islessgreater(c,d);
a=floor(a);
}
