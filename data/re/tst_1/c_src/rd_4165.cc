#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=asin(b);
b=exp(c);
while(isless(b,b)){
d=fmax(b,b);
a=isgreaterequal(b,c);
}
b=log10(d);
a=sin(b);
}
