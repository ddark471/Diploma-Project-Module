#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=sqrt(a);
f=fmax(e,c);
f=ceil(e);
g=fmin(e,a);
f=isgreaterequal(f,g);
while(islessgreater(b,e)){
d=exp(e);
d=islessgreater(a,b);
}
d=fmax(g,b);
d=isgreaterequal(f,g);
f=atan(g);
e=isgreater(a,g);
e=islessgreater(a,a);
}
