#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=isgreater(e,c);
g=asin(f);
a=log10(c);
while(isless(f,e)){
g=log10(b);
h=ceil(g);
g=fdim(c,c);
}
b=islessequal(f,c);
e=exp(b);
d=cos(c);
d=fdim(b,f);
a=isgreater(e,c);
}
