#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=isless(b,d);
b=isgreater(c,b);
while(isgreaterequal(d,a)){
a=isgreaterequal(b,a);
d=cos(d);
c=isless(c,d);
d=islessequal(b,d);
a=exp(b);
}
d=isless(a,c);
a=sqrt(c);
d=isgreater(d,d);
}
