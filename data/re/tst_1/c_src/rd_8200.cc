#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=islessgreater(c,c);
d=isless(a,d);
while(islessequal(a,b)){
d=sin(a);
e=ceil(a);
}
d=isgreater(c,e);
c=cos(c);
}
