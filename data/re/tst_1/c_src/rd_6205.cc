#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=log(d);
a=floor(g);
a=sin(a);
while(islessequal(e,b)){
e=cos(g);
f=isgreaterequal(c,e);
e=fmax(e,b);
c=asin(g);
c=cos(b);
}
g=isless(e,c);
g=fmax(a,f);
d=log(d);
}
