#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=islessgreater(a,c);
a=islessgreater(a,c);
a=islessgreater(c,b);
d=pow(c,b);
while(islessequal(d,a)){
d=isgreater(b,c);
a=atan2(b,d);
b=log10(b);
c=exp(c);
d=fmin(a,b);
}
c=islessequal(c,b);
c=log(b);
c=sin(d);
}
