#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=isgreaterequal(a,e);
a=sqrt(d);
while(isgreaterequal(b,f)){
f=acos(a);
f=isless(c,b);
}
e=isgreater(c,a);
c=exp(c);
d=asin(d);
e=fmax(f,b);
}
