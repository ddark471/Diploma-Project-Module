#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=isgreater(c,c);
d=cos(a);
while(isgreaterequal(b,a)){
c=islessgreater(d,a);
c=pow(d,b);
}
f=isgreater(a,g);
d=fmax(g,b);
}
