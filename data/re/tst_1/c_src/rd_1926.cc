#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=exp(g);
d=log(c);
a=fdim(d,f);
while(islessequal(d,b)){
f=isless(h,b);
g=fdim(b,d);
h=islessequal(a,d);
h=atan2(f,g);
}
g=sqrt(e);
c=tan(a);
e=islessequal(e,a);
h=cos(a);
}
