#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=atan2(c,e);
f=isless(e,a);
while(islessgreater(b,c)){
b=exp(d);
f=isless(c,b);
a=atan2(f,a);
}
b=fdim(e,c);
c=isgreaterequal(d,f);
}
