#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=islessequal(c,b);
d=fmin(b,d);
a=fmax(c,a);
while(islessgreater(c,a)){
a=isgreaterequal(d,d);
c=fmin(a,a);
d=sqrt(a);
d=fdim(c,b);
c=pow(a,a);
}
a=fmax(a,d);
b=sqrt(b);
d=acos(d);
c=tan(a);
}
