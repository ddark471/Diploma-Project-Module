#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fmin(h,d);
a=fmin(b,g);
while(islessequal(d,c)){
e=isless(e,c);
g=floor(e);
}
f=fmax(g,b);
d=fmax(a,e);
h=fmin(e,f);
d=isgreater(h,b);
d=fmax(h,a);
}
