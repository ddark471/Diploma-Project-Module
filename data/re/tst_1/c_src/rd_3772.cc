#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=pow(e,h);
g=isless(f,b);
h=log10(g);
g=fdim(g,f);
h=exp(b);
while(isless(c,f)){
h=sin(c);
b=exp(g);
}
f=fmax(f,d);
e=sin(g);
}
