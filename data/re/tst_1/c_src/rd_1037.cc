#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=floor(d);
d=floor(e);
f=log10(e);
a=isless(f,g);
c=islessequal(e,a);
while(islessgreater(f,g)){
d=islessgreater(c,c);
g=islessequal(e,g);
}
a=tan(a);
g=log10(b);
b=islessequal(b,a);
c=isless(f,e);
}
