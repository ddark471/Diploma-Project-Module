#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=sqrt(d);
e=log(d);
b=fmin(d,d);
a=pow(g,d);
if(isless(e,g)){
c=isgreaterequal(g,c);
d=fmin(f,b);
g=atan2(h,a);
}
c=exp(b);
c=tan(h);
c=fmin(c,h);
h=log(h);
g=fdim(a,g);
while(islessequal(h,g)){
h=pow(g,h);
a=asin(h);
c=isless(f,d);
h=isgreater(g,a);
}
g=isless(e,g);
a=fmin(a,g);
a=ceil(a);
f=atan2(a,c);
if(isless(g,f)){
e=atan(e);
c=isgreaterequal(d,b);
}
a=atan2(b,a);
g=fmin(b,c);
g=islessequal(c,f);
c=sqrt(a);
}
