#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=log(c);
f=isless(d,d);
a=fmin(g,c);
e=islessgreater(b,d);
a=log10(f);
if(isgreaterequal(a,c)){
c=sqrt(c);
c=islessgreater(f,g);
a=fmin(b,c);
e=log(b);
}
else{
e=isgreater(e,a);
a=pow(d,e);
}
f=fmin(b,g);
d=islessequal(d,b);
g=atan2(f,f);
f=log(d);
while(isless(d,f)){
a=cos(g);
e=islessgreater(a,c);
}
b=pow(c,c);
b=fmin(f,c);
}
