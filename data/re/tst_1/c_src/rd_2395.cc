#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=exp(a);
f=isless(f,c);
while(islessequal(a,d)){
c=cos(f);
a=acos(e);
d=acos(f);
}
d=atan2(c,c);
d=log10(f);
}
