#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=fmin(c,e);
f=cos(c);
while(isgreaterequal(f,d)){
c=isgreaterequal(b,d);
b=isless(b,f);
d=isless(f,d);
c=pow(b,e);
c=atan(c);
}
d=acos(d);
c=exp(b);
b=fmax(a,a);
d=ceil(b);
if(isgreaterequal(c,a)){
c=pow(d,b);
d=fmax(a,b);
}
else{
c=pow(d,c);
a=exp(d);
c=fmin(b,b);
c=isless(d,a);
}
d=atan2(b,a);
c=sin(b);
b=islessequal(c,c);
}
