#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=atan2(b,a);
f=atan2(f,b);
a=log10(b);
c=exp(d);
while(islessgreater(a,b)){
c=islessequal(f,f);
e=islessgreater(f,f);
b=fmax(e,c);
}
b=pow(b,e);
d=fmax(c,f);
}
