#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=exp(d);
b=atan2(d,d);
c=fmin(c,c);
while(isless(c,a)){
d=log10(c);
c=fmin(d,d);
a=pow(d,a);
}
d=fmax(b,b);
a=atan2(c,c);
d=isgreater(b,b);
}
