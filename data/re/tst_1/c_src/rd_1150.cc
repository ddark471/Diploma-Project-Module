#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=islessequal(c,g);
d=fmax(h,c);
f=islessequal(a,g);
e=fmax(a,b);
g=floor(f);
while(islessequal(c,f)){
b=isgreaterequal(d,a);
c=ceil(c);
}
b=log10(a);
f=log(a);
}
