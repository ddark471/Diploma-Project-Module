#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=fmax(e,d);
c=islessgreater(d,a);
e=fmin(b,b);
while(isgreaterequal(e,b)){
e=atan2(a,c);
a=isless(c,e);
d=isgreater(c,d);
a=floor(a);
c=ceil(d);
}
b=isless(c,a);
d=cos(b);
}
