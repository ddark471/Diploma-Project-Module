#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=sqrt(c);
g=atan(f);
g=islessgreater(g,b);
while(islessgreater(e,f)){
a=fmin(c,a);
h=ceil(e);
e=islessequal(h,h);
b=exp(e);
}
e=islessequal(a,g);
e=floor(h);
}
