#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fmin(c,b);
d=floor(d);
while(isless(a,a)){
c=isless(c,d);
a=sin(a);
b=isless(a,c);
b=isless(c,a);
}
d=sin(b);
d=floor(b);
c=acos(d);
}
