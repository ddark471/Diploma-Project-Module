#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fmin(d,e);
d=islessequal(b,f);
while(isgreaterequal(d,a)){
b=fmin(b,a);
g=log10(b);
}
g=exp(b);
e=fmax(e,b);
g=sqrt(c);
a=acos(d);
a=islessgreater(a,g);
}
