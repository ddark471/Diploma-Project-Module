#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=log10(b);
d=isless(a,a);
a=log10(b);
d=isgreaterequal(d,d);
while(isgreaterequal(a,d)){
b=isgreater(a,b);
d=atan(a);
}
b=islessgreater(c,a);
c=islessgreater(a,a);
}
