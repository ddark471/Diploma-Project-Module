#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=tan(a);
f=log(b);
f=exp(f);
while(islessequal(f,f)){
g=sqrt(f);
c=isgreaterequal(c,d);
c=pow(b,b);
d=atan2(h,g);
}
c=pow(e,a);
c=pow(e,h);
f=acos(f);
d=pow(g,b);
}
