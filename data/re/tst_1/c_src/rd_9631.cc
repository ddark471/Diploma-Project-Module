#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=isless(d,e);
c=isless(a,b);
while(islessgreater(b,a)){
b=tan(c);
d=tan(a);
}
a=islessequal(c,c);
h=fmax(b,h);
f=ceil(d);
d=fmin(e,b);
}
