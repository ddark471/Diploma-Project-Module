#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=log10(d);
d=fmin(d,a);
c=pow(e,e);
e=pow(b,e);
while(islessgreater(f,b)){
e=pow(e,f);
b=pow(d,f);
}
c=atan(d);
c=exp(d);
d=ceil(a);
c=log(e);
}
