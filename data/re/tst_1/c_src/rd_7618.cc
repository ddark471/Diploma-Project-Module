#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=atan(b);
h=sqrt(h);
while(islessgreater(b,f)){
g=tan(h);
c=log(f);
}
b=fdim(b,d);
c=sqrt(b);
a=isgreaterequal(c,a);
d=islessequal(a,d);
if(isgreaterequal(d,c)){
c=tan(a);
c=atan(a);
}
a=fmax(d,b);
b=pow(c,a);
b=fmax(a,c);
}
