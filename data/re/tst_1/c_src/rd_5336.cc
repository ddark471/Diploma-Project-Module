#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=log(b);
e=tan(c);
d=fmin(c,d);
while(islessgreater(e,d)){
h=pow(g,g);
g=exp(h);
c=pow(c,g);
}
f=tan(h);
b=atan(d);
e=fmax(b,f);
}
