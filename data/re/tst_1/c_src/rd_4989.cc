#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=atan(b);
d=isless(a,a);
b=fdim(c,b);
b=isgreater(b,b);
while(isless(b,d)){
a=fmax(d,a);
d=cos(b);
c=log10(a);
d=fdim(b,a);
}
c=cos(c);
a=isgreaterequal(c,d);
c=sqrt(d);
}
