#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=islessequal(d,b);
c=exp(e);
d=log10(c);
while(islessequal(f,b)){
a=isgreater(f,e);
a=islessequal(d,g);
f=isgreater(d,g);
f=fdim(c,e);
f=log10(f);
}
g=sqrt(b);
d=isless(b,d);
e=pow(a,d);
f=log10(g);
}
