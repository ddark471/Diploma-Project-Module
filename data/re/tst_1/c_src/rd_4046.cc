#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=exp(g);
c=atan(e);
c=floor(d);
while(isless(a,c)){
f=exp(d);
a=atan2(e,a);
}
d=atan(f);
b=isgreaterequal(f,b);
a=floor(f);
c=isgreaterequal(d,e);
}
