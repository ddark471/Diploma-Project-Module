#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=isless(a,g);
g=isless(c,c);
f=ceil(b);
b=islessequal(g,c);
e=isgreaterequal(b,c);
while(isless(e,b)){
g=cos(e);
d=pow(d,e);
b=tan(g);
c=islessequal(e,a);
d=islessequal(g,f);
}
g=tan(g);
a=ceil(g);
g=atan2(f,b);
g=isgreater(e,f);
a=islessgreater(e,c);
}
