#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=exp(b);
d=fmin(c,b);
a=fmin(d,a);
b=atan(b);
while(isless(d,d)){
d=exp(b);
b=exp(b);
}
c=log(b);
c=isgreater(b,c);
}
