#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=log10(b);
a=islessequal(c,e);
f=log10(f);
g=tan(g);
g=log(h);
while(islessequal(a,c)){
a=isgreaterequal(g,g);
a=isless(h,f);
f=asin(g);
}
c=atan(c);
a=log10(d);
e=isless(h,h);
g=exp(a);
}
