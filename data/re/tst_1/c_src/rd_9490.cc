#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=sqrt(d);
d=ceil(b);
a=floor(b);
if(islessequal(c,a)){
b=islessgreater(c,b);
b=log10(b);
}
else{
b=isless(d,c);
c=islessequal(b,b);
}
d=isless(d,c);
a=sin(a);
while(islessgreater(a,b)){
c=floor(c);
a=tan(b);
}
a=isgreater(d,d);
d=fmax(d,d);
c=isless(c,d);
}
