#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=cos(b);
b=floor(c);
while(isgreaterequal(a,d)){
h=isless(e,d);
c=pow(b,d);
h=floor(h);
}
f=isless(d,d);
d=pow(e,g);
f=tan(c);
d=islessequal(d,d);
}
