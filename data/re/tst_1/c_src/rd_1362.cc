#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=islessequal(b,a);
c=exp(a);
if(islessequal(d,b)){
d=pow(b,b);
c=pow(a,c);
}
d=exp(c);
b=cos(b);
d=atan(d);
while(islessequal(a,a)){
a=exp(a);
c=isgreaterequal(b,b);
}
d=isgreater(g,d);
g=islessequal(g,a);
e=atan(b);
b=asin(d);
d=fmax(a,a);
}
