#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=log10(c);
b=isgreaterequal(a,a);
if(isless(c,g)){
h=atan2(e,f);
f=isgreaterequal(b,h);
}
else{
h=islessequal(a,b);
e=pow(d,g);
c=isgreaterequal(d,a);
}
h=islessgreater(c,h);
a=fmax(e,d);
f=cos(a);
b=atan(d);
while(isgreaterequal(e,f)){
a=cos(a);
e=ceil(c);
d=exp(c);
d=atan(e);
}
d=islessequal(b,b);
d=isless(a,c);
}
