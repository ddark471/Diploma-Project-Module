#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=pow(d,a);
d=isgreaterequal(c,a);
d=islessequal(a,c);
b=isgreater(b,c);
while(islessgreater(c,d)){
c=atan(d);
c=fmax(c,c);
}
a=isgreaterequal(b,c);
a=fdim(c,a);
}
