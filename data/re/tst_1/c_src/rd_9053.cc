#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=sin(a);
e=sqrt(e);
c=atan2(e,c);
a=sin(c);
a=pow(b,d);
while(isless(d,f)){
c=log(e);
f=atan(b);
a=fmax(c,f);
a=islessequal(c,a);
}
d=exp(e);
e=exp(d);
if(isless(f,f)){
d=asin(f);
c=isless(c,c);
}
d=log(f);
f=ceil(a);
b=exp(f);
}
