#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=log(b);
b=sqrt(c);
while(islessgreater(e,a)){
b=islessgreater(e,c);
b=isgreater(d,b);
}
f=fdim(f,b);
c=fmin(c,b);
e=fmax(f,f);
c=islessequal(a,b);
}
