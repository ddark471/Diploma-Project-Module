#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=pow(g,c);
b=isgreater(f,g);
h=atan2(d,a);
f=cos(c);
while(isless(g,h)){
d=fmin(a,h);
f=islessequal(g,d);
}
b=isgreater(c,c);
a=acos(a);
g=exp(h);
}
