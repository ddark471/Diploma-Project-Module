#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=isgreater(c,b);
d=isless(e,f);
g=fmin(b,f);
if(isless(a,a)){
f=islessequal(f,g);
d=islessgreater(a,g);
}
else{
g=isgreater(e,e);
a=isgreater(f,g);
f=islessgreater(e,c);
a=islessequal(c,d);
f=fmax(g,d);
}
f=cos(c);
g=fmin(e,b);
b=isgreaterequal(a,g);
while(isless(f,g)){
d=isgreaterequal(f,c);
d=fmax(b,c);
b=sin(b);
}
b=isgreater(c,f);
f=log(d);
d=islessgreater(d,e);
c=tan(a);
}
