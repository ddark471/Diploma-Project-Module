#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fdim(c,c);
b=ceil(d);
d=asin(a);
while(isless(b,b)){
d=fmax(b,c);
c=pow(d,a);
}
a=sqrt(c);
b=islessequal(c,d);
c=islessequal(a,d);
}
