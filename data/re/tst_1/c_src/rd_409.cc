#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fmax(c,d);
a=islessequal(c,c);
c=isgreaterequal(d,c);
while(islessgreater(a,a)){
d=tan(b);
b=ceil(c);
c=log(c);
a=ceil(d);
}
b=isless(c,c);
d=fmax(a,d);
b=fmax(c,d);
d=isgreaterequal(b,b);
}
