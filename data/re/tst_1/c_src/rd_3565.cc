#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=cos(c);
c=pow(b,d);
while(isgreaterequal(a,g)){
a=isgreater(f,d);
g=asin(d);
a=islessequal(h,b);
d=log(h);
a=log10(c);
}
c=atan(b);
e=fmin(d,c);
e=islessgreater(e,d);
d=fmax(b,b);
e=isgreater(e,b);
}
