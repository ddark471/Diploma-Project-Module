#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=floor(a);
e=pow(b,c);
while(islessgreater(b,b)){
c=islessgreater(d,c);
c=isgreaterequal(b,c);
b=sqrt(g);
}
f=islessequal(b,f);
a=fmax(c,a);
c=islessgreater(e,a);
}
