#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=fmin(h,b);
h=pow(d,c);
h=pow(c,d);
while(isless(h,h)){
f=fmax(f,d);
d=pow(f,d);
b=cos(e);
g=atan(h);
}
f=floor(b);
e=acos(e);
}
