#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=cos(c);
d=acos(h);
d=isgreater(c,a);
f=islessgreater(a,c);
while(islessgreater(e,d)){
e=fmax(h,h);
a=islessgreater(f,c);
d=pow(g,e);
a=fdim(g,h);
}
c=isgreater(c,c);
e=islessequal(d,d);
}
