#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=ceil(c);
d=sin(f);
while(islessequal(c,c)){
h=exp(e);
f=fmax(a,c);
}
e=ceil(d);
f=log(d);
b=acos(e);
if(islessequal(b,g)){
f=atan(a);
c=acos(c);
d=exp(f);
f=floor(d);
}
f=fdim(f,b);
b=pow(f,a);
c=fmax(b,a);
}
