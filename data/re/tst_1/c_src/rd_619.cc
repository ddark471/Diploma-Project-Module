#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=tan(d);
d=exp(d);
while(islessgreater(d,c)){
d=log10(b);
a=atan2(a,a);
d=fdim(d,a);
}
d=fmax(c,d);
c=exp(a);
}
