#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=fmax(c,b);
c=cos(a);
b=islessgreater(d,c);
while(islessequal(a,b)){
d=cos(b);
c=fmin(d,d);
c=fmax(a,c);
}
d=log(c);
b=isless(d,b);
d=sqrt(c);
b=exp(a);
}
