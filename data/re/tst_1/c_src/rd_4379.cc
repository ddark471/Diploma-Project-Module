#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=cos(d);
f=atan(g);
while(islessequal(d,g)){
f=fmax(a,d);
a=isgreater(d,a);
g=sqrt(e);
b=fmin(b,f);
}
f=fdim(a,f);
a=atan(f);
e=atan(f);
}
