#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=asin(d);
e=pow(g,c);
e=log10(c);
while(islessequal(e,e)){
b=pow(g,d);
f=fmin(g,g);
e=sin(f);
g=isless(e,h);
g=pow(f,f);
}
a=sin(d);
c=pow(h,e);
f=isless(b,f);
h=exp(d);
h=atan(f);
}
