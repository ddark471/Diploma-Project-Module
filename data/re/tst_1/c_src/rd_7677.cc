#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=atan(b);
c=tan(c);
while(isless(d,b)){
a=fmin(a,a);
c=fmax(b,c);
b=cos(d);
}
a=fmin(a,b);
d=atan2(b,a);
}
