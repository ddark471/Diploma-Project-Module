#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=acos(c);
a=ceil(d);
d=ceil(f);
g=exp(b);
e=atan2(b,f);
while(islessgreater(h,d)){
c=log10(h);
c=floor(d);
d=exp(c);
f=fmax(b,e);
}
h=isless(d,d);
b=log10(b);
}
