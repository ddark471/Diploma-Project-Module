#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=cos(c);
a=tan(a);
b=fmax(d,d);
while(isgreaterequal(d,b)){
a=pow(a,a);
a=atan2(a,a);
}
f=atan2(b,d);
d=asin(b);
c=log(a);
}
