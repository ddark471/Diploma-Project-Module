#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=sin(c);
d=floor(b);
f=isgreater(g,d);
b=isgreater(f,h);
while(isless(f,b)){
h=ceil(e);
d=ceil(c);
}
b=acos(a);
h=fmax(c,e);
}
