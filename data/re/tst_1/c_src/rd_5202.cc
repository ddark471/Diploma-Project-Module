#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=asin(g);
g=cos(d);
c=pow(f,f);
while(isgreaterequal(d,a)){
f=isgreaterequal(d,f);
f=fmax(f,c);
}
d=islessequal(d,a);
d=sin(f);
g=floor(a);
c=log10(g);
g=exp(b);
}
