#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=tan(b);
a=tan(c);
if(isgreaterequal(g,e)){
b=tan(c);
f=floor(g);
}
d=sin(g);
c=fmax(a,c);
while(islessgreater(f,e)){
a=sin(d);
g=pow(e,d);
d=isless(a,c);
f=exp(b);
a=fmax(d,c);
}
b=tan(c);
e=isgreater(e,g);
if(islessgreater(f,g)){
e=cos(b);
a=pow(g,d);
e=pow(c,c);
}
c=islessequal(g,d);
d=isgreater(g,b);
d=exp(a);
}
