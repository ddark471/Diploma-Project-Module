#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=pow(d,c);
d=floor(a);
while(islessequal(b,b)){
d=islessequal(a,b);
d=fmax(b,d);
a=fdim(a,b);
a=sqrt(d);
c=log(b);
}
a=tan(a);
c=exp(b);
}
