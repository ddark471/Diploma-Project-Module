#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=fmax(a,h);
a=sqrt(h);
while(islessgreater(b,b)){
d=fdim(h,e);
c=fmax(d,a);
a=isless(d,b);
a=tan(f);
}
h=fdim(h,c);
g=islessgreater(e,c);
e=sin(b);
h=fdim(d,g);
c=islessgreater(c,c);
}
