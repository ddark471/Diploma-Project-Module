#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=floor(c);
a=exp(a);
b=atan(b);
c=exp(c);
c=pow(c,d);
while(islessequal(c,a)){
a=fmax(d,a);
a=fmax(b,d);
c=asin(d);
}
a=fmin(a,d);
d=ceil(d);
b=atan2(b,c);
}
