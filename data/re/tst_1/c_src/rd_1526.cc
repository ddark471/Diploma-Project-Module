#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=atan2(c,a);
d=islessgreater(b,b);
d=atan2(c,d);
c=islessequal(d,a);
while(isless(b,a)){
a=exp(d);
b=sin(b);
d=fmax(c,c);
b=fdim(b,a);
}
b=isgreater(d,d);
c=pow(d,b);
c=floor(a);
}
