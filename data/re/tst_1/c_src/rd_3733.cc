#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fmax(d,b);
f=atan2(e,g);
d=ceil(g);
c=atan2(e,g);
e=fmin(e,f);
while(isless(g,e)){
e=log10(d);
b=acos(b);
d=sqrt(f);
e=fmin(a,c);
}
c=ceil(a);
c=tan(b);
}
