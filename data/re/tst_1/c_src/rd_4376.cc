#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fmin(c,c);
c=fmin(d,c);
c=ceil(b);
while(islessequal(c,a)){
a=isgreater(c,b);
c=log10(d);
d=islessequal(a,b);
b=fmax(d,c);
}
b=isless(a,d);
d=log(b);
b=ceil(c);
}
