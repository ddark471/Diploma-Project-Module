#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=fmin(a,d);
c=islessequal(b,a);
d=fdim(b,d);
while(isless(c,b)){
c=log10(a);
d=isgreaterequal(a,d);
c=islessgreater(b,a);
d=log10(b);
}
b=fmax(b,a);
d=fmax(c,d);
d=islessequal(d,b);
b=ceil(c);
}
