#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=islessequal(a,b);
h=ceil(g);
e=acos(f);
h=fmax(c,g);
while(isless(b,h)){
e=atan(c);
a=ceil(a);
}
f=exp(c);
b=isgreaterequal(c,f);
f=fmin(d,g);
c=atan(f);
if(islessequal(b,g)){
c=sqrt(c);
g=exp(a);
}
e=atan2(a,a);
a=islessequal(g,h);
a=pow(c,f);
d=isgreater(d,d);
f=ceil(e);
}
