#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=exp(a);
d=exp(b);
while(islessgreater(c,a)){
d=log10(a);
c=ceil(d);
a=acos(b);
d=asin(c);
}
d=isless(d,a);
d=pow(b,c);
b=isgreater(b,c);
d=log10(b);
}
