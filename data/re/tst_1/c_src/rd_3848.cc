#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=cos(c);
d=isgreater(b,c);
b=atan2(b,c);
while(isgreaterequal(b,c)){
b=fmax(d,c);
d=fmin(a,a);
}
b=isgreater(c,b);
b=atan2(d,d);
c=islessgreater(c,a);
}
