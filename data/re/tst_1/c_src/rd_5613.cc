#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=atan2(e,a);
b=ceil(a);
while(islessgreater(d,e)){
e=atan2(e,a);
c=asin(c);
}
a=sqrt(d);
c=cos(e);
d=fmax(b,a);
}
