#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=islessgreater(b,a);
a=isgreater(b,d);
if(islessgreater(c,b)){
c=sin(b);
c=islessgreater(b,c);
d=pow(c,d);
a=fdim(c,c);
a=sin(a);
}
else{
a=log10(b);
d=asin(b);
a=fmax(b,a);
}
c=log(a);
a=acos(d);
a=sqrt(b);
c=floor(c);
while(islessequal(d,b)){
c=sqrt(b);
d=fmax(c,c);
c=fmax(c,a);
d=atan(a);
d=fmax(c,c);
}
c=asin(c);
b=fmax(c,a);
if(isgreaterequal(a,c)){
b=pow(d,d);
c=fmax(a,c);
}
else{
c=islessequal(b,a);
b=sin(b);
a=isless(c,a);
a=isgreater(d,d);
c=fdim(d,d);
}
a=cos(c);
a=fmin(c,a);
}
