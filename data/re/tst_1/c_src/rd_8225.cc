#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=isgreater(d,d);
b=log(e);
c=cos(c);
while(islessequal(c,b)){
e=ceil(d);
c=fmin(a,e);
d=ceil(b);
d=isgreater(b,e);
}
b=log10(d);
b=cos(a);
a=isless(c,a);
c=islessequal(b,a);
if(islessgreater(d,b)){
c=fmax(d,d);
d=sin(d);
c=atan2(b,d);
}
else{
a=islessequal(b,d);
a=floor(b);
a=sin(c);
a=log10(d);
}
c=exp(d);
d=islessgreater(a,d);
d=islessgreater(b,c);
}
