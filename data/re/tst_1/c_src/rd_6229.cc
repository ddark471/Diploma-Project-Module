#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=floor(b);
f=isless(h,a);
f=fmax(b,h);
g=isgreater(c,b);
while(isless(c,e)){
h=floor(c);
g=pow(c,e);
}
g=fdim(g,d);
g=log10(g);
b=tan(b);
}
