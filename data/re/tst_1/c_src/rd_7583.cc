#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=sin(g);
b=exp(e);
g=floor(e);
f=isgreater(f,f);
while(isless(e,e)){
g=pow(h,g);
c=floor(e);
h=islessgreater(e,d);
}
d=atan(h);
a=log(a);
d=atan2(d,h);
b=sqrt(e);
}
