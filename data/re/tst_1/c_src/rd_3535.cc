#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fmax(c,a);
c=isless(e,d);
while(islessgreater(e,e)){
c=isgreater(a,e);
d=isgreater(d,d);
}
b=isless(a,b);
a=exp(e);
d=floor(a);
d=fmax(b,a);
a=atan2(c,c);
}
