#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=pow(b,a);
b=log10(c);
while(islessequal(d,c)){
a=log(a);
c=isless(a,a);
}
d=isgreaterequal(c,b);
a=islessequal(d,b);
c=log10(b);
c=fmin(d,b);
c=sqrt(b);
}
