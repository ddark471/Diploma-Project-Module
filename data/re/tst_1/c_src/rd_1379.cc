#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=fmin(g,d);
a=isgreaterequal(d,f);
g=islessequal(g,b);
a=isless(f,c);
c=fmax(d,b);
while(islessgreater(f,e)){
c=islessequal(b,a);
d=islessequal(g,c);
f=fmin(g,e);
b=pow(c,e);
c=floor(f);
}
c=fmax(a,a);
c=sqrt(d);
c=fmin(d,c);
e=isless(g,f);
c=ceil(f);
}
