#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=tan(g);
h=fmin(e,d);
e=fmax(e,d);
h=isless(f,d);
d=cos(b);
while(isless(h,f)){
b=islessgreater(h,h);
g=floor(c);
}
g=isless(f,b);
b=floor(f);
g=isgreater(e,g);
d=floor(b);
}
