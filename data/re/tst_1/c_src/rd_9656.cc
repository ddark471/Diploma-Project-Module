#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=isless(b,b);
b=atan(a);
while(islessequal(a,b)){
b=ceil(c);
c=exp(a);
d=ceil(d);
}
a=ceil(a);
b=ceil(c);
c=ceil(b);
}
