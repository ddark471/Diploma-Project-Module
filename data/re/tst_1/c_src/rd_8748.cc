#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=islessequal(d,b);
c=atan(d);
a=fdim(a,a);
while(isless(b,c)){
b=log10(a);
c=fmax(a,c);
d=fmax(c,d);
}
a=ceil(c);
b=log(b);
d=pow(d,d);
}
