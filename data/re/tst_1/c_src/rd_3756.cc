#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=isgreaterequal(h,d);
d=islessequal(e,a);
g=atan2(e,e);
f=isgreater(e,g);
if(islessequal(g,h)){
d=islessgreater(h,d);
c=islessequal(e,h);
}
else{
d=ceil(a);
h=pow(e,f);
d=isgreaterequal(g,g);
}
d=atan(c);
g=log10(h);
g=islessgreater(f,f);
e=cos(h);
while(isgreaterequal(g,a)){
f=isless(c,b);
c=fmax(b,c);
}
a=islessgreater(b,a);
h=log10(e);
c=exp(g);
c=exp(h);
if(islessgreater(h,h)){
a=floor(g);
g=islessgreater(g,h);
a=sqrt(h);
g=log10(f);
a=acos(d);
}
else{
a=atan2(e,b);
g=fmin(b,b);
d=sin(e);
a=atan2(f,g);
}
f=exp(b);
g=asin(c);
c=pow(g,g);
c=acos(c);
d=isgreater(b,f);
}
