#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=isgreaterequal(c,h);
d=islessequal(f,g);
a=islessequal(a,f);
d=fmax(c,f);
while(isgreaterequal(f,e)){
c=fmax(c,b);
c=log(a);
}
d=fmin(b,g);
e=exp(h);
g=log(c);
h=islessequal(a,e);
g=fdim(e,d);
}
