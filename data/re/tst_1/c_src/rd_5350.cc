#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=atan(e);
f=acos(b);
b=pow(c,c);
while(islessgreater(g,d)){
a=fmax(b,e);
c=fmax(f,f);
}
g=isgreater(b,g);
c=atan2(b,c);
d=islessgreater(g,d);
if(isgreaterequal(e,a)){
b=cos(e);
f=acos(d);
d=floor(a);
c=pow(f,f);
a=fdim(d,b);
}
else{
c=atan(b);
d=fmax(e,a);
a=fdim(a,b);
}
c=fmax(c,e);
a=isless(d,c);
a=cos(g);
}
