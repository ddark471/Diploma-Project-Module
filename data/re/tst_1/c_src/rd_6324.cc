#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=sqrt(b);
d=isless(e,h);
c=atan2(e,f);
if(islessequal(f,h)){
f=isgreater(g,a);
h=floor(a);
}
d=isless(h,d);
c=fmax(b,f);
while(isgreaterequal(e,c)){
h=ceil(c);
d=acos(d);
e=islessequal(b,g);
e=fmin(b,f);
}
e=isgreater(g,a);
e=fmin(h,h);
}
