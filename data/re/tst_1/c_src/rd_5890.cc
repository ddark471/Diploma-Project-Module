#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=floor(g);
a=log(a);
c=exp(h);
d=fmin(e,e);
while(isless(b,b)){
f=floor(c);
f=log10(a);
c=exp(f);
d=isgreaterequal(d,f);
g=log10(d);
}
d=ceil(f);
h=asin(e);
h=isgreaterequal(a,h);
b=acos(g);
f=fmax(e,g);
}
