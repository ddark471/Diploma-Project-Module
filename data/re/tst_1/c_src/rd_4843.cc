#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=asin(a);
c=log10(c);
a=fmax(d,d);
b=log(d);
d=isless(b,d);
while(isless(d,c)){
d=isgreater(c,a);
c=islessequal(b,b);
}
d=fmax(a,a);
a=sqrt(c);
}
