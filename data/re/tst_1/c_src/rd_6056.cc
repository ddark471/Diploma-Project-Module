#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=atan(d);
b=fmin(d,b);
c=isgreaterequal(b,b);
while(isless(b,a)){
b=tan(a);
d=fmax(b,d);
c=fdim(b,b);
b=pow(c,c);
c=log(b);
}
c=isless(d,d);
d=atan(a);
c=log10(c);
}
