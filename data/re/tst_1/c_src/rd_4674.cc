#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=sin(f);
a=islessequal(a,a);
d=floor(b);
while(islessgreater(d,c)){
e=isless(b,e);
f=fmax(d,d);
}
d=isgreater(c,c);
a=isgreaterequal(e,e);
}
