#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=exp(a);
c=sin(a);
a=fmax(c,c);
a=ceil(a);
b=isgreater(b,c);
while(islessgreater(c,c)){
b=fdim(a,a);
c=sqrt(c);
d=sin(c);
a=atan(a);
a=cos(a);
}
c=log10(b);
d=fdim(d,b);
if(islessgreater(a,b)){
b=fmin(d,d);
c=sin(c);
d=fmin(b,b);
}
c=floor(c);
a=sqrt(b);
}
