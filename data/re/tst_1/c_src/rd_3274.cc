#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=fmin(b,b);
d=fmax(e,c);
e=fmax(a,b);
c=isless(a,e);
while(isless(d,a)){
a=ceil(c);
a=atan(c);
a=islessgreater(a,b);
}
f=fmax(c,f);
h=log10(g);
f=ceil(d);
h=log10(c);
if(islessgreater(g,c)){
f=fmax(e,d);
b=acos(h);
a=ceil(c);
}
b=atan2(h,d);
a=pow(b,b);
}
