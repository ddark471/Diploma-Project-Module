#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=fmin(b,d);
a=cos(d);
while(islessequal(d,b)){
d=log(a);
d=log(d);
a=atan(a);
d=exp(c);
}
b=islessequal(a,d);
c=tan(d);
a=log10(b);
c=isgreater(b,b);
}
