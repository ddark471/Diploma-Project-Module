#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=ceil(a);
b=isgreater(e,a);
c=atan(b);
d=fdim(e,e);
while(islessequal(d,d)){
c=sin(b);
b=acos(c);
e=fmax(b,b);
}
c=sin(d);
d=fdim(b,b);
if(islessequal(a,b)){
a=acos(c);
c=floor(d);
}
else{
d=fmax(d,d);
c=islessequal(d,d);
a=sqrt(b);
}
a=sqrt(a);
d=fmax(a,a);
a=asin(c);
}
