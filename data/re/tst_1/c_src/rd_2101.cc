#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=atan2(d,c);
d=islessequal(c,c);
while(islessequal(a,c)){
b=fdim(a,a);
a=fmin(a,a);
e=isless(e,d);
}
d=atan2(a,a);
a=atan2(c,e);
d=cos(c);
d=fmax(d,c);
b=log10(b);
}
