#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=islessequal(c,g);
h=pow(h,b);
h=ceil(b);
while(islessgreater(g,b)){
d=exp(h);
c=ceil(g);
}
a=log(b);
a=log10(b);
if(islessgreater(c,c)){
e=fmax(c,a);
c=islessequal(d,a);
e=islessgreater(e,c);
b=sin(e);
a=islessequal(d,e);
}
else{
b=atan2(c,a);
b=isgreaterequal(b,c);
}
d=isgreaterequal(b,b);
c=islessgreater(a,c);
d=islessgreater(d,e);
}
