#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=sin(f);
c=exp(f);
while(isless(a,f)){
d=isless(d,d);
d=floor(a);
a=log10(c);
}
b=asin(f);
c=acos(b);
d=asin(a);
b=ceil(f);
d=fmax(b,c);
}
