#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=pow(d,c);
d=pow(b,b);
while(isgreaterequal(d,c)){
d=sqrt(c);
c=islessequal(a,a);
d=isless(b,c);
d=log10(b);
d=ceil(d);
}
b=isgreaterequal(b,c);
c=asin(b);
c=fmax(a,d);
}
