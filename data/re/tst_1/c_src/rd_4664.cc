#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=sin(f);
g=exp(e);
g=atan(g);
d=islessequal(e,c);
d=atan2(b,g);
while(islessequal(e,b)){
e=fmax(c,c);
e=exp(e);
g=fmin(f,b);
a=log(h);
d=atan(h);
}
f=islessequal(a,c);
c=isgreater(a,h);
b=atan2(h,d);
if(isless(a,g)){
d=asin(h);
f=ceil(c);
e=sqrt(c);
g=log(d);
}
else{
a=log(d);
h=fmin(c,e);
a=fmax(g,c);
e=atan(b);
}
f=exp(a);
a=atan(e);
f=log(c);
c=log10(g);
}
