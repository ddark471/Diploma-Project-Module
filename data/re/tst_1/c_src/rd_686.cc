#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=isgreater(a,d);
c=isgreaterequal(c,a);
c=asin(b);
c=isgreater(b,b);
while(isless(d,c)){
a=pow(a,c);
b=islessequal(a,c);
c=ceil(d);
d=isgreater(c,d);
}
a=asin(c);
d=ceil(d);
a=fdim(d,a);
}
