#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=exp(a);
d=fdim(a,c);
while(islessgreater(c,d)){
b=isgreaterequal(a,c);
d=acos(c);
}
d=isless(a,c);
c=pow(c,b);
a=pow(a,b);
}
