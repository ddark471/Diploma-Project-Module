#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=isless(c,a);
c=fdim(b,b);
a=pow(d,b);
while(isless(c,b)){
c=log(d);
d=sin(d);
d=isgreater(d,a);
}
a=asin(c);
a=isgreater(a,c);
b=sin(b);
}
