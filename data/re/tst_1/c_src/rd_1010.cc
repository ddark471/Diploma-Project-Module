#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=pow(b,f);
d=pow(a,d);
a=islessequal(d,e);
while(isless(b,f)){
h=fmin(g,e);
b=atan(h);
a=pow(a,c);
}
d=pow(b,c);
d=isgreaterequal(c,c);
c=ceil(a);
c=atan2(b,d);
b=exp(c);
}
