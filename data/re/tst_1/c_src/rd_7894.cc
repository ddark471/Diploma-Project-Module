#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=sin(a);
d=pow(c,d);
a=fdim(a,d);
if(islessequal(b,c)){
a=fmax(d,a);
c=atan2(a,a);
}
d=sin(d);
b=fmax(d,c);
while(islessgreater(b,c)){
c=islessgreater(d,a);
d=atan(d);
b=islessgreater(d,b);
a=log(c);
c=fmax(b,b);
}
e=isless(a,f);
g=sin(e);
d=fdim(d,a);
d=log(h);
a=fdim(g,c);
}
