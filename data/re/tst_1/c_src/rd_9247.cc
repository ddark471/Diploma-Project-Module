#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=islessequal(c,c);
c=sqrt(d);
if(isgreaterequal(c,d)){
d=log(c);
d=pow(d,b);
c=pow(d,c);
b=isless(b,b);
d=isless(b,d);
}
else{
d=isgreater(a,c);
d=asin(c);
}
b=log(b);
a=pow(d,c);
d=cos(a);
d=atan2(c,d);
d=isgreater(d,b);
while(islessequal(a,b)){
a=fmax(d,b);
c=log10(d);
}
c=log(b);
b=fmax(d,c);
a=fmax(d,c);
if(isgreaterequal(d,b)){
d=log10(b);
a=islessequal(d,c);
d=floor(a);
d=floor(b);
}
else{
a=sqrt(d);
d=log10(b);
c=atan(d);
}
d=islessequal(d,c);
d=atan2(c,d);
b=cos(c);
a=tan(b);
}
