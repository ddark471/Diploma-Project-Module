#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=pow(c,d);
c=log10(c);
while(islessequal(b,d)){
d=asin(c);
a=atan(d);
}
a=isgreaterequal(a,a);
c=fmax(b,b);
c=log10(a);
c=sqrt(c);
d=fmin(b,b);
}
