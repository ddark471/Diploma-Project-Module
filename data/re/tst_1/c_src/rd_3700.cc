#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=floor(c);
d=fdim(c,c);
while(isgreaterequal(a,a)){
d=fmax(a,a);
a=atan(b);
b=islessequal(a,b);
a=pow(d,c);
a=atan(d);
}
d=fdim(a,a);
b=sqrt(d);
}
