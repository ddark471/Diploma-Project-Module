#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=isgreater(d,c);
d=sin(d);
while(isgreaterequal(c,a)){
a=log(c);
b=isless(a,c);
}
b=sqrt(b);
c=fmax(c,d);
d=isgreater(c,c);
}
