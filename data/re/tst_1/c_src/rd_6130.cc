#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=atan2(h,b);
d=asin(b);
c=ceil(g);
g=isless(d,c);
d=isless(d,d);
if(islessgreater(f,f)){
d=cos(g);
c=log10(c);
c=asin(a);
g=isgreater(a,a);
}
f=fdim(d,b);
d=islessgreater(g,a);
f=pow(b,g);
while(islessgreater(g,e)){
d=tan(c);
a=ceil(h);
d=tan(f);
f=fdim(d,h);
e=isgreaterequal(g,d);
}
a=pow(g,f);
c=sin(d);
c=fmax(b,a);
g=cos(a);
f=exp(a);
if(islessgreater(a,c)){
f=isless(e,b);
g=pow(b,e);
f=isgreater(e,e);
g=fmin(g,a);
}
else{
e=islessequal(a,a);
f=sin(d);
d=fmin(b,f);
g=islessequal(g,c);
f=log10(d);
}
d=fmax(e,g);
e=fmax(c,a);
f=islessequal(d,g);
e=ceil(f);
}
