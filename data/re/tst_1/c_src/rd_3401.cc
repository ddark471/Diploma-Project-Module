#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fdim(g,b);
c=log10(g);
while(islessgreater(c,b)){
b=isgreaterequal(g,a);
f=floor(d);
}
b=pow(d,b);
a=log10(a);
}
