#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=atan2(d,f);
a=islessgreater(d,h);
e=fmax(b,e);
f=islessequal(h,h);
h=isless(c,a);
while(isless(b,b)){
f=asin(a);
a=atan2(a,a);
d=log(g);
}
e=isless(c,g);
f=islessgreater(a,c);
f=exp(e);
d=floor(d);
c=sqrt(c);
if(islessequal(f,c)){
d=acos(d);
e=fmax(e,e);
f=atan(c);
b=fmax(c,f);
g=asin(a);
}
else{
g=islessgreater(b,g);
b=atan2(c,g);
f=exp(e);
g=islessgreater(f,g);
a=isgreater(g,e);
}
g=asin(g);
d=islessequal(e,e);
g=fmin(c,g);
}
