#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=floor(e);
a=ceil(f);
d=sqrt(g);
a=log10(g);
d=fmin(f,a);
while(islessgreater(g,f)){
b=atan(d);
a=islessequal(a,h);
}
a=isgreater(f,h);
h=cos(d);
h=tan(c);
h=pow(c,g);
f=isgreater(e,d);
}
