#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=sin(a);
d=isless(b,a);
c=log(b);
while(islessgreater(a,a)){
b=asin(c);
d=exp(d);
d=asin(d);
}
b=islessgreater(a,c);
a=sqrt(d);
a=fdim(a,c);
}
