#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=exp(e);
c=asin(b);
while(isless(e,a)){
h=floor(d);
e=sin(a);
}
d=floor(f);
h=fmin(h,d);
}
