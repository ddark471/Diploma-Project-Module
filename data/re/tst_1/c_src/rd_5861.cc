#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=exp(a);
b=atan(a);
b=ceil(b);
while(isgreaterequal(a,b)){
c=log(b);
d=fmax(c,d);
a=log(d);
d=sqrt(d);
d=tan(a);
}
d=atan2(c,h);
d=atan2(e,h);
e=sqrt(f);
}
