#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=fdim(h,c);
f=fdim(d,b);
while(isless(c,g)){
b=pow(e,f);
h=fmin(g,a);
}
d=fdim(d,c);
g=fdim(c,h);
g=fmin(a,h);
g=atan2(f,f);
b=isgreaterequal(c,d);
}
