#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=exp(b);
b=ceil(b);
d=isgreater(a,d);
b=pow(b,a);
a=isless(c,a);
while(islessequal(b,a)){
c=fmin(a,c);
c=isgreaterequal(b,a);
d=islessequal(b,c);
}
b=isless(c,b);
c=exp(d);
b=atan(a);
c=fmax(d,d);
b=atan(d);
}
