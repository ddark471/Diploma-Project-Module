#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=asin(a);
f=isless(d,d);
c=cos(a);
f=isgreaterequal(h,h);
b=islessgreater(f,d);
if(isless(b,f)){
a=fmax(h,d);
c=isgreaterequal(e,e);
f=fmin(c,b);
}
else{
g=floor(d);
h=sin(f);
}
f=isless(b,e);
f=fmax(h,a);
d=islessgreater(e,g);
f=fmin(h,b);
while(islessequal(b,e)){
d=islessequal(c,f);
g=asin(a);
h=sqrt(e);
a=exp(f);
}
b=floor(a);
b=isgreater(c,f);
}
