#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=cos(d);
f=fmax(d,g);
while(islessequal(a,c)){
d=floor(b);
c=ceil(a);
a=ceil(a);
e=fmax(c,c);
c=isgreater(g,g);
}
d=pow(b,a);
a=cos(a);
d=sin(c);
a=islessequal(b,c);
if(islessgreater(d,d)){
c=exp(b);
a=islessgreater(b,a);
a=fmax(b,d);
c=fmin(d,c);
d=exp(c);
}
d=isgreater(b,b);
a=isless(a,a);
c=atan(c);
b=cos(b);
}
