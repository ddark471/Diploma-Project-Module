#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fmin(b,a);
b=tan(c);
while(isgreaterequal(d,a)){
c=atan(c);
d=islessgreater(e,b);
}
c=fmax(e,a);
e=log10(a);
}
