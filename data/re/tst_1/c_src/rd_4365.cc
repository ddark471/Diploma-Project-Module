#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=atan2(b,d);
h=fmax(c,g);
b=fdim(f,d);
if(islessequal(c,c)){
h=sqrt(f);
c=pow(b,f);
a=ceil(g);
a=tan(h);
e=fmin(d,h);
}
h=pow(c,f);
e=isless(d,g);
f=asin(e);
f=isgreater(e,g);
d=fmin(f,c);
while(isgreaterequal(b,e)){
a=fmin(c,d);
a=ceil(e);
h=fdim(c,c);
}
c=atan2(a,d);
c=log10(f);
f=cos(e);
}
