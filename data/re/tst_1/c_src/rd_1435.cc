#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=atan(c);
a=fmax(h,g);
if(islessequal(b,h)){
a=isgreater(e,e);
f=ceil(c);
g=tan(a);
}
else{
a=pow(f,b);
g=atan(h);
}
b=fmin(f,e);
e=fmax(g,g);
h=log(d);
h=isless(c,g);
d=fdim(d,h);
while(isgreaterequal(d,f)){
h=atan2(h,h);
g=atan2(g,g);
b=islessgreater(e,f);
h=islessequal(c,a);
}
d=isgreater(d,a);
c=pow(c,b);
}
