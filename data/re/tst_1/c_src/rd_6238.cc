#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=pow(f,a);
g=pow(d,d);
a=isgreater(a,g);
g=islessgreater(h,e);
a=pow(e,h);
while(islessequal(e,e)){
g=fdim(c,e);
d=isgreaterequal(d,c);
b=pow(d,g);
d=tan(c);
}
e=fmax(d,h);
c=isgreater(d,f);
c=isless(b,h);
g=acos(c);
}
