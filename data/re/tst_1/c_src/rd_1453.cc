#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=atan(g);
d=atan2(g,d);
c=isgreaterequal(g,h);
b=isless(g,g);
g=pow(c,e);
while(islessgreater(a,f)){
d=fmax(e,g);
c=cos(e);
d=isgreater(e,d);
}
g=log(b);
c=fmin(g,d);
if(islessgreater(e,g)){
b=isless(b,b);
d=isgreater(c,d);
e=islessequal(e,a);
b=islessequal(e,e);
c=cos(c);
}
e=fmin(f,g);
g=fmin(b,b);
}
