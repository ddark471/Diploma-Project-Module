#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=cos(b);
a=isless(b,b);
c=islessequal(b,d);
d=atan2(c,c);
while(isgreaterequal(a,c)){
b=log10(d);
c=asin(c);
c=atan(b);
a=floor(a);
d=sin(c);
}
b=fmax(c,a);
d=acos(c);
c=exp(d);
}
