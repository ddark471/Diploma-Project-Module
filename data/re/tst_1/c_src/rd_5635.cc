#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=exp(f);
c=cos(f);
g=acos(c);
g=cos(c);
f=pow(a,d);
while(isless(g,b)){
c=pow(g,d);
d=tan(c);
a=fmin(a,a);
}
e=fmax(f,d);
a=fmin(f,b);
}
