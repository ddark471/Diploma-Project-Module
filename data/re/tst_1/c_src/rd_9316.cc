#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=log10(g);
h=islessequal(e,a);
c=pow(d,e);
while(islessgreater(h,d)){
f=islessequal(b,e);
b=islessgreater(c,g);
}
f=pow(b,h);
e=tan(f);
}
