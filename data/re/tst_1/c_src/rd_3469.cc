#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=islessequal(a,d);
a=sin(d);
c=isgreater(a,a);
c=atan(b);
while(islessgreater(b,d)){
a=fmax(b,c);
c=fdim(b,a);
a=atan2(a,a);
a=isless(a,a);
c=atan(b);
}
c=sqrt(c);
c=fmax(a,c);
a=atan2(c,d);
c=fmin(c,c);
}
