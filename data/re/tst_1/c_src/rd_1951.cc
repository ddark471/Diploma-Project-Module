#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=islessequal(d,b);
d=atan(g);
while(isless(h,g)){
g=atan(b);
d=asin(c);
}
b=sqrt(a);
f=tan(d);
d=atan(d);
b=asin(d);
if(islessequal(e,a)){
g=cos(e);
d=log10(a);
f=fmax(b,a);
f=isgreater(c,a);
c=fmax(e,f);
}
a=sqrt(d);
a=log10(b);
g=islessgreater(g,a);
d=pow(b,f);
}
