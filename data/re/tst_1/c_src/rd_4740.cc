#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=isgreater(g,g);
g=atan2(a,g);
while(islessequal(c,a)){
a=log(a);
g=exp(e);
c=islessequal(c,e);
g=exp(g);
}
e=islessequal(c,c);
e=islessequal(a,f);
if(isless(a,b)){
b=isgreater(d,f);
d=exp(a);
e=fmin(e,c);
}
else{
c=log10(d);
a=atan2(a,f);
e=isgreaterequal(c,b);
f=log10(b);
}
d=ceil(e);
f=pow(f,d);
}
