#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=fmax(e,d);
d=fdim(g,g);
c=cos(a);
while(islessequal(f,f)){
c=pow(a,e);
a=log10(d);
g=islessgreater(e,e);
b=islessequal(a,a);
}
f=isless(f,a);
g=sin(f);
e=pow(c,a);
g=islessgreater(f,b);
d=exp(a);
}
