#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=log(c);
a=tan(b);
d=pow(c,d);
while(isless(b,a)){
a=acos(c);
d=islessgreater(d,d);
d=isgreaterequal(a,b);
}
c=ceil(d);
c=isgreater(d,d);
a=log10(c);
c=fmax(c,c);
c=asin(a);
}
