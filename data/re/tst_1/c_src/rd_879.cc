#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=islessequal(b,a);
c=exp(b);
while(isless(a,b)){
e=ceil(e);
d=pow(b,f);
}
d=exp(f);
d=fmax(b,d);
b=fmin(a,e);
if(isgreaterequal(f,f)){
d=tan(b);
g=fmax(g,d);
e=isless(e,g);
b=cos(f);
c=islessgreater(b,g);
}
else{
d=tan(c);
e=pow(e,a);
}
g=log10(d);
c=atan2(c,b);
f=islessgreater(g,f);
g=fmax(c,b);
}
