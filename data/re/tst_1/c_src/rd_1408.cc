#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=cos(b);
f=floor(f);
e=isless(e,c);
e=isgreater(c,a);
a=pow(c,f);
if(isless(f,d)){
b=ceil(d);
e=isless(c,c);
d=isgreater(a,a);
}
else{
c=islessgreater(f,e);
d=log10(d);
c=isgreaterequal(d,d);
d=cos(b);
}
a=isless(c,b);
e=islessgreater(f,e);
c=islessequal(b,c);
d=pow(f,c);
a=sqrt(f);
while(islessequal(e,f)){
b=log(f);
c=islessequal(e,d);
a=islessequal(a,a);
c=sin(c);
}
c=fmin(d,b);
c=atan(d);
a=pow(c,a);
}
