#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=exp(g);
f=fmax(e,e);
while(isless(f,c)){
g=fdim(h,g);
g=log10(f);
f=isgreater(h,a);
}
b=tan(a);
c=isgreaterequal(e,g);
if(isless(a,a)){
b=pow(d,g);
a=islessequal(b,c);
}
g=tan(c);
a=floor(e);
a=sqrt(g);
b=islessgreater(b,e);
}
