#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=tan(b);
c=cos(d);
if(isless(b,c)){
c=tan(b);
a=fmin(a,e);
d=pow(e,e);
c=sin(e);
}
c=islessequal(c,e);
a=sqrt(c);
c=islessgreater(c,b);
c=ceil(d);
d=fmin(d,e);
while(islessgreater(b,e)){
c=islessequal(c,d);
c=isgreaterequal(b,a);
}
g=fmin(e,b);
c=pow(f,h);
g=fdim(h,d);
f=atan2(d,g);
d=pow(b,b);
if(islessgreater(f,e)){
c=pow(f,b);
a=sin(h);
e=isless(f,g);
}
else{
d=floor(a);
f=asin(h);
b=exp(f);
}
g=floor(a);
d=isless(a,c);
c=fmin(f,e);
}
