#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=pow(g,a);
f=isless(a,c);
if(islessgreater(f,b)){
g=isgreaterequal(g,f);
g=asin(f);
a=isless(e,g);
g=fdim(a,e);
d=acos(a);
}
g=atan2(c,g);
f=log(h);
f=ceil(h);
b=isgreater(e,d);
while(islessequal(h,g)){
a=islessequal(b,d);
d=fdim(c,h);
g=isless(e,b);
}
d=tan(f);
d=islessgreater(e,f);
d=fmax(f,d);
e=islessgreater(e,e);
}
