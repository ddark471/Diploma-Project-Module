#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=atan(f);
d=isgreater(f,b);
d=pow(a,g);
while(islessgreater(e,e)){
e=islessgreater(a,d);
d=isless(e,e);
d=islessequal(d,c);
c=atan(c);
e=log(g);
}
a=pow(d,d);
b=pow(b,c);
if(islessgreater(f,f)){
d=pow(d,b);
e=fmax(e,b);
f=asin(d);
}
else{
e=pow(e,b);
f=atan(a);
b=floor(f);
c=isgreater(a,c);
}
d=cos(d);
e=pow(b,d);
}
