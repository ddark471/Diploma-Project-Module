#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=exp(d);
d=isless(c,a);
while(isgreaterequal(d,a)){
b=isless(b,b);
b=fmin(b,d);
}
b=fmax(a,a);
b=ceil(a);
}
