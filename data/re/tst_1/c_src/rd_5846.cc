#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=tan(a);
c=islessequal(b,c);
b=fmin(d,d);
while(islessgreater(a,c)){
d=acos(c);
c=fmax(a,b);
}
d=islessequal(c,c);
d=sqrt(c);
d=floor(d);
d=tan(d);
b=fmax(a,b);
}
