#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=sqrt(b);
a=islessequal(b,d);
d=isless(a,a);
d=isgreater(a,d);
while(isless(b,a)){
b=atan2(a,b);
b=fdim(a,a);
d=isgreater(b,a);
}
c=cos(a);
d=fmax(c,b);
}
