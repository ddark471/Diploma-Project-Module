#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=floor(d);
a=exp(c);
while(isgreaterequal(c,b)){
a=floor(c);
d=sin(d);
}
b=acos(d);
a=acos(c);
a=fmax(a,a);
c=fmax(a,b);
a=fmin(d,a);
}
