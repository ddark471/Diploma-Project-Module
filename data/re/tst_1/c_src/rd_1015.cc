#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=pow(a,b);
g=sin(e);
while(isgreaterequal(d,f)){
a=islessequal(b,a);
a=exp(f);
h=atan(e);
d=fmax(b,f);
d=sin(h);
}
b=pow(a,b);
c=sqrt(a);
}
