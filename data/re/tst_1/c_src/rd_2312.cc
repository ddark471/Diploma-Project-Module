#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=exp(c);
a=islessgreater(f,g);
while(isgreaterequal(c,c)){
e=isless(f,g);
d=pow(g,a);
}
b=exp(f);
c=isless(g,f);
d=acos(a);
f=sin(e);
d=floor(e);
}
