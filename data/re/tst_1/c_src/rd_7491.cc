#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=pow(e,a);
d=pow(b,b);
d=log(d);
d=isless(a,a);
while(isgreaterequal(c,b)){
f=fmin(e,e);
a=log10(d);
}
c=log(f);
b=asin(d);
c=fmin(f,a);
c=isless(d,f);
d=fmax(f,e);
}
