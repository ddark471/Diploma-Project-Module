#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fmin(b,d);
a=fmin(c,c);
while(islessequal(c,c)){
b=fdim(d,c);
b=exp(a);
a=fmax(c,d);
d=ceil(a);
}
d=floor(c);
d=isless(b,a);
a=cos(c);
c=exp(c);
c=ceil(d);
}
