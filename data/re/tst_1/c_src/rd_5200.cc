#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=fdim(g,g);
c=log(e);
while(islessequal(e,a)){
e=log10(b);
e=isgreater(b,b);
d=fmax(f,d);
}
c=sqrt(g);
a=ceil(b);
}
