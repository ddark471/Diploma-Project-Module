#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=ceil(c);
d=pow(d,c);
while(isless(b,b)){
b=pow(c,d);
b=islessequal(d,c);
b=log10(b);
}
c=isgreater(d,a);
a=sin(a);
c=atan(b);
}
