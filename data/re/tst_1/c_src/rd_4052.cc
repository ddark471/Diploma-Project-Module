#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=islessequal(a,c);
b=fmax(a,d);
a=fdim(b,d);
while(islessequal(d,b)){
c=fmin(c,c);
c=cos(c);
}
c=exp(c);
a=islessequal(c,c);
a=islessequal(b,a);
b=fmax(b,b);
}
