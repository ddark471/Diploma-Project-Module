#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=isgreater(g,a);
b=tan(d);
a=sin(f);
while(islessequal(c,e)){
g=islessgreater(g,d);
c=islessequal(g,a);
g=pow(f,d);
e=atan(f);
}
f=atan(a);
a=pow(b,d);
b=cos(g);
e=fmax(d,e);
}
