#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fmin(c,a);
e=sin(g);
e=atan(g);
c=fdim(c,g);
while(islessequal(b,e)){
d=atan(f);
e=ceil(e);
}
f=fmax(a,f);
e=atan(e);
c=pow(b,e);
}
