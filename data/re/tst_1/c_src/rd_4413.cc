#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=fdim(b,b);
c=isgreater(d,c);
d=ceil(c);
d=acos(b);
while(isgreaterequal(a,d)){
c=sqrt(b);
c=log10(d);
b=ceil(d);
c=log10(c);
c=pow(d,a);
}
b=isgreater(a,c);
c=sin(a);
c=atan(b);
a=islessequal(c,c);
if(islessequal(a,d)){
a=sin(b);
d=sqrt(a);
d=log10(d);
d=log(d);
}
a=pow(b,a);
c=isless(b,d);
}
