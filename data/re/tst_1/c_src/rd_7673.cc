#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=log(b);
e=floor(d);
a=isgreater(b,a);
a=log10(b);
while(islessequal(d,f)){
b=fmin(d,a);
d=atan2(c,d);
b=isless(f,b);
}
d=fmax(c,a);
b=fmax(d,b);
a=exp(d);
c=isgreater(b,a);
e=sqrt(e);
}
