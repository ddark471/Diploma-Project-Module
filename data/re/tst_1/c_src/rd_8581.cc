#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=atan2(b,c);
d=exp(b);
while(isless(b,b)){
a=acos(c);
b=isless(b,d);
a=pow(a,c);
}
c=cos(d);
e=fmax(f,a);
b=floor(f);
e=atan2(c,a);
}
