#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fmin(e,c);
d=fdim(g,h);
f=ceil(d);
c=islessgreater(e,a);
f=sin(e);
if(islessgreater(d,g)){
f=exp(d);
a=islessequal(b,c);
a=tan(h);
b=log(h);
}
else{
d=pow(c,b);
f=sin(g);
e=exp(a);
}
h=sqrt(c);
f=fmax(e,e);
d=isgreater(b,f);
c=ceil(c);
while(isless(b,h)){
f=isless(a,d);
f=sqrt(g);
}
a=isgreater(a,c);
c=floor(a);
a=floor(d);
b=log(c);
d=fmax(a,c);
if(islessequal(d,b)){
c=islessequal(b,d);
a=pow(d,a);
c=fmax(a,d);
}
a=log(d);
a=isgreater(c,a);
a=isless(c,b);
c=pow(c,d);
}
