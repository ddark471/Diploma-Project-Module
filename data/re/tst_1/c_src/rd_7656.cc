#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fmin(f,h);
e=fmin(b,h);
g=asin(h);
b=islessgreater(d,e);
if(isgreaterequal(f,d)){
a=fmax(a,g);
f=exp(h);
h=ceil(d);
}
e=isgreater(d,b);
f=log(g);
c=sqrt(d);
while(islessgreater(b,g)){
e=log(b);
c=atan2(f,g);
a=atan2(f,e);
h=islessequal(d,b);
}
g=atan2(a,d);
a=atan2(d,d);
e=isgreaterequal(g,b);
if(isless(c,c)){
a=isgreater(e,e);
c=fmax(h,h);
b=tan(h);
}
else{
c=log(h);
d=fmin(d,d);
}
g=ceil(e);
h=fmax(c,f);
h=acos(g);
d=isgreater(c,g);
}
