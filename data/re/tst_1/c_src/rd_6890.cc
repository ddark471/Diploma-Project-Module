#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fmax(d,d);
a=isgreater(a,d);
a=fmax(b,b);
d=pow(b,d);
d=sin(c);
while(islessequal(b,c)){
c=exp(b);
c=pow(d,b);
}
a=log10(b);
c=pow(a,b);
b=log10(a);
d=asin(c);
b=fmax(c,a);
}
