#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=exp(c);
c=islessgreater(c,a);
while(isless(c,b)){
g=exp(a);
g=sin(e);
g=fdim(c,c);
e=isless(f,b);
a=isgreaterequal(f,c);
}
c=fmax(c,d);
b=fmax(c,a);
if(isless(a,e)){
b=pow(c,a);
d=log(e);
c=fmax(e,e);
e=fdim(d,d);
e=isless(c,e);
}
d=fdim(e,a);
c=exp(d);
a=islessequal(e,e);
d=pow(a,d);
c=atan(d);
}
