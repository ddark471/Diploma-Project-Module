#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=islessgreater(b,d);
h=fmin(c,g);
e=exp(g);
a=sqrt(h);
if(isless(h,f)){
h=fmax(c,e);
a=fmin(c,d);
h=acos(a);
}
h=asin(g);
c=log10(h);
h=atan2(h,f);
h=pow(h,f);
b=fmin(f,a);
while(isless(f,b)){
a=floor(c);
g=pow(b,c);
a=asin(h);
}
e=floor(d);
b=exp(e);
f=fmax(d,b);
c=exp(c);
if(islessgreater(f,c)){
d=isgreater(c,d);
e=islessequal(a,f);
d=isgreater(f,b);
c=exp(b);
c=fmax(f,c);
}
else{
d=acos(f);
a=islessgreater(c,b);
b=asin(f);
f=asin(a);
}
c=sqrt(e);
d=islessgreater(d,f);
c=ceil(b);
}
