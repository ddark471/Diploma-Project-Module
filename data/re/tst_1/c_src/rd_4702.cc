#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=cos(b);
a=sqrt(d);
while(isgreaterequal(b,d)){
b=pow(c,c);
d=islessequal(c,d);
}
b=exp(c);
a=acos(b);
d=atan2(d,a);
c=pow(c,e);
}
