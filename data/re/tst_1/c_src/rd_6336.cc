#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=exp(a);
c=fmin(g,g);
f=sin(a);
while(isless(e,g)){
c=fdim(e,e);
f=fmax(d,a);
}
e=sqrt(c);
c=fdim(f,a);
f=floor(d);
a=atan2(e,b);
}
