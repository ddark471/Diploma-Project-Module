#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=atan(c);
c=isless(d,c);
a=isgreater(b,c);
d=atan2(a,d);
while(isless(d,d)){
c=fmax(d,c);
d=log(a);
}
d=sqrt(b);
d=cos(a);
d=sqrt(c);
d=tan(d);
c=fmax(a,b);
}
