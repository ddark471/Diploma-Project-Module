#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=islessequal(c,f);
e=atan(a);
if(isless(f,c)){
f=isless(d,d);
a=tan(f);
}
c=fmax(a,d);
c=atan2(f,a);
a=cos(b);
c=cos(f);
c=exp(a);
while(isless(c,a)){
b=acos(a);
f=atan(f);
}
a=log10(d);
a=fmax(b,b);
c=log(a);
c=fdim(d,a);
}
