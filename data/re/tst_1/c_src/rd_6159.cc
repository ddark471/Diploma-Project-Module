#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=isgreater(c,a);
d=ceil(c);
a=ceil(c);
if(isless(a,d)){
c=asin(a);
a=islessequal(a,a);
}
else{
d=log(b);
c=isless(c,a);
c=islessgreater(c,a);
c=ceil(a);
c=isgreaterequal(a,c);
}
d=log(c);
c=floor(c);
c=fmax(a,b);
b=islessequal(b,a);
d=floor(b);
while(islessequal(a,a)){
d=ceil(c);
b=log10(d);
c=cos(c);
}
g=isgreater(g,c);
d=atan2(g,g);
g=isgreaterequal(b,c);
e=floor(g);
if(isgreaterequal(g,e)){
g=log(a);
g=asin(f);
}
g=isless(e,f);
d=isless(b,a);
}
