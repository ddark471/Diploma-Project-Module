#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=pow(b,a);
e=isless(e,b);
f=fdim(d,a);
c=sqrt(d);
g=isgreater(e,b);
while(islessequal(e,f)){
d=asin(d);
f=atan2(b,e);
}
d=sqrt(d);
b=islessgreater(e,e);
}
