#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=tan(b);
d=log(c);
while(isless(c,e)){
c=cos(d);
d=exp(e);
}
b=islessequal(c,d);
c=fmax(e,e);
}
