#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=islessequal(a,c);
d=isless(c,a);
if(isgreaterequal(c,c)){
b=sin(a);
a=isless(c,c);
c=pow(c,b);
c=islessequal(c,d);
}
else{
c=islessequal(d,c);
d=sqrt(d);
}
d=cos(d);
e=isless(c,e);
a=ceil(a);
while(isgreaterequal(d,e)){
a=fdim(c,c);
b=islessgreater(a,d);
}
b=islessequal(a,f);
f=isless(f,e);
d=islessequal(b,c);
b=asin(c);
}
