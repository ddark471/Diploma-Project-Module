#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=ceil(b);
c=fdim(a,c);
a=floor(c);
while(isless(b,a)){
c=fdim(d,a);
d=isless(c,b);
}
a=islessgreater(b,c);
b=isless(c,b);
}
