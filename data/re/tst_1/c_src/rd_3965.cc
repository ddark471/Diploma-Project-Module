#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=fmin(c,d);
d=cos(b);
f=acos(a);
while(islessgreater(h,c)){
e=ceil(g);
g=cos(g);
b=isgreater(h,g);
}
h=ceil(e);
g=isless(d,c);
e=isless(g,a);
c=pow(h,g);
b=asin(d);
}
