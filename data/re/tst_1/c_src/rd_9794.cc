#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fmax(c,e);
a=pow(g,d);
while(isless(d,c)){
d=floor(f);
f=fmax(e,b);
g=cos(c);
b=isgreaterequal(e,b);
}
a=sin(d);
e=fmin(f,c);
e=ceil(c);
}
