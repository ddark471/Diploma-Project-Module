#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=sin(f);
f=islessequal(g,c);
while(isless(b,h)){
d=tan(g);
b=islessequal(d,e);
d=sqrt(h);
}
f=fmax(g,b);
d=islessgreater(a,f);
a=islessgreater(g,e);
g=fmin(e,e);
}
