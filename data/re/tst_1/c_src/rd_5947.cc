#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=log10(b);
c=ceil(g);
d=exp(c);
d=log(c);
while(isless(f,c)){
c=asin(f);
c=tan(d);
}
e=isless(b,f);
c=cos(c);
d=islessequal(e,d);
e=acos(d);
}
