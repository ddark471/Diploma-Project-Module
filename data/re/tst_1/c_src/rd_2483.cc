#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=fmax(e,g);
f=floor(h);
c=atan2(e,e);
b=fdim(c,c);
while(isless(d,f)){
h=sin(a);
h=pow(e,b);
d=cos(c);
d=pow(b,f);
}
a=asin(e);
a=isgreater(d,a);
f=fdim(g,h);
}
