#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=fmax(a,d);
f=pow(a,c);
while(islessgreater(c,f)){
c=islessgreater(b,a);
d=sqrt(e);
b=floor(c);
}
b=ceil(b);
c=sqrt(c);
}
