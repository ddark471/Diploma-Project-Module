#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=fmax(g,a);
c=exp(g);
h=tan(a);
h=sqrt(h);
g=log(f);
while(islessgreater(e,b)){
c=floor(f);
g=atan(e);
c=fmax(e,g);
}
f=exp(a);
a=exp(g);
}
