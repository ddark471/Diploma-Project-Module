#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=atan2(f,g);
d=atan(g);
while(isless(a,f)){
f=isgreaterequal(c,b);
c=isgreater(b,f);
}
f=tan(a);
e=islessgreater(f,c);
g=ceil(f);
g=pow(a,b);
}
