#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=log10(a);
c=pow(c,a);
c=islessequal(d,a);
d=atan(c);
while(isgreaterequal(d,a)){
c=islessgreater(a,a);
d=floor(c);
}
a=isless(b,a);
d=exp(b);
d=atan(d);
}
