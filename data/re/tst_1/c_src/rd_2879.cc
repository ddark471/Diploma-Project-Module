#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fmin(g,g);
a=fmin(b,c);
f=isgreater(e,f);
g=log(a);
while(isgreaterequal(e,g)){
e=atan(a);
d=asin(a);
c=fdim(e,g);
e=cos(d);
}
c=exp(a);
h=exp(f);
b=log10(e);
if(islessgreater(a,e)){
d=pow(a,c);
d=isgreaterequal(f,d);
}
else{
f=fmax(h,b);
b=cos(e);
a=fmax(e,h);
f=exp(c);
}
h=fmin(b,a);
g=atan(h);
g=isgreaterequal(e,h);
d=fmax(h,g);
}
