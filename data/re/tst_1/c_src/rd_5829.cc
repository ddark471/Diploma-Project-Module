#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=isgreater(c,b);
c=isgreater(c,d);
c=isgreaterequal(d,d);
b=exp(c);
c=log(d);
while(islessequal(a,a)){
c=acos(b);
c=ceil(b);
}
a=log10(a);
a=pow(a,a);
}
