#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=tan(c);
d=exp(f);
g=tan(b);
h=isgreater(a,g);
b=isgreater(h,a);
if(isless(c,c)){
f=pow(b,b);
b=isless(f,g);
h=atan(g);
}
else{
f=fmin(g,h);
b=tan(e);
d=isless(a,a);
a=atan2(f,d);
}
d=sin(h);
g=isgreater(b,g);
while(isless(h,g)){
h=exp(c);
c=exp(b);
}
c=islessequal(c,b);
a=isgreaterequal(c,a);
}
