#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=atan2(a,b);
d=ceil(a);
a=isless(b,d);
b=atan(d);
if(isless(c,d)){
b=asin(a);
a=asin(d);
b=atan(d);
}
a=islessgreater(c,c);
d=atan2(a,c);
while(islessgreater(c,b)){
a=islessequal(d,a);
d=atan(a);
c=ceil(a);
}
e=sin(g);
e=exp(h);
h=sqrt(b);
b=islessequal(c,d);
if(islessequal(h,f)){
b=pow(e,b);
d=atan2(b,h);
g=cos(c);
d=fmax(d,d);
}
f=cos(d);
h=fdim(e,g);
a=islessequal(f,d);
a=fmax(c,e);
}
