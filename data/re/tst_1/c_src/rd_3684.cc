#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fdim(c,a);
e=isgreater(a,g);
a=islessequal(f,h);
while(isgreaterequal(f,a)){
c=islessequal(f,b);
d=isgreaterequal(h,e);
}
h=fmax(d,d);
c=isgreater(g,b);
a=islessequal(f,a);
f=islessequal(d,c);
}
