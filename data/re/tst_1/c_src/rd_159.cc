#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=cos(d);
d=log10(b);
f=tan(f);
while(isless(g,e)){
b=atan(c);
a=exp(d);
}
d=atan(b);
e=isless(c,d);
c=exp(b);
g=fdim(g,d);
}
