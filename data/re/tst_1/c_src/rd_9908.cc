#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=sqrt(f);
f=islessgreater(h,h);
while(islessgreater(g,e)){
h=pow(b,c);
g=sin(g);
f=exp(h);
e=isless(e,a);
h=fmax(h,c);
}
f=islessequal(f,e);
h=islessequal(g,d);
c=acos(d);
e=log10(b);
}
