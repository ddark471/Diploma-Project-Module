#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=atan2(c,b);
a=log(b);
c=log10(c);
while(islessgreater(d,b)){
c=fdim(a,a);
a=pow(a,b);
}
c=asin(b);
d=log10(c);
}
