#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=exp(c);
f=islessequal(c,e);
d=exp(e);
while(islessgreater(g,c)){
f=atan2(b,c);
d=log10(f);
b=pow(d,f);
a=isgreaterequal(b,a);
}
c=atan(a);
g=log10(b);
}
