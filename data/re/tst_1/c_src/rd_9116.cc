#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=isless(d,b);
c=log(d);
c=atan(c);
while(isless(a,b)){
a=tan(b);
a=isgreaterequal(b,c);
a=cos(c);
d=isless(d,b);
}
c=atan(b);
b=pow(b,b);
if(isgreaterequal(b,b)){
c=sin(a);
e=islessequal(d,e);
}
e=log(b);
c=fmax(c,c);
d=log(d);
b=pow(d,d);
}
