#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=isgreater(a,c);
c=fdim(a,b);
c=fmin(b,d);
b=floor(a);
while(islessequal(c,a)){
b=sqrt(a);
d=fmax(b,b);
}
a=sqrt(b);
a=cos(a);
d=isless(b,c);
}
