#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=atan(c);
a=fmin(c,c);
a=exp(d);
b=asin(f);
a=acos(b);
if(islessgreater(a,c)){
f=pow(d,a);
a=islessequal(a,c);
}
a=pow(a,b);
a=pow(f,b);
d=isgreater(f,f);
d=cos(b);
while(isgreaterequal(c,b)){
b=fmax(c,b);
d=log(d);
a=log10(b);
}
g=floor(b);
b=pow(f,g);
a=islessgreater(a,h);
a=log(d);
if(isgreaterequal(b,c)){
a=ceil(f);
g=log10(b);
}
else{
d=fmax(c,b);
c=pow(h,c);
h=isless(d,d);
}
h=asin(h);
a=fmax(h,c);
b=log10(e);
b=pow(f,a);
}
