#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=atan(g);
d=isgreaterequal(d,g);
g=pow(e,f);
g=atan(b);
d=isgreater(h,g);
while(islessequal(g,d)){
d=tan(a);
h=tan(h);
c=log10(h);
}
c=ceil(c);
b=fmin(a,c);
}
