#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=fmax(b,g);
f=isgreaterequal(h,e);
a=cos(a);
while(isless(a,a)){
e=asin(h);
f=ceil(h);
h=atan(d);
}
d=log10(d);
d=tan(a);
g=islessequal(d,f);
}
