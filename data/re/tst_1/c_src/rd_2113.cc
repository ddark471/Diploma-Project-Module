#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=exp(a);
c=log10(c);
while(islessequal(c,c)){
b=isgreaterequal(c,d);
b=exp(a);
c=isgreaterequal(b,c);
a=atan2(c,b);
d=tan(c);
}
d=isless(a,a);
c=isless(c,c);
c=islessgreater(b,d);
}
