#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=isgreater(c,c);
d=exp(d);
if(islessgreater(a,a)){
a=sqrt(c);
c=log(b);
}
b=isgreater(c,a);
c=islessequal(a,a);
while(isless(b,d)){
b=pow(a,d);
a=islessgreater(c,b);
c=islessequal(d,a);
}
a=isgreater(e,d);
b=tan(c);
if(islessgreater(e,d)){
e=atan(e);
a=log10(b);
}
else{
a=islessgreater(c,b);
b=atan(a);
d=log10(d);
}
d=log(a);
e=pow(a,c);
}
