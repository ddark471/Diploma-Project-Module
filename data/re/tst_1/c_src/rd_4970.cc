#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=log10(b);
f=fmax(c,a);
b=fmax(f,b);
while(islessequal(c,b)){
c=sin(a);
b=asin(c);
}
e=floor(g);
b=pow(c,f);
g=pow(c,c);
d=islessgreater(c,c);
}
