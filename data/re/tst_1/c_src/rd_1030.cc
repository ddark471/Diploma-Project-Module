#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=atan(d);
a=isgreaterequal(b,d);
while(islessequal(e,e)){
d=isgreater(d,d);
b=islessequal(b,c);
}
c=atan(b);
a=islessequal(d,c);
e=log(e);
if(islessgreater(b,f)){
d=log10(d);
c=pow(b,a);
}
else{
d=isgreaterequal(f,g);
b=atan(c);
f=isgreater(e,b);
}
a=tan(g);
g=fmax(e,d);
a=ceil(f);
}
