#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=isgreater(g,f);
e=atan(b);
d=pow(f,a);
e=ceil(b);
while(islessgreater(f,c)){
f=isgreaterequal(c,d);
c=atan(e);
b=isgreaterequal(d,e);
}
d=fmax(a,d);
a=atan(b);
a=fmax(b,f);
a=atan(d);
d=acos(b);
}
