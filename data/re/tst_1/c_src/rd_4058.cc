#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=fmax(b,d);
c=ceil(e);
g=atan2(a,g);
d=islessgreater(d,d);
while(islessgreater(b,f)){
g=fdim(a,g);
f=fmax(c,c);
e=asin(a);
}
b=islessgreater(d,d);
a=fmax(c,g);
a=exp(c);
d=fmin(d,b);
f=islessequal(f,e);
if(isless(c,g)){
c=atan2(a,a);
b=log10(c);
a=isgreater(c,b);
g=isgreaterequal(c,a);
a=asin(c);
}
b=fmin(e,c);
a=islessequal(c,b);
}
