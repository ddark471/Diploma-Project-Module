#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=cos(a);
a=floor(c);
if(isless(h,h)){
e=islessgreater(c,d);
h=fmax(f,g);
f=log10(d);
d=fmin(e,h);
a=isgreaterequal(b,h);
}
else{
e=log10(h);
g=isgreater(h,a);
h=acos(b);
a=islessequal(b,b);
}
f=exp(g);
g=ceil(g);
while(isless(g,d)){
b=fmax(e,d);
a=sin(f);
a=pow(d,b);
f=fmax(c,c);
}
c=isless(g,c);
d=exp(b);
c=sin(a);
d=fmin(d,b);
}
