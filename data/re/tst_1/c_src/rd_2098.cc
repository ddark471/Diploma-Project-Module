#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=isgreater(b,e);
b=fmax(c,b);
while(islessequal(b,c)){
b=ceil(d);
e=floor(c);
c=sqrt(b);
d=sqrt(e);
}
b=exp(d);
c=log10(c);
c=isgreater(b,e);
}
