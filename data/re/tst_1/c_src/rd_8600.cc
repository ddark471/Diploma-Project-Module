#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fmin(a,c);
c=islessgreater(f,d);
d=isgreater(a,e);
f=cos(c);
while(islessgreater(f,f)){
f=pow(f,a);
c=log10(a);
}
e=atan2(a,e);
c=fmax(e,e);
e=isgreaterequal(e,d);
a=pow(e,b);
if(islessgreater(a,c)){
d=asin(c);
d=isless(b,b);
a=islessgreater(d,b);
d=atan2(e,d);
}
else{
d=islessgreater(e,d);
c=pow(e,d);
a=cos(d);
c=islessgreater(a,b);
c=fmin(d,e);
}
e=sin(c);
d=sin(c);
}
