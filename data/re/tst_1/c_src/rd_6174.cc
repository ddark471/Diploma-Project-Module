#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=islessequal(c,e);
d=islessgreater(b,e);
e=log(d);
d=fmax(e,a);
d=fmax(d,a);
while(islessgreater(a,e)){
d=log10(c);
d=log(d);
}
c=fdim(e,c);
d=floor(a);
b=log(b);
}
