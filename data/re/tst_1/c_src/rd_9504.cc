#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=exp(c);
c=sqrt(b);
while(islessgreater(a,b)){
d=log10(b);
d=ceil(d);
d=sqrt(d);
a=exp(a);
}
e=fmax(g,g);
h=islessequal(a,e);
e=fdim(c,b);
d=isgreater(a,f);
g=acos(f);
if(islessgreater(g,d)){
b=exp(b);
b=isless(h,e);
}
g=islessequal(h,b);
a=ceil(b);
b=fmin(a,d);
h=isless(e,f);
f=islessgreater(h,c);
}
