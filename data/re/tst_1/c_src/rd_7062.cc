#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=log10(h);
f=islessgreater(e,a);
d=isless(b,b);
f=tan(b);
e=fmax(a,a);
while(islessequal(h,g)){
d=islessequal(c,h);
e=sin(b);
g=isgreater(f,h);
}
c=log10(d);
e=fmax(b,f);
g=log10(f);
h=acos(a);
a=atan2(d,a);
}
