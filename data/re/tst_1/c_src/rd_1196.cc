#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=cos(c);
a=exp(a);
d=islessequal(d,d);
d=log(d);
while(islessequal(b,b)){
a=islessgreater(c,b);
d=sqrt(c);
d=log(b);
}
d=sin(c);
c=log(d);
d=ceil(c);
b=tan(a);
}
