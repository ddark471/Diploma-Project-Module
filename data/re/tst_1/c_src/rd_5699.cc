#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=isgreaterequal(c,a);
b=pow(c,b);
while(isless(g,a)){
g=log10(b);
a=isgreaterequal(f,f);
}
d=isgreaterequal(c,f);
e=sqrt(c);
c=islessequal(f,b);
c=ceil(d);
}
