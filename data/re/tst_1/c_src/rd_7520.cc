#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=exp(a);
b=exp(c);
b=pow(a,d);
d=islessgreater(b,b);
while(islessequal(e,e)){
e=isless(c,b);
c=fmax(a,b);
}
d=log(c);
a=fmax(b,c);
}
