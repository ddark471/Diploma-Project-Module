#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=tan(f);
d=atan2(e,b);
f=islessequal(f,g);
d=atan(g);
if(islessgreater(a,d)){
a=sqrt(a);
a=pow(c,f);
f=fmax(e,a);
a=isgreater(g,d);
}
else{
a=isless(b,g);
a=exp(e);
a=isless(a,g);
}
f=atan(c);
e=sqrt(e);
e=isgreater(b,b);
while(isgreaterequal(c,d)){
g=exp(c);
e=isgreaterequal(e,g);
}
e=islessgreater(e,a);
e=log(a);
d=acos(b);
}
