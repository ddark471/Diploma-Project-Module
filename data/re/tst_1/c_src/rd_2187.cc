#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fdim(g,e);
f=log(a);
e=tan(c);
h=isgreater(g,b);
while(islessgreater(f,d)){
a=floor(e);
d=atan(d);
d=atan2(d,h);
}
b=islessequal(g,b);
f=isless(f,h);
g=floor(h);
}
