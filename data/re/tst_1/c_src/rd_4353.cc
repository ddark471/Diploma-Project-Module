#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=fmin(e,d);
f=fmin(e,h);
if(islessequal(b,a)){
a=atan(b);
h=sqrt(b);
h=fmin(h,h);
h=fmax(a,g);
}
else{
b=isless(b,a);
d=tan(b);
e=isgreater(h,h);
e=isgreater(h,a);
}
g=tan(f);
g=atan2(f,h);
c=asin(c);
f=ceil(a);
while(islessgreater(c,h)){
e=floor(g);
f=acos(b);
a=isgreaterequal(a,f);
}
c=floor(a);
f=fmin(b,b);
}
