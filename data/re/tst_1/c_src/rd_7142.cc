#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=sin(c);
f=sqrt(e);
d=atan2(c,c);
while(islessequal(f,b)){
b=fmax(e,a);
a=fmax(f,f);
f=floor(e);
}
d=acos(c);
a=sin(a);
b=log(f);
d=log(b);
}
