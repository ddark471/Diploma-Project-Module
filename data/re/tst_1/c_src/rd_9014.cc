#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=exp(b);
c=log10(c);
d=isgreaterequal(e,f);
a=ceil(c);
f=sqrt(b);
while(islessgreater(b,e)){
g=pow(g,c);
a=pow(b,g);
e=fmin(d,f);
c=atan(b);
c=pow(g,g);
}
g=fdim(a,a);
c=floor(c);
c=fmin(d,g);
d=islessgreater(g,d);
}
