#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=pow(f,a);
d=log(c);
c=fmax(e,b);
while(islessequal(f,c)){
d=isless(c,f);
a=fmax(f,a);
e=exp(c);
f=log10(c);
}
a=fdim(b,f);
c=log10(b);
}
