#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=sin(g);
e=isgreater(d,d);
d=sin(c);
while(islessgreater(a,f)){
a=islessgreater(f,g);
g=ceil(f);
}
e=floor(c);
d=tan(c);
}
