#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=exp(f);
e=isgreater(c,h);
while(isgreaterequal(e,f)){
e=isless(a,g);
b=fmin(f,a);
}
h=islessgreater(h,h);
b=islessequal(f,a);
b=floor(b);
a=cos(b);
}
