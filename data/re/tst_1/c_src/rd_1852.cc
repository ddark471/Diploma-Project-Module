#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=isless(e,g);
a=floor(b);
g=fmax(e,d);
f=atan2(h,e);
while(isless(d,c)){
g=islessgreater(a,h);
h=sin(c);
b=pow(b,g);
f=isless(d,e);
g=fmax(f,e);
}
g=islessequal(b,f);
a=isless(c,c);
}
