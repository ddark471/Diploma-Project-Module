#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=tan(b);
d=pow(b,b);
while(isgreaterequal(b,c)){
c=fmax(b,b);
d=isgreater(c,d);
c=cos(d);
b=isgreaterequal(c,b);
}
b=isless(a,a);
c=log10(b);
c=ceil(d);
}
