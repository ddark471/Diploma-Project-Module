#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=acos(d);
d=isgreater(b,c);
while(isgreaterequal(a,d)){
c=atan2(b,d);
c=fmax(d,c);
}
a=isless(c,a);
c=sqrt(b);
d=asin(b);
c=fmax(a,b);
d=sqrt(d);
}
