#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=log10(b);
d=fmin(a,a);
d=ceil(d);
while(isgreaterequal(d,c)){
c=atan2(a,b);
b=fmax(a,b);
b=log(a);
}
b=sqrt(c);
c=fmin(b,a);
}
