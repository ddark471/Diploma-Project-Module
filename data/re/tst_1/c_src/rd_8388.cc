#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=floor(a);
c=exp(a);
while(islessgreater(a,d)){
c=isgreaterequal(b,d);
b=fmax(b,b);
}
d=log(c);
d=islessequal(a,b);
a=tan(d);
c=isgreater(a,b);
}
