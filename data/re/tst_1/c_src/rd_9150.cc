#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=sqrt(f);
d=pow(e,a);
f=isless(f,d);
d=cos(e);
d=isless(e,d);
while(isless(b,c)){
d=log10(f);
c=cos(d);
d=asin(d);
}
a=exp(f);
c=pow(f,b);
f=ceil(b);
}
