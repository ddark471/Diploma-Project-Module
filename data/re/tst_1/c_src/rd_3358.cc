#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=pow(f,a);
b=exp(b);
while(islessequal(c,c)){
b=log(d);
a=sin(c);
d=acos(a);
}
c=fdim(d,c);
d=fmax(d,a);
}
