#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fmax(c,a);
c=fdim(c,c);
while(islessequal(b,b)){
c=sin(b);
b=pow(e,b);
d=pow(c,e);
}
a=exp(c);
a=exp(a);
c=fmax(a,d);
e=fmin(e,d);
}
