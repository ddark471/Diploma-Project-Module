#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=floor(d);
b=atan(e);
while(isgreaterequal(a,f)){
a=tan(d);
g=isgreater(f,g);
a=atan2(e,e);
}
a=fmax(a,d);
d=fmax(c,f);
f=fmax(c,d);
b=exp(g);
}
