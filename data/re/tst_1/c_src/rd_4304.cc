#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=pow(b,d);
a=pow(c,d);
while(isless(a,d)){
c=fmax(a,d);
d=fdim(d,d);
b=fmin(b,a);
a=cos(a);
}
b=atan(c);
a=log10(d);
c=pow(d,b);
}
