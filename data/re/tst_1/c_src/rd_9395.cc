#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=isgreater(a,d);
c=islessequal(d,c);
while(isless(a,b)){
d=log(c);
d=atan(d);
}
c=pow(a,a);
a=asin(a);
b=tan(c);
d=cos(c);
}
