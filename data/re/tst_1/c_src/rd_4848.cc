#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=sqrt(d);
f=sqrt(b);
while(islessequal(a,e)){
c=tan(f);
a=atan(d);
c=fmax(a,a);
f=isless(e,a);
}
b=ceil(c);
d=fmax(e,a);
e=fmin(c,d);
}
