#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=pow(a,c);
a=isless(c,d);
while(islessequal(c,a)){
c=fmin(b,a);
b=islessgreater(c,c);
b=fmax(a,b);
c=floor(c);
b=isgreater(c,c);
}
d=isgreaterequal(a,a);
a=atan(d);
if(isless(h,c)){
h=pow(c,d);
g=isgreater(e,c);
}
f=isgreater(e,e);
f=fmin(c,d);
a=atan(g);
f=tan(a);
}
