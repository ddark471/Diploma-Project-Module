#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=cos(c);
c=isless(a,d);
a=atan2(c,b);
while(isless(d,c)){
c=atan2(a,c);
c=atan2(c,b);
a=isgreaterequal(b,a);
c=fmax(d,c);
}
b=islessequal(b,b);
c=isless(a,c);
}
