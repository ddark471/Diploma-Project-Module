#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=atan2(b,e);
f=log10(g);
h=isgreaterequal(a,h);
if(islessequal(f,c)){
h=atan(b);
f=sqrt(c);
d=fmax(d,g);
h=fmin(g,f);
}
d=log10(a);
a=cos(h);
d=floor(e);
f=isgreater(b,c);
while(isgreaterequal(g,d)){
h=ceil(a);
b=islessequal(c,g);
h=ceil(d);
f=fmin(a,g);
e=log10(e);
}
f=atan2(a,f);
d=isgreaterequal(e,a);
}
