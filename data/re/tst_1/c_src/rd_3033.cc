#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=isless(g,d);
d=sqrt(d);
c=log10(g);
while(islessgreater(d,c)){
d=sin(c);
a=pow(b,d);
b=islessgreater(a,f);
}
g=islessequal(a,c);
b=islessequal(a,g);
f=islessgreater(c,f);
g=fmax(d,b);
f=fmin(a,d);
}
