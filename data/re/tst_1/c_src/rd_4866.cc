#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=log(c);
e=pow(a,b);
c=log10(b);
f=islessgreater(e,g);
h=log10(f);
while(isless(c,e)){
a=fmax(e,g);
f=islessequal(e,e);
h=sin(g);
}
d=tan(b);
e=asin(d);
g=islessequal(a,c);
}
