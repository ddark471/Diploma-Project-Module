#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=isless(d,a);
b=log10(d);
if(islessgreater(c,b)){
c=log(d);
d=pow(d,d);
a=sin(b);
}
a=ceil(d);
c=log(c);
c=isgreaterequal(b,b);
c=islessgreater(c,d);
c=fmax(a,a);
while(islessequal(d,b)){
b=isgreater(d,c);
b=isgreater(a,c);
}
b=ceil(e);
f=isgreater(a,c);
a=atan(f);
g=islessequal(c,f);
if(isgreaterequal(a,g)){
h=fdim(h,b);
h=cos(b);
}
g=acos(b);
d=fmin(b,c);
h=fdim(g,h);
}
