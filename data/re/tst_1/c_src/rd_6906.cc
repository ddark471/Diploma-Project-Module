#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fmin(c,c);
b=tan(b);
d=isless(b,b);
a=islessequal(a,c);
while(isless(c,c)){
b=isless(b,b);
a=isgreater(a,d);
c=isless(d,a);
c=fmin(d,b);
c=fdim(b,b);
}
c=fmax(a,a);
c=atan(a);
}
