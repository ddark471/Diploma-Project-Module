#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=sqrt(c);
c=log10(c);
b=atan2(e,d);
a=acos(e);
e=log10(b);
while(islessgreater(b,b)){
a=ceil(e);
c=fdim(c,c);
a=pow(c,b);
d=ceil(e);
d=asin(e);
}
d=tan(b);
g=log(a);
a=log(f);
f=isless(c,c);
c=exp(g);
if(islessequal(f,g)){
d=ceil(e);
a=isless(h,g);
a=isless(d,e);
h=isless(b,b);
}
else{
h=exp(h);
f=cos(d);
e=atan(h);
d=isgreater(c,g);
}
d=log10(g);
h=isless(c,g);
}
