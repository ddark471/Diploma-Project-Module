#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=atan(a);
a=atan2(d,c);
c=ceil(c);
a=log(d);
while(islessgreater(a,b)){
c=floor(a);
c=islessgreater(c,a);
c=isgreaterequal(a,b);
a=atan2(b,a);
}
d=islessgreater(c,b);
d=floor(c);
}
