#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=atan2(a,d);
c=atan2(b,c);
e=isgreater(e,b);
while(islessequal(b,d)){
c=log(e);
d=atan2(d,b);
a=fmax(d,e);
}
c=islessequal(b,c);
d=exp(e);
a=fdim(c,c);
}
