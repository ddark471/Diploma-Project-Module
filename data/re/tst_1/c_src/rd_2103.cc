#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=islessgreater(a,b);
d=isless(c,c);
d=sin(a);
c=isgreaterequal(b,c);
while(isgreaterequal(b,b)){
c=fdim(b,a);
d=log(d);
}
c=cos(b);
a=isless(a,b);
}
