#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=ceil(d);
b=atan(a);
d=isless(b,c);
c=tan(e);
b=isless(d,a);
while(isless(c,f)){
c=islessequal(a,d);
f=isgreater(c,b);
e=isless(c,b);
}
b=atan2(a,f);
d=fmax(c,c);
}
