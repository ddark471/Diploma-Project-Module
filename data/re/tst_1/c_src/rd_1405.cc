#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=isless(d,h);
g=fmax(a,d);
d=atan2(a,g);
f=islessequal(c,e);
c=sqrt(b);
while(islessgreater(g,b)){
a=isless(c,d);
g=pow(d,f);
d=tan(e);
h=sin(h);
}
b=fmax(b,d);
g=fmax(e,g);
f=pow(h,g);
}
