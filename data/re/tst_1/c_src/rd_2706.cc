#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=pow(g,b);
h=ceil(c);
f=fmin(h,g);
c=islessequal(a,a);
b=islessequal(b,h);
while(islessequal(d,e)){
e=islessgreater(f,a);
b=sqrt(a);
c=log(d);
a=cos(c);
}
b=sqrt(h);
f=sqrt(a);
}
