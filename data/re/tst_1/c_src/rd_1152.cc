#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=isless(d,c);
c=floor(a);
d=fmax(d,a);
b=isgreaterequal(a,b);
while(isless(c,c)){
a=sqrt(b);
c=floor(a);
b=floor(c);
}
g=log10(b);
c=acos(c);
e=fmin(f,g);
if(isgreaterequal(a,d)){
e=fmax(c,b);
a=sqrt(c);
g=asin(h);
}
else{
g=tan(h);
d=atan(a);
c=cos(a);
c=sqrt(d);
}
h=sin(d);
h=sin(b);
}
