#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fmax(d,e);
e=pow(c,b);
while(islessequal(b,b)){
b=sqrt(e);
c=exp(e);
}
a=log(e);
a=isgreaterequal(a,d);
c=tan(b);
if(islessgreater(g,c)){
e=fmax(g,d);
g=atan(a);
h=sqrt(a);
c=isgreaterequal(d,d);
f=log10(e);
}
c=sqrt(f);
h=log(f);
}
