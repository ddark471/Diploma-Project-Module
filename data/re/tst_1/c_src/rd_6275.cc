#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=cos(a);
f=exp(a);
while(islessequal(a,d)){
e=log10(d);
g=atan(b);
}
c=atan2(g,b);
a=sqrt(b);
e=log(d);
a=floor(f);
}
