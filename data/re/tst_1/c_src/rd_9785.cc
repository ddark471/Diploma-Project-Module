#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=ceil(b);
f=isless(f,a);
while(isgreaterequal(a,b)){
d=exp(c);
c=sqrt(e);
e=sin(c);
f=atan(f);
d=asin(c);
}
f=sqrt(f);
b=isgreaterequal(a,c);
}
