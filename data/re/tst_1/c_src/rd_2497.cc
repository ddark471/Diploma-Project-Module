#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=islessequal(h,f);
d=fmax(g,h);
d=log(d);
while(isgreaterequal(b,c)){
h=isgreater(b,g);
e=log10(c);
a=isgreaterequal(e,b);
d=isgreater(g,f);
f=ceil(b);
}
a=pow(h,a);
g=sqrt(g);
}
