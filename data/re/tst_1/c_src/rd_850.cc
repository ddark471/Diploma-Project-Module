#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=ceil(d);
c=pow(a,e);
while(isless(d,c)){
a=fdim(b,a);
a=atan2(b,c);
}
b=atan2(a,c);
a=asin(b);
}
