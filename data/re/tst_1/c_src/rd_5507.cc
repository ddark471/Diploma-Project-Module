#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=pow(f,a);
a=pow(b,f);
d=pow(e,b);
while(isless(a,d)){
e=pow(d,e);
c=pow(e,c);
b=islessgreater(f,d);
c=isless(f,e);
}
b=isgreaterequal(b,b);
f=cos(a);
a=exp(b);
}
