#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=exp(a);
b=fmax(d,a);
d=isless(b,a);
while(islessgreater(c,b)){
d=islessequal(b,a);
a=log10(b);
}
d=exp(a);
d=fmax(a,d);
e=exp(a);
d=islessgreater(c,b);
b=isless(b,c);
}
