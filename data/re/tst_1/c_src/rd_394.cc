#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=isless(c,b);
c=log10(d);
c=pow(b,b);
if(isless(a,a)){
a=fdim(d,c);
c=atan(b);
b=sqrt(a);
b=exp(a);
d=acos(b);
}
a=exp(c);
a=islessequal(d,d);
a=log10(b);
while(islessequal(b,a)){
c=isgreater(c,c);
d=isless(b,d);
d=isless(c,c);
d=sin(a);
}
b=atan(c);
c=exp(a);
a=cos(d);
d=atan(d);
b=log10(d);
}
