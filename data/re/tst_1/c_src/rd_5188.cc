#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=ceil(a);
g=fmin(f,e);
f=cos(a);
c=fmax(e,g);
while(islessequal(c,g)){
a=islessgreater(g,g);
a=fmax(a,c);
g=log10(f);
f=atan2(c,b);
b=isgreaterequal(a,d);
}
b=exp(g);
e=tan(f);
c=log10(e);
d=atan2(g,f);
a=ceil(g);
}
