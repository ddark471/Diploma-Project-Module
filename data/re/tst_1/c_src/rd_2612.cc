#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=fmax(a,b);
c=log(b);
if(islessequal(a,b)){
a=tan(d);
c=pow(b,c);
}
b=fdim(d,b);
b=islessgreater(d,b);
while(islessequal(a,d)){
d=cos(c);
c=pow(c,d);
}
e=fmax(d,a);
b=asin(e);
}
