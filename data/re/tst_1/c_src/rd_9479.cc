#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fmax(a,d);
f=isless(e,b);
e=pow(d,b);
e=pow(e,b);
d=pow(c,f);
while(isless(c,e)){
f=asin(a);
e=log(d);
e=fmin(e,e);
d=ceil(d);
a=sin(b);
}
c=log(c);
f=pow(b,b);
f=exp(a);
}
