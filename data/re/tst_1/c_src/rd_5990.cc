#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=pow(d,a);
a=fmax(e,e);
d=isless(b,c);
e=sqrt(a);
while(isgreaterequal(e,e)){
c=sqrt(c);
c=pow(b,e);
}
b=isless(a,b);
f=log10(a);
a=atan(g);
a=atan(b);
if(islessgreater(a,f)){
f=sqrt(g);
e=sqrt(f);
d=fdim(g,a);
c=log10(d);
f=log10(e);
}
c=islessequal(d,c);
f=atan(c);
}
