#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=islessequal(c,c);
f=islessgreater(a,e);
while(isless(c,a)){
g=atan2(d,f);
e=sin(f);
g=islessgreater(b,f);
b=isgreater(g,e);
g=pow(b,g);
}
d=isgreater(b,f);
d=fmin(g,a);
e=islessgreater(e,c);
}
