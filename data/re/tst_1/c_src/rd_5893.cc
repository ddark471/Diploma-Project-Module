#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=log(d);
b=islessequal(c,b);
while(islessequal(a,c)){
b=isless(c,d);
c=fdim(d,d);
}
a=exp(d);
d=sqrt(d);
}
