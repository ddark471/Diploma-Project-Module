#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=pow(a,d);
c=log10(b);
a=islessgreater(a,a);
d=islessgreater(b,c);
b=ceil(d);
while(islessgreater(a,a)){
c=acos(c);
c=fmax(c,c);
c=atan(d);
d=cos(d);
}
b=asin(h);
c=atan2(c,g);
e=islessgreater(d,f);
if(islessequal(e,h)){
f=fdim(b,g);
a=fmax(f,c);
g=log10(b);
h=islessequal(h,g);
}
else{
h=log(f);
b=pow(b,b);
b=isless(b,h);
g=acos(h);
d=log10(h);
}
b=floor(h);
e=fmax(a,c);
}
