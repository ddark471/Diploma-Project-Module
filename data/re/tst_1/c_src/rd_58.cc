#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=pow(b,b);
c=pow(a,c);
while(islessgreater(d,d)){
b=sqrt(b);
a=isgreater(d,c);
d=exp(b);
}
d=islessequal(c,d);
a=fmin(d,d);
d=log10(a);
b=sin(a);
d=islessgreater(b,d);
}
