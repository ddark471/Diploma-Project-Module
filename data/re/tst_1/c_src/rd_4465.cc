#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=log10(e);
f=atan2(f,f);
a=isgreater(h,g);
b=fmax(d,c);
while(isgreaterequal(h,e)){
f=isgreaterequal(f,c);
c=atan2(b,a);
}
e=sqrt(c);
e=atan(b);
}
