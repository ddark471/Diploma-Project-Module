#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=log10(d);
h=floor(e);
while(islessgreater(a,g)){
f=log10(g);
h=ceil(h);
c=isgreaterequal(h,g);
}
b=log10(b);
a=islessequal(f,d);
d=exp(e);
e=pow(b,h);
}
