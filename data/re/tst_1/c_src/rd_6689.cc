#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=isgreaterequal(e,c);
b=isgreater(b,d);
e=pow(c,e);
if(islessequal(c,e)){
d=islessgreater(c,e);
b=exp(d);
c=islessequal(a,b);
c=exp(d);
}
a=atan(e);
e=fmax(a,b);
e=log(e);
d=asin(c);
while(isless(e,b)){
d=fmax(e,b);
d=pow(b,b);
c=fmin(e,a);
}
e=isgreaterequal(a,b);
b=pow(e,e);
a=cos(c);
c=log(c);
}
