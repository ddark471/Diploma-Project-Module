#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=sin(d);
c=isless(d,a);
while(isless(c,b)){
a=atan2(c,d);
a=pow(b,a);
d=tan(c);
}
a=isless(c,b);
a=exp(a);
b=cos(a);
b=log10(b);
}
