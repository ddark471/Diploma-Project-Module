#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=sin(c);
a=sin(b);
a=pow(a,b);
if(isless(c,c)){
d=log10(b);
b=atan2(a,b);
d=cos(d);
}
else{
c=log10(a);
b=pow(d,c);
}
d=pow(c,b);
d=log(a);
b=log(a);
d=pow(a,a);
while(islessequal(c,c)){
a=fmin(a,b);
c=log10(c);
d=log10(b);
}
b=islessequal(a,b);
a=isgreater(e,e);
a=atan2(a,a);
if(islessgreater(d,c)){
e=pow(c,e);
a=isgreater(d,d);
a=cos(c);
}
e=sqrt(c);
b=log(a);
c=tan(e);
c=fdim(a,a);
}
