#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=sqrt(f);
b=log10(c);
if(isless(a,d)){
b=pow(a,b);
d=isless(b,e);
c=fmin(f,e);
}
b=isless(c,d);
d=islessequal(b,b);
e=asin(e);
d=log(f);
f=sin(f);
while(isless(b,c)){
a=isless(d,a);
c=acos(d);
d=ceil(f);
f=islessgreater(d,c);
f=fmax(c,f);
}
c=atan2(a,d);
c=fmin(a,b);
b=fmin(b,c);
if(isless(c,d)){
c=pow(a,c);
a=sqrt(c);
c=fmin(a,a);
d=sqrt(b);
}
c=isgreaterequal(a,c);
b=log(d);
d=fmax(d,b);
b=fmax(c,c);
c=floor(a);
}
