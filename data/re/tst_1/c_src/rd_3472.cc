#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=atan(a);
c=atan2(e,d);
d=isgreaterequal(b,b);
while(islessequal(a,e)){
d=floor(b);
c=islessgreater(e,d);
d=isless(a,a);
}
d=fmax(c,d);
e=sin(e);
}
