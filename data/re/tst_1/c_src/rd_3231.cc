#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=isless(c,c);
b=cos(h);
f=log(c);
while(islessequal(e,a)){
a=cos(h);
g=pow(f,a);
g=log(b);
c=tan(h);
e=tan(f);
}
h=log(h);
d=isgreater(f,b);
}
