#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=atan(d);
c=cos(a);
b=pow(a,a);
c=isgreater(b,a);
while(islessgreater(b,c)){
a=log10(d);
c=ceil(a);
}
a=exp(a);
d=log(b);
d=atan2(b,b);
c=exp(b);
}
