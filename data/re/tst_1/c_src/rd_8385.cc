#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=floor(b);
c=atan2(d,c);
d=atan2(c,b);
c=exp(a);
while(isless(c,c)){
a=sqrt(c);
d=fmin(d,a);
a=log10(c);
d=fmax(b,d);
}
b=ceil(b);
a=fmax(d,a);
b=fdim(d,d);
a=acos(d);
c=floor(c);
}
