#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=pow(b,d);
d=floor(a);
a=fdim(a,b);
while(isless(a,a)){
d=pow(d,c);
a=pow(b,a);
}
a=exp(f);
h=isgreaterequal(c,c);
g=islessgreater(d,e);
b=isless(c,e);
if(islessgreater(h,g)){
c=asin(g);
a=exp(b);
}
b=fmin(g,b);
c=atan(g);
a=fdim(g,g);
}
