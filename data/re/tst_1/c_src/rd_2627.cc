#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fdim(a,a);
d=sqrt(e);
d=fmax(f,f);
h=log10(f);
while(isgreaterequal(g,c)){
e=fmax(c,c);
f=fmax(h,c);
g=sqrt(f);
g=isgreaterequal(h,c);
d=asin(a);
}
f=log(c);
e=acos(c);
c=isless(e,d);
e=isless(c,d);
d=islessequal(f,a);
}
