#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=floor(c);
g=atan2(g,c);
f=floor(g);
while(islessgreater(g,a)){
b=cos(d);
d=islessequal(a,b);
a=islessequal(f,f);
f=fmax(e,c);
}
g=islessgreater(d,a);
d=fmax(a,e);
}
