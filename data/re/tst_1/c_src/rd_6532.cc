#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=isless(c,d);
c=isgreater(b,a);
while(isless(a,b)){
d=fdim(b,e);
c=exp(d);
d=pow(b,b);
b=log(b);
a=fdim(c,e);
}
c=fmax(e,d);
e=exp(e);
c=asin(f);
b=atan2(b,f);
if(islessgreater(d,c)){
f=pow(e,f);
c=atan(e);
}
c=atan(d);
f=cos(f);
f=islessgreater(b,d);
d=fmin(a,b);
d=sqrt(a);
}
