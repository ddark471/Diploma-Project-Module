#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=cos(a);
b=tan(b);
d=isgreater(e,e);
b=fmax(c,b);
while(islessequal(e,d)){
d=islessequal(c,a);
c=fmax(d,c);
}
a=sqrt(e);
d=pow(a,a);
c=sin(a);
}
