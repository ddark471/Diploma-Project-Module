#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=fdim(d,b);
f=isgreater(g,g);
g=atan2(f,a);
if(isgreaterequal(h,b)){
c=pow(a,c);
b=fmin(h,d);
h=floor(b);
}
else{
f=pow(a,a);
g=sin(c);
}
f=fmin(g,f);
c=isgreater(e,d);
h=sqrt(e);
h=islessequal(b,e);
while(isless(f,c)){
e=ceil(h);
d=islessequal(e,a);
g=acos(g);
d=pow(e,c);
c=fmin(d,a);
}
b=floor(e);
a=log(b);
c=sin(e);
if(isgreaterequal(f,b)){
c=exp(b);
b=islessequal(f,e);
e=acos(f);
d=atan2(b,c);
}
else{
b=cos(f);
d=fmax(e,b);
}
a=pow(c,b);
a=exp(b);
e=cos(c);
}
