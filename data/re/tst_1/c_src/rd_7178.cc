#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=pow(a,a);
b=log(c);
b=isgreaterequal(a,c);
d=islessgreater(a,d);
while(isgreaterequal(b,a)){
c=isgreater(b,d);
a=isless(b,d);
}
d=isgreater(b,d);
a=ceil(d);
}
