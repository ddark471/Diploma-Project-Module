#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=log(a);
c=pow(a,d);
c=ceil(a);
d=sin(c);
d=pow(a,b);
while(islessgreater(d,a)){
a=exp(d);
a=atan2(b,c);
a=fdim(b,a);
}
b=fdim(d,c);
c=exp(c);
d=islessequal(a,c);
d=fdim(b,a);
c=fdim(c,a);
}
