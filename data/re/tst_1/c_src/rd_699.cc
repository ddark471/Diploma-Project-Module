#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=ceil(b);
g=exp(c);
b=sin(d);
g=fmax(c,d);
while(islessgreater(f,b)){
b=fmax(a,a);
g=fmax(f,a);
c=sin(c);
}
a=fmax(b,c);
b=tan(a);
}
