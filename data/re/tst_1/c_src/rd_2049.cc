#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=asin(h);
h=sqrt(f);
g=asin(a);
if(isgreaterequal(g,f)){
a=fmin(f,d);
e=sin(g);
}
else{
g=asin(f);
d=cos(c);
}
f=asin(g);
d=cos(h);
while(islessgreater(a,c)){
c=sqrt(c);
g=fmin(h,b);
c=isgreater(g,d);
h=cos(a);
b=isgreater(e,b);
}
g=islessequal(c,e);
g=fmax(g,h);
f=fmin(f,a);
a=log10(b);
}
