#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fdim(c,b);
c=exp(a);
a=exp(a);
d=fmin(b,d);
d=ceil(c);
while(isgreaterequal(c,b)){
b=log(d);
c=sqrt(d);
}
c=log10(a);
d=ceil(c);
a=acos(c);
}
