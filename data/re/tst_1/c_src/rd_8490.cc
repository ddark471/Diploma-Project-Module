#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=cos(d);
c=ceil(d);
d=acos(d);
b=log10(a);
c=isgreater(d,c);
while(islessequal(a,c)){
a=isgreater(d,b);
a=exp(c);
a=islessgreater(d,a);
d=pow(a,a);
d=log(d);
}
c=islessequal(b,b);
b=ceil(c);
d=isgreater(a,d);
d=atan2(c,c);
d=fmin(b,d);
if(islessgreater(a,c)){
c=isgreaterequal(a,a);
c=asin(c);
}
c=acos(b);
a=islessequal(d,d);
a=sin(c);
c=fmax(c,b);
}
