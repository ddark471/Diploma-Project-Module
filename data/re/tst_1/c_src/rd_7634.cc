#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=pow(a,d);
a=isgreater(a,a);
c=islessgreater(a,c);
b=floor(d);
if(islessequal(a,c)){
b=isless(a,a);
d=exp(c);
c=fdim(d,a);
}
d=floor(a);
d=exp(a);
while(isgreaterequal(c,a)){
b=log(a);
c=cos(a);
}
b=ceil(a);
d=isless(c,a);
e=isless(a,e);
}
