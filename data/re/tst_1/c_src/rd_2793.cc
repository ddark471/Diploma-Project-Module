#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=ceil(e);
c=fmax(e,b);
while(isless(a,c)){
b=log10(c);
e=pow(b,g);
d=fmax(c,a);
g=pow(d,d);
}
f=isless(d,f);
c=isgreater(b,b);
}
