#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=atan2(e,c);
f=ceil(c);
d=isgreater(a,f);
while(islessequal(a,e)){
a=atan(f);
a=acos(f);
}
e=isgreaterequal(c,e);
a=isless(f,a);
c=islessequal(b,b);
a=islessequal(c,d);
}
