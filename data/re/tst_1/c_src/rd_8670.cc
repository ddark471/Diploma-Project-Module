#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=ceil(a);
a=sin(c);
a=tan(c);
while(isgreaterequal(c,a)){
d=log(b);
a=cos(d);
}
b=atan(b);
d=pow(a,d);
a=exp(b);
}
