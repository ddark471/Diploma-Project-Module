#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=acos(a);
f=isgreater(f,g);
h=cos(f);
e=fmax(h,b);
h=fdim(g,e);
while(isless(g,h)){
b=cos(a);
e=islessequal(b,a);
f=isgreaterequal(e,g);
g=atan2(e,c);
c=islessequal(c,b);
}
b=atan2(f,f);
d=isless(a,h);
c=ceil(a);
}
