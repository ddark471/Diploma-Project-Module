#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=atan2(e,c);
d=isless(f,e);
e=log(d);
while(isgreaterequal(a,d)){
f=log10(a);
c=isgreaterequal(c,a);
f=fmax(d,b);
}
b=atan2(f,f);
b=atan(b);
}
