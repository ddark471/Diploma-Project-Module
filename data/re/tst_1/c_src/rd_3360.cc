#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fmax(c,a);
b=atan2(c,a);
c=isless(c,a);
while(islessequal(d,b)){
d=islessgreater(c,c);
c=fmax(b,d);
b=islessequal(d,c);
c=log(b);
}
c=fdim(d,b);
a=tan(c);
}
