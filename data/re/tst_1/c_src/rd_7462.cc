#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fmin(f,a);
d=asin(c);
while(isless(b,f)){
d=cos(c);
e=fmax(e,e);
c=islessequal(d,c);
}
f=isgreater(b,f);
b=islessequal(a,e);
b=atan2(c,c);
c=isgreaterequal(e,f);
d=fmax(c,a);
}
