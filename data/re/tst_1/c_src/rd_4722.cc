#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=islessgreater(c,c);
a=islessequal(a,c);
c=isgreaterequal(b,c);
b=log(d);
c=fmax(d,c);
while(isgreaterequal(b,c)){
b=isgreater(d,c);
d=log(c);
d=pow(c,a);
}
c=cos(d);
a=sqrt(d);
}
