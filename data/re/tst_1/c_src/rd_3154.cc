#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=atan2(c,b);
c=atan(c);
while(islessgreater(c,a)){
a=cos(b);
a=isless(a,b);
c=fmin(c,d);
c=isless(b,b);
b=isgreaterequal(b,b);
}
d=islessgreater(d,b);
b=atan2(a,b);
}
