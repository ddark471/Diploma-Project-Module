#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=floor(b);
c=log(a);
a=islessequal(a,b);
a=fdim(c,d);
c=ceil(c);
while(islessequal(c,a)){
d=log(d);
d=fmax(c,b);
a=asin(c);
b=ceil(d);
c=ceil(d);
}
b=isless(c,a);
c=log10(c);
a=log(a);
c=log10(a);
}
