#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=fmax(d,b);
b=floor(a);
d=ceil(d);
d=asin(b);
while(isless(b,a)){
b=sin(b);
c=log(d);
}
c=exp(d);
d=floor(a);
}
