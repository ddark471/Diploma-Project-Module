#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fmax(b,f);
c=isgreater(a,e);
while(isless(f,f)){
a=isless(d,b);
a=islessequal(d,c);
a=exp(b);
e=ceil(b);
}
f=atan(e);
f=fmax(f,b);
a=islessgreater(e,f);
a=fmax(d,a);
}
