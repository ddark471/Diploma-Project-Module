#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=log(f);
e=log(f);
f=sqrt(d);
while(islessgreater(d,e)){
g=isgreater(b,f);
b=acos(e);
c=islessequal(f,g);
}
f=fmax(b,b);
e=pow(a,e);
a=cos(c);
if(islessgreater(c,f)){
f=cos(b);
c=atan2(d,b);
d=floor(d);
d=log(f);
}
a=isgreater(a,d);
d=floor(c);
f=fmax(e,b);
c=tan(b);
}
