#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=sqrt(f);
h=asin(d);
g=log(g);
g=atan2(a,e);
h=islessequal(c,b);
if(isless(e,a)){
h=exp(c);
d=ceil(b);
e=atan2(g,c);
}
else{
g=pow(h,c);
g=islessequal(f,c);
}
f=isgreater(a,d);
a=pow(d,g);
b=tan(b);
d=sin(e);
a=ceil(a);
while(islessequal(f,h)){
d=atan(h);
e=fmin(a,c);
h=ceil(b);
a=sin(d);
e=tan(c);
}
b=log(e);
f=sin(h);
g=isless(a,h);
if(isless(c,f)){
c=islessgreater(h,g);
c=atan(e);
c=sin(c);
}
a=acos(h);
f=atan2(c,e);
}
