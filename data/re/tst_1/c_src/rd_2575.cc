#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=log10(a);
a=exp(b);
while(isgreaterequal(c,b)){
a=islessequal(a,b);
d=isless(b,d);
c=isgreaterequal(c,d);
a=tan(b);
c=cos(b);
}
c=fmin(b,a);
d=isgreater(c,a);
}
