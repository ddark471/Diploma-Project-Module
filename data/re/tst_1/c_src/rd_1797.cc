#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fmin(a,d);
d=asin(c);
a=fdim(a,c);
d=fdim(b,d);
d=fmax(c,d);
while(islessequal(a,a)){
a=cos(a);
c=islessgreater(b,a);
a=exp(d);
}
d=fmax(d,b);
a=cos(c);
d=cos(c);
a=isgreaterequal(b,b);
}
