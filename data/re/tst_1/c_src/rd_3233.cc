#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=pow(c,d);
c=fmax(a,d);
b=tan(a);
a=ceil(b);
while(islessequal(b,d)){
d=cos(a);
c=islessequal(c,c);
d=fdim(a,c);
}
c=islessgreater(a,b);
d=pow(c,b);
a=pow(c,c);
}
