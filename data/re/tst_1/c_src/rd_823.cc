#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=acos(b);
d=ceil(c);
d=acos(d);
while(isgreaterequal(c,b)){
a=islessequal(a,d);
a=sqrt(c);
}
c=fdim(c,a);
c=ceil(a);
}
