#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=cos(h);
f=tan(c);
e=isless(f,b);
h=sin(f);
while(isless(f,b)){
g=asin(e);
f=atan2(f,d);
e=floor(b);
c=isgreater(a,e);
}
a=sqrt(e);
d=atan2(d,b);
f=islessgreater(d,e);
if(isgreaterequal(b,f)){
f=isless(f,d);
f=floor(f);
a=log10(f);
a=pow(g,c);
}
else{
b=fmin(c,g);
d=atan2(g,f);
g=atan(g);
}
e=islessequal(d,g);
g=cos(c);
}
