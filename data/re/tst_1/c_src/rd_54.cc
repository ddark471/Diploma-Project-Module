#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fmin(c,d);
d=cos(c);
a=isgreaterequal(c,b);
d=exp(a);
c=fdim(b,c);
while(islessequal(a,d)){
c=floor(a);
d=sqrt(c);
c=isless(c,c);
d=fmax(a,a);
c=log(a);
}
a=fmin(a,d);
c=log(b);
d=log(d);
b=floor(d);
c=isgreaterequal(c,c);
}
