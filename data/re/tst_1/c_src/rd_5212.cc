#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=fmax(g,e);
d=islessequal(h,a);
h=acos(d);
d=asin(c);
c=fmax(d,d);
while(isgreaterequal(b,f)){
e=pow(h,b);
d=fmin(d,g);
f=log(c);
h=log10(c);
}
d=asin(c);
g=atan2(h,c);
e=isgreater(g,e);
f=isless(c,e);
}
