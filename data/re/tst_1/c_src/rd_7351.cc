#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=floor(e);
d=isgreater(c,a);
while(isless(b,f)){
g=sqrt(c);
d=ceil(f);
d=isless(e,c);
}
g=isless(e,e);
f=isgreater(d,f);
g=tan(a);
e=log10(c);
}
