#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=islessequal(a,d);
c=cos(b);
b=atan2(d,a);
c=islessgreater(d,a);
a=acos(a);
while(isless(d,b)){
a=islessequal(b,c);
c=fmin(c,a);
}
d=isgreater(d,b);
d=fmax(b,a);
}
