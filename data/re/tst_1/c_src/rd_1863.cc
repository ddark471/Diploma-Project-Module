#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=isless(a,d);
a=ceil(b);
d=fmin(c,c);
a=tan(d);
a=fmax(d,a);
while(isless(c,b)){
a=isgreater(c,a);
c=ceil(d);
a=floor(c);
c=isless(a,c);
}
a=fmax(c,d);
d=atan(a);
}
