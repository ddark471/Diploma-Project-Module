#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=log10(b);
d=pow(e,a);
c=log(e);
while(isless(b,b)){
a=isgreater(a,b);
a=islessequal(d,b);
a=log10(a);
}
c=atan2(a,b);
a=floor(a);
d=fmax(b,e);
c=sqrt(d);
c=tan(d);
}
