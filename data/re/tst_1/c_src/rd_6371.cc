#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=floor(f);
d=exp(h);
h=isgreater(e,h);
while(islessgreater(d,a)){
h=ceil(a);
a=ceil(h);
e=ceil(f);
b=islessequal(h,f);
}
d=islessequal(c,e);
a=log10(c);
a=sin(d);
}
