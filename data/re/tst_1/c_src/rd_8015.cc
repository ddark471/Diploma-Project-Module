#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=tan(b);
a=islessequal(b,a);
while(isless(c,e)){
d=islessequal(b,b);
f=sin(e);
a=atan2(b,a);
}
d=islessgreater(c,a);
b=fdim(c,a);
d=floor(d);
if(isless(c,b)){
d=islessgreater(b,d);
c=fdim(b,d);
}
else{
b=isgreaterequal(b,b);
a=isless(b,a);
d=islessgreater(c,e);
a=islessequal(e,a);
a=fmax(b,b);
}
e=exp(d);
d=fmax(e,d);
c=acos(d);
}
