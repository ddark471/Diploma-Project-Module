#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=isless(a,c);
c=isless(c,c);
h=fmin(e,e);
g=log10(f);
while(isless(f,d)){
f=isgreaterequal(h,f);
d=log(b);
e=tan(c);
d=islessgreater(e,a);
}
d=fmin(h,g);
h=fmin(g,h);
h=asin(a);
b=isgreater(g,a);
d=isgreaterequal(g,e);
}
