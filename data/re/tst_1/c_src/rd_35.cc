#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=islessgreater(d,b);
d=exp(b);
if(islessgreater(d,a)){
a=exp(a);
b=sqrt(c);
a=tan(c);
a=isless(b,c);
}
d=ceil(a);
d=fdim(a,d);
b=isgreater(b,b);
a=islessequal(a,b);
c=sqrt(a);
while(isgreaterequal(c,c)){
c=ceil(c);
b=fmax(d,d);
b=atan2(a,d);
c=islessequal(b,d);
}
d=atan2(a,b);
c=log(d);
}
