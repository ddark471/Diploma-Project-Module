#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=isgreaterequal(d,c);
a=tan(c);
b=log(c);
while(islessequal(e,a)){
c=fmax(a,b);
e=sqrt(c);
}
b=exp(c);
e=atan2(b,a);
d=islessequal(a,g);
}
