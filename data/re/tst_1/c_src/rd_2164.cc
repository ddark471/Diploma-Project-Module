#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=ceil(c);
d=isless(c,c);
c=log10(b);
d=log(a);
while(isless(a,c)){
d=fmax(c,a);
c=isgreater(b,c);
a=sqrt(b);
c=pow(c,b);
c=acos(d);
}
c=isgreaterequal(b,a);
c=sqrt(a);
b=isgreater(b,a);
a=acos(c);
}
