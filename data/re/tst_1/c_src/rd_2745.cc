#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=sqrt(c);
c=fmax(a,d);
c=fdim(c,b);
while(isgreaterequal(c,a)){
c=cos(b);
d=log10(b);
a=isgreaterequal(b,a);
d=sin(b);
d=fdim(d,b);
}
c=isless(b,b);
c=fdim(d,c);
d=sqrt(b);
d=cos(c);
if(islessequal(b,d)){
c=ceil(c);
c=acos(a);
b=isgreater(b,c);
}
else{
b=exp(b);
d=log(b);
d=isgreater(b,b);
a=sqrt(b);
}
c=atan2(a,c);
a=fdim(b,a);
}
