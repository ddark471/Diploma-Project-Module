#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=isgreater(a,a);
d=atan(b);
e=fmax(a,b);
while(isless(d,d)){
e=atan(c);
c=log(f);
}
b=asin(b);
a=isless(a,f);
b=fmax(e,e);
d=cos(d);
c=islessequal(e,e);
}
