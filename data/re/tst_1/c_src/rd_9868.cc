#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=pow(c,a);
b=fmax(b,c);
c=exp(c);
while(islessequal(c,e)){
a=fmax(e,c);
c=cos(b);
d=fmax(a,b);
}
e=isgreater(d,e);
e=fdim(c,a);
b=isgreaterequal(e,b);
e=pow(a,c);
}
