#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=isless(d,d);
b=atan(c);
while(isless(b,b)){
c=tan(b);
a=floor(b);
c=atan(b);
c=log(c);
d=fmax(d,c);
}
b=sin(c);
c=log10(c);
}
