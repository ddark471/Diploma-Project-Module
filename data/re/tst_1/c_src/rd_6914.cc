#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=pow(e,d);
c=islessequal(e,a);
b=sin(b);
c=fmax(d,b);
while(islessequal(e,b)){
c=sin(a);
a=log(d);
}
b=ceil(a);
c=fmax(e,b);
b=sqrt(e);
}
