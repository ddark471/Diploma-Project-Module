#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=isgreater(c,d);
a=exp(c);
a=islessequal(a,a);
d=cos(b);
d=exp(b);
while(isless(a,c)){
a=sqrt(a);
d=islessgreater(d,b);
c=sin(b);
b=atan(d);
}
b=fdim(c,c);
c=tan(a);
}
