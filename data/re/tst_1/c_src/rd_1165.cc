#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=fmin(b,f);
e=sqrt(a);
h=sqrt(d);
f=log(c);
a=atan2(b,h);
if(islessequal(e,b)){
c=fdim(f,f);
g=islessgreater(f,e);
}
f=fmax(b,c);
h=ceil(b);
while(isgreaterequal(g,d)){
a=asin(d);
d=sin(h);
g=fmin(d,e);
d=log(h);
h=exp(h);
}
a=asin(a);
c=isless(d,a);
b=log10(b);
}
