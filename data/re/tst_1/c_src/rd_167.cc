#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=atan(c);
a=islessequal(b,d);
b=asin(c);
d=islessgreater(c,d);
c=log10(c);
while(islessequal(c,b)){
d=isless(d,a);
b=fmax(a,b);
d=islessgreater(b,a);
}
d=sin(d);
d=islessequal(a,d);
}
