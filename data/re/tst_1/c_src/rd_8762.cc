#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=log10(d);
e=floor(g);
while(islessequal(e,f)){
f=fmax(e,f);
e=isgreaterequal(g,e);
c=islessequal(e,c);
g=fdim(b,d);
}
c=log10(d);
a=exp(f);
}
