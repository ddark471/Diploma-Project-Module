#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=log(c);
c=fmax(a,c);
d=sqrt(b);
c=exp(a);
while(islessgreater(c,b)){
b=fmin(b,d);
d=fmin(b,a);
}
b=cos(c);
c=islessequal(b,a);
}
