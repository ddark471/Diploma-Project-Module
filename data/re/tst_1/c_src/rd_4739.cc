#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=log10(c);
f=pow(e,a);
d=sin(h);
b=isgreater(h,h);
c=islessequal(c,g);
if(isgreaterequal(g,h)){
f=pow(g,a);
g=acos(g);
g=isless(e,h);
}
g=tan(f);
h=fdim(f,b);
g=cos(h);
while(isgreaterequal(g,h)){
a=log(e);
h=pow(b,e);
h=islessequal(e,e);
}
b=exp(b);
a=ceil(a);
if(isless(g,c)){
c=floor(a);
g=log(h);
}
else{
a=fmin(c,d);
h=fmin(c,c);
h=pow(h,c);
g=log(h);
}
e=log10(g);
b=islessgreater(a,b);
h=isless(g,a);
}
