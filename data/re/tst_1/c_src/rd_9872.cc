#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=isless(c,g);
a=exp(d);
d=isless(e,c);
h=islessgreater(h,g);
if(islessequal(a,f)){
d=cos(h);
f=atan(b);
g=isgreaterequal(e,c);
}
e=isgreater(c,e);
d=fmax(d,a);
while(isless(f,f)){
d=sqrt(d);
c=isless(a,b);
c=atan(g);
c=atan(b);
}
b=exp(f);
c=fmin(d,c);
b=log10(c);
d=tan(e);
d=isless(d,b);
}
