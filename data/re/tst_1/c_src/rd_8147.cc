#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=exp(b);
a=exp(d);
c=pow(e,a);
c=islessgreater(c,c);
while(isless(a,d)){
a=fmax(b,b);
e=cos(b);
}
a=atan(e);
e=isless(a,b);
}
