#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fmax(e,d);
c=atan2(b,a);
while(isless(e,c)){
c=sqrt(d);
c=fmax(a,a);
d=fdim(e,c);
}
e=floor(d);
a=isgreaterequal(c,a);
d=fmin(e,c);
e=pow(e,e);
b=tan(d);
}
