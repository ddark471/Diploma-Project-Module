#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fmax(b,d);
c=isgreater(c,d);
e=fmax(g,e);
c=log10(d);
e=islessgreater(e,c);
while(isless(d,d)){
d=log(a);
d=exp(g);
}
f=atan(g);
d=islessgreater(a,g);
c=floor(a);
}
