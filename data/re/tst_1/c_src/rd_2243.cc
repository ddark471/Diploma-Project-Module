#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=acos(d);
c=log10(d);
a=islessequal(b,d);
b=pow(a,a);
while(islessequal(d,a)){
a=isgreaterequal(b,b);
a=pow(a,b);
d=isgreater(b,a);
c=fdim(c,b);
}
a=exp(b);
c=cos(d);
}
