#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fmax(h,g);
a=exp(c);
c=atan2(e,b);
while(isgreaterequal(b,a)){
a=log(e);
a=pow(h,d);
}
d=fmax(f,b);
f=exp(b);
e=asin(c);
h=isless(d,b);
f=exp(a);
}
