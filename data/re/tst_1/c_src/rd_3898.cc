#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=pow(e,e);
d=ceil(g);
e=atan2(f,a);
f=atan2(b,h);
while(isless(d,a)){
e=asin(d);
e=atan(a);
g=fmax(c,c);
e=fmax(b,f);
h=fmin(h,c);
}
c=ceil(a);
d=acos(c);
}
