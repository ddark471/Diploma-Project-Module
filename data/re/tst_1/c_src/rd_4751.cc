#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=islessequal(e,d);
a=log(e);
e=fmax(a,d);
while(isless(e,f)){
f=log(f);
c=log10(f);
b=sqrt(g);
}
d=isgreater(e,b);
c=fmax(b,c);
e=fmax(c,d);
c=isgreaterequal(h,e);
}
