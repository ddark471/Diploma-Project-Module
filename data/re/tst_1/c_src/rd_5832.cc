#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=isless(g,c);
a=isgreater(c,c);
while(isgreaterequal(a,f)){
d=isless(c,f);
d=fmin(c,d);
a=atan2(b,e);
}
e=isless(f,g);
g=floor(a);
f=isgreaterequal(g,a);
}
