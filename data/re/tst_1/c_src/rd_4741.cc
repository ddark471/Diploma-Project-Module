#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fdim(c,c);
c=islessequal(b,d);
d=atan2(d,c);
a=islessequal(c,b);
while(islessequal(d,c)){
b=exp(b);
a=isgreater(d,b);
d=exp(a);
d=islessgreater(c,a);
d=log(d);
}
c=pow(b,a);
b=atan2(a,b);
c=tan(d);
d=log(b);
b=fmax(b,c);
}
