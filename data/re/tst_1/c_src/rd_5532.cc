#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=log(a);
c=islessequal(b,c);
a=tan(a);
d=isgreater(a,a);
d=ceil(d);
while(isgreaterequal(b,c)){
d=cos(a);
b=asin(d);
c=atan2(a,a);
d=atan(b);
}
a=log10(a);
c=sin(d);
}
