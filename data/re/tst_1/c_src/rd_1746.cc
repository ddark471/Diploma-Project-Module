#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fdim(d,h);
h=log10(f);
h=tan(c);
while(isless(h,d)){
c=log10(g);
e=log(c);
f=ceil(a);
b=fmax(e,c);
}
b=isgreater(f,b);
c=ceil(e);
h=islessequal(e,a);
f=log10(h);
f=sqrt(g);
}
