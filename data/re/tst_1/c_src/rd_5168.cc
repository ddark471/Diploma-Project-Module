#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fmin(b,b);
d=isless(d,c);
c=islessgreater(d,d);
a=atan2(a,d);
while(isgreaterequal(a,e)){
e=isgreater(e,a);
b=ceil(b);
}
c=fmax(d,e);
c=isgreater(b,c);
e=exp(c);
}
