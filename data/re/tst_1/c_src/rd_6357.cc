#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=tan(g);
c=floor(a);
while(isgreaterequal(e,e)){
h=isless(g,b);
d=islessequal(c,a);
}
c=cos(g);
f=cos(g);
}
