#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=pow(f,d);
f=floor(f);
while(islessgreater(d,h)){
g=pow(h,c);
d=tan(g);
}
f=fmax(h,a);
h=isless(c,a);
a=islessequal(g,g);
d=isgreaterequal(c,d);
}
