#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=islessequal(b,b);
c=isless(d,c);
b=cos(a);
c=floor(a);
while(isgreaterequal(c,c)){
d=ceil(a);
c=ceil(d);
c=cos(c);
}
c=log10(b);
a=pow(c,c);
}
