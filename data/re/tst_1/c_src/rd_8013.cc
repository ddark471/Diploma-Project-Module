#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=atan(e);
h=acos(g);
h=ceil(d);
a=cos(f);
g=fdim(c,d);
while(isless(f,e)){
h=isgreater(a,d);
f=isgreaterequal(h,f);
h=isless(c,c);
h=cos(g);
}
d=fmin(f,f);
a=isgreaterequal(f,c);
}
