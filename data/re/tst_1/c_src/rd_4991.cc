#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=fmax(c,b);
f=floor(g);
f=fmax(h,e);
while(isless(e,c)){
e=exp(e);
d=exp(g);
e=cos(d);
}
a=fdim(c,a);
f=isgreaterequal(c,b);
d=isless(c,h);
d=atan(g);
d=log10(b);
}
