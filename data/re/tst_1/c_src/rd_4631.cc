#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=fmin(a,d);
d=exp(b);
b=fmin(e,e);
a=atan2(h,c);
if(islessequal(b,e)){
d=sqrt(c);
h=pow(a,g);
}
d=atan2(h,g);
f=atan2(e,d);
g=fmax(d,d);
c=atan2(b,d);
b=floor(c);
while(isless(a,b)){
h=isless(a,c);
d=log(f);
f=exp(c);
f=tan(d);
a=fdim(c,d);
}
f=tan(g);
c=fdim(c,f);
d=exp(b);
g=islessequal(b,b);
if(islessequal(c,g)){
f=fmin(f,b);
e=islessequal(d,g);
g=sin(b);
g=islessequal(b,e);
a=ceil(c);
}
else{
f=pow(e,b);
a=exp(c);
d=isless(f,e);
g=fmin(b,e);
}
f=sin(c);
d=fmax(c,a);
e=isgreaterequal(f,f);
a=sin(f);
g=log10(b);
}
