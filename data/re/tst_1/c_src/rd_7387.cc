#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=exp(a);
h=cos(h);
c=isless(g,b);
while(isless(e,h)){
a=acos(f);
e=pow(b,b);
}
a=atan2(a,e);
g=isgreater(b,a);
b=islessgreater(f,g);
if(isless(b,a)){
h=floor(g);
g=atan2(b,e);
d=sqrt(e);
}
else{
h=islessgreater(h,e);
f=fdim(a,h);
f=isless(a,h);
e=fmin(e,b);
}
e=pow(e,a);
e=sqrt(b);
h=asin(b);
}
