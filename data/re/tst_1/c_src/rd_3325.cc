#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=cos(c);
g=floor(f);
d=islessequal(b,e);
if(isless(d,f)){
a=sqrt(c);
a=ceil(f);
f=atan2(d,a);
}
else{
e=ceil(a);
c=exp(a);
}
b=atan2(f,b);
f=islessequal(g,c);
e=isgreaterequal(b,d);
while(islessequal(f,a)){
a=cos(d);
c=exp(f);
d=log(a);
}
g=fdim(c,e);
b=islessequal(a,b);
f=fmax(a,c);
a=fmax(d,a);
g=isgreater(a,g);
if(islessgreater(f,e)){
g=sin(d);
e=cos(g);
}
else{
d=log(c);
a=log(e);
e=isgreater(e,a);
d=fdim(e,a);
}
a=fmax(a,c);
e=log(b);
}
