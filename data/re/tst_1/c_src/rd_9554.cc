#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=log10(a);
d=log(d);
while(islessgreater(e,f)){
a=isgreaterequal(e,c);
c=isgreater(f,c);
f=log(d);
}
f=isless(f,d);
c=ceil(d);
a=ceil(d);
a=pow(f,e);
}
