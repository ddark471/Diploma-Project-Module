#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=exp(c);
h=floor(g);
f=sin(d);
e=isgreaterequal(c,g);
g=islessgreater(e,g);
while(isless(g,b)){
e=fmax(g,h);
f=fdim(f,h);
f=isgreaterequal(b,f);
h=fmax(h,h);
a=islessequal(a,h);
}
d=sin(d);
c=log(f);
if(isless(d,a)){
b=islessgreater(c,d);
a=atan(e);
c=fdim(e,a);
}
a=islessequal(f,f);
b=sqrt(c);
f=fdim(d,d);
}
