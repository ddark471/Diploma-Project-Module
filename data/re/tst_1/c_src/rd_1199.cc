#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=atan2(f,a);
a=isgreaterequal(h,c);
g=isgreaterequal(b,e);
g=floor(b);
while(isless(g,g)){
h=isgreater(b,e);
f=fmax(h,a);
d=islessequal(d,a);
}
h=islessequal(f,b);
f=fmax(f,b);
a=isless(g,f);
}
