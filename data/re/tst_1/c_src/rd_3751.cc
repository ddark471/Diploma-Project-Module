#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=isless(g,c);
e=ceil(f);
f=fmax(e,e);
d=tan(g);
d=islessgreater(c,e);
while(islessgreater(b,d)){
c=sqrt(e);
g=islessequal(h,b);
}
c=isless(d,a);
g=fmin(a,f);
b=pow(h,a);
e=pow(h,e);
if(islessgreater(d,d)){
f=exp(d);
e=asin(g);
}
else{
f=cos(b);
e=fmin(e,c);
c=sqrt(c);
d=fmax(d,c);
}
g=isgreater(b,e);
g=log10(e);
c=ceil(b);
c=sqrt(e);
h=islessequal(f,b);
}
