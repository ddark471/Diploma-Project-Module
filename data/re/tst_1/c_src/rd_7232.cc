#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=sin(g);
a=isless(g,d);
b=fmax(c,e);
b=islessequal(g,a);
c=exp(f);
if(islessgreater(e,b)){
c=acos(g);
b=exp(b);
a=isless(a,b);
}
b=tan(c);
c=cos(d);
f=isgreater(b,c);
c=exp(g);
while(islessequal(c,c)){
e=fmin(e,a);
g=sqrt(g);
d=log(b);
g=fmax(a,c);
f=islessgreater(c,f);
}
d=log10(f);
b=fdim(c,d);
}
