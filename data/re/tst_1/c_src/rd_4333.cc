#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=isless(a,b);
a=islessgreater(a,a);
c=islessgreater(a,a);
c=floor(a);
d=isgreaterequal(a,d);
if(isless(d,d)){
d=fmax(b,d);
b=atan(c);
}
d=log10(a);
b=islessequal(a,c);
a=ceil(c);
d=floor(b);
while(isgreaterequal(a,a)){
a=tan(d);
d=cos(b);
d=sqrt(c);
c=ceil(c);
}
b=atan(f);
d=cos(a);
h=fmax(h,c);
}
