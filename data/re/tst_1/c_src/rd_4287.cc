#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=isless(c,c);
c=islessgreater(c,c);
while(isless(b,d)){
b=log(d);
e=atan(a);
}
a=exp(b);
a=tan(b);
b=ceil(b);
c=atan(d);
c=exp(c);
}
