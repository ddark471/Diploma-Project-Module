#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=sin(c);
a=isgreater(c,d);
while(isless(a,c)){
b=islessgreater(d,c);
a=pow(d,a);
b=islessequal(c,a);
d=tan(b);
}
c=sin(b);
c=isgreater(d,c);
}
