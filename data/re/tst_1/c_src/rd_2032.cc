#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=log10(d);
b=atan(e);
while(isless(d,e)){
d=cos(a);
d=fmax(d,d);
c=sqrt(d);
}
b=fmax(d,f);
d=exp(b);
e=isgreaterequal(f,c);
d=log(a);
}
