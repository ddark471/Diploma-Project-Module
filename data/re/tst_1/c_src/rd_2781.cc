#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=isgreaterequal(e,b);
d=floor(c);
while(islessgreater(c,a)){
e=fdim(e,b);
a=atan2(c,a);
e=pow(c,e);
d=floor(b);
}
b=pow(e,b);
a=ceil(e);
}
