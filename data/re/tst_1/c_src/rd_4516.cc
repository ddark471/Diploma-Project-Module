#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=atan(b);
d=cos(e);
d=asin(d);
d=exp(c);
while(islessequal(a,b)){
b=log(c);
b=isgreater(d,d);
d=pow(a,d);
a=islessgreater(d,c);
}
b=asin(a);
c=cos(a);
b=sqrt(c);
}
