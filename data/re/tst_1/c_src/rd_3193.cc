#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=islessequal(b,e);
b=log(a);
d=isless(a,b);
d=ceil(a);
while(isless(b,a)){
c=pow(a,d);
d=islessgreater(e,b);
d=ceil(d);
d=fdim(e,d);
c=log(d);
}
d=islessequal(b,b);
e=exp(b);
c=tan(b);
d=log10(e);
}
