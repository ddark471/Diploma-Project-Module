#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=atan(b);
c=floor(a);
while(islessgreater(d,c)){
d=pow(c,d);
c=isgreater(d,b);
e=atan(a);
b=isless(c,e);
d=tan(e);
}
a=sqrt(b);
d=fmax(d,a);
b=sin(b);
d=islessgreater(e,a);
if(isless(c,b)){
c=fmax(f,f);
e=isgreaterequal(b,a);
d=isgreater(b,a);
c=tan(c);
}
else{
b=atan2(e,b);
a=atan2(b,a);
}
d=islessequal(a,c);
f=tan(d);
c=atan(f);
e=floor(a);
}
