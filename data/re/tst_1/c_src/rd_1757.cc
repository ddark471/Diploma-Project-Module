#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=cos(c);
f=log10(b);
if(islessequal(a,c)){
c=atan(c);
e=tan(f);
c=isgreater(b,e);
f=isgreater(a,a);
h=ceil(f);
}
else{
c=isless(a,h);
g=pow(b,d);
b=cos(b);
a=fmin(b,b);
g=floor(e);
}
f=fmax(c,a);
f=fmax(h,h);
while(islessgreater(d,g)){
e=islessequal(d,d);
d=floor(e);
}
c=isgreaterequal(e,c);
h=pow(a,b);
f=fmax(f,b);
}
