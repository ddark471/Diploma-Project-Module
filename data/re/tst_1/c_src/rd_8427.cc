#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fmin(g,c);
a=fmax(b,c);
d=floor(f);
f=atan2(g,g);
b=isgreaterequal(g,e);
while(isless(d,e)){
a=floor(f);
g=pow(e,g);
c=atan(b);
c=atan2(e,d);
}
g=fmax(d,e);
d=fmax(b,c);
}
