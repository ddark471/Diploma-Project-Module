#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=fdim(e,c);
g=pow(c,a);
f=islessgreater(g,a);
e=exp(b);
while(isgreaterequal(b,e)){
d=sin(a);
c=sqrt(a);
a=exp(g);
}
c=sqrt(d);
b=sin(c);
a=isgreaterequal(b,b);
if(isgreaterequal(b,c)){
c=sqrt(a);
c=islessgreater(b,b);
}
b=isless(b,c);
a=sin(d);
}
