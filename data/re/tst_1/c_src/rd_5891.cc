#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=sqrt(c);
b=floor(d);
b=islessgreater(b,b);
if(islessgreater(c,a)){
a=isgreater(c,b);
b=isgreater(b,b);
a=exp(a);
}
else{
c=cos(d);
a=atan2(d,c);
a=fmax(d,b);
}
d=floor(b);
a=atan(c);
a=exp(c);
while(isless(b,c)){
c=floor(b);
a=isgreater(a,b);
c=isgreater(b,c);
c=cos(d);
}
d=islessgreater(d,b);
d=exp(a);
c=fmax(a,d);
c=fmax(c,c);
d=isgreaterequal(a,c);
}
