#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=floor(g);
b=pow(c,e);
while(islessequal(a,a)){
a=isgreater(c,d);
f=log(b);
f=exp(e);
c=fdim(f,b);
d=pow(f,e);
}
d=islessgreater(b,g);
a=acos(g);
c=sin(e);
}
