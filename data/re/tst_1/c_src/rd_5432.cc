#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=isgreaterequal(c,a);
c=exp(b);
d=pow(a,a);
b=islessequal(c,a);
while(isless(b,d)){
d=ceil(b);
c=asin(b);
}
d=floor(c);
a=atan(b);
d=sqrt(a);
b=ceil(d);
c=fmin(b,a);
if(isless(d,b)){
d=cos(d);
b=floor(c);
}
b=log10(b);
c=log10(c);
a=asin(c);
c=isless(a,b);
}
