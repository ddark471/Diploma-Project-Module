#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=sqrt(c);
d=isless(a,a);
b=atan(f);
d=isless(e,b);
while(isless(a,d)){
d=isless(e,c);
b=islessgreater(c,d);
d=log10(f);
a=tan(a);
c=exp(f);
}
e=fmax(e,a);
b=cos(a);
}
