#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fdim(a,b);
d=fmin(b,c);
c=fmax(d,d);
c=pow(a,a);
d=tan(b);
while(islessgreater(b,b)){
a=sqrt(a);
d=atan(c);
a=islessequal(b,a);
b=isless(d,d);
d=pow(b,d);
}
d=acos(a);
d=fdim(d,d);
}
