#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=pow(d,d);
c=sin(b);
d=exp(a);
d=sin(c);
c=ceil(c);
while(islessequal(d,d)){
c=fmax(b,d);
c=islessequal(d,a);
b=ceil(d);
}
c=acos(h);
g=sin(h);
f=tan(a);
c=exp(e);
g=ceil(g);
if(isless(g,g)){
h=isgreaterequal(c,d);
d=fmax(f,g);
}
e=log10(e);
e=fmax(c,a);
a=atan2(f,h);
f=ceil(d);
e=pow(d,c);
}
