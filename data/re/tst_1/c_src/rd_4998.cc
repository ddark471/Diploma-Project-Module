#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=acos(a);
a=isgreater(a,f);
if(islessgreater(c,a)){
g=islessgreater(d,d);
a=fmin(g,a);
f=fdim(d,f);
g=isgreater(b,b);
b=fmin(c,g);
}
c=sqrt(c);
g=fmin(g,f);
f=sin(b);
while(islessgreater(b,c)){
c=isgreater(g,f);
f=fmax(e,b);
}
g=islessgreater(f,g);
e=islessgreater(b,g);
f=tan(g);
}
