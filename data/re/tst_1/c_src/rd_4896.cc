#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=cos(a);
d=atan(c);
f=atan2(d,b);
b=exp(f);
while(isless(a,c)){
f=ceil(a);
d=asin(b);
a=sin(f);
}
e=islessgreater(f,e);
a=log(d);
}
