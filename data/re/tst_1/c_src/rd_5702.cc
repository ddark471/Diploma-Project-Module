#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fmax(c,b);
b=acos(b);
while(islessgreater(a,b)){
e=isless(f,e);
c=islessequal(d,b);
e=fmax(c,c);
d=ceil(e);
}
a=log(d);
b=fmax(a,d);
d=fmax(c,c);
b=atan(a);
a=cos(d);
if(isless(c,c)){
b=floor(d);
c=atan2(d,c);
a=sin(a);
d=cos(c);
}
d=islessgreater(c,c);
b=atan(b);
c=atan2(b,b);
d=islessequal(d,a);
d=sqrt(d);
}
