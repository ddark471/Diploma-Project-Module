#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=log(c);
b=tan(g);
if(islessgreater(e,f)){
f=exp(b);
g=fmax(f,a);
}
a=sin(b);
b=sqrt(g);
f=ceil(a);
c=fmax(a,g);
while(islessequal(d,c)){
f=atan(d);
d=fdim(d,c);
f=asin(c);
d=tan(c);
g=fdim(f,g);
}
f=isless(e,e);
g=sqrt(c);
if(islessgreater(h,e)){
e=acos(d);
a=atan(e);
g=sqrt(f);
c=pow(c,b);
d=islessgreater(g,a);
}
else{
e=cos(b);
d=sqrt(g);
d=pow(g,d);
h=fmin(e,b);
d=atan(f);
}
h=floor(a);
b=acos(f);
a=fmin(h,d);
h=fmax(d,g);
}
