#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=pow(b,d);
f=log(c);
g=fmax(g,e);
while(islessgreater(e,c)){
b=isless(e,e);
g=fmin(g,g);
}
a=fdim(d,b);
c=islessequal(a,b);
b=islessequal(c,d);
d=islessgreater(b,a);
if(isless(d,c)){
b=atan2(a,b);
c=fmax(a,a);
}
else{
c=fmin(c,c);
c=atan2(b,a);
}
a=log10(a);
b=fmax(c,a);
c=ceil(a);
d=atan2(a,a);
b=isgreaterequal(b,d);
}
