#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=islessgreater(a,c);
c=acos(d);
while(isless(c,a)){
b=sqrt(a);
c=isgreater(a,b);
b=cos(c);
d=atan2(b,a);
d=islessequal(a,b);
}
d=islessequal(b,a);
d=isless(a,c);
d=fmin(d,b);
a=fmax(b,b);
}
