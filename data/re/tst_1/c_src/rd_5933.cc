#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=exp(c);
e=atan(f);
a=isgreater(f,e);
if(isless(f,c)){
b=log10(f);
e=log(e);
}
else{
c=fmax(a,b);
f=isgreaterequal(d,a);
d=log10(c);
c=atan(g);
}
b=ceil(b);
b=atan(h);
e=floor(g);
d=islessequal(a,g);
while(islessequal(e,c)){
b=islessgreater(h,a);
f=islessequal(e,b);
}
b=isgreaterequal(f,a);
d=log(f);
c=fmax(a,c);
}
