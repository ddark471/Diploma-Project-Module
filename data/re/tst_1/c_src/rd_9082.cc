#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=atan2(c,a);
c=floor(a);
f=ceil(c);
f=fmin(e,f);
d=log10(d);
while(isgreaterequal(g,g)){
a=floor(c);
c=islessequal(d,g);
d=pow(d,b);
f=fdim(g,a);
}
h=sqrt(g);
d=islessequal(h,e);
if(islessequal(g,e)){
e=islessequal(g,a);
b=sin(a);
}
else{
a=floor(e);
d=islessgreater(e,d);
e=isless(g,h);
}
a=fmin(h,c);
h=floor(d);
c=cos(f);
d=sin(b);
}
