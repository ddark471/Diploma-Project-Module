#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=acos(g);
e=atan2(e,f);
while(isless(d,e)){
c=fmin(b,b);
c=isgreater(c,a);
a=fmin(d,d);
a=isgreater(f,g);
a=atan(g);
}
f=fmax(f,a);
e=sin(a);
d=isless(e,c);
e=atan2(f,g);
g=cos(g);
}
