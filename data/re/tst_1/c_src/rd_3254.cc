#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=isgreater(e,e);
f=fmax(f,b);
while(islessequal(f,c)){
f=sin(e);
e=fmax(d,f);
}
c=sin(b);
c=pow(a,c);
d=floor(c);
d=log(d);
e=log(a);
}
