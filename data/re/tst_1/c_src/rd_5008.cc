#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=atan2(g,a);
a=isgreater(f,c);
b=fmax(c,h);
h=floor(b);
a=atan(f);
while(islessequal(b,h)){
e=islessgreater(b,h);
g=sin(d);
h=exp(f);
f=atan(h);
}
c=atan2(d,h);
f=pow(d,b);
a=cos(a);
h=log10(d);
e=ceil(e);
}
