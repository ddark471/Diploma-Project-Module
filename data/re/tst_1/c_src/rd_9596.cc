#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=isless(a,d);
d=islessequal(a,b);
while(islessequal(c,c)){
c=islessequal(d,d);
c=isgreaterequal(c,d);
c=isgreater(b,d);
b=atan2(b,b);
}
c=fmax(c,c);
d=log(b);
}
