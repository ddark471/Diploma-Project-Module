#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=floor(e);
d=sqrt(d);
while(isless(a,e)){
d=floor(c);
f=islessgreater(f,a);
e=fmax(c,a);
}
d=floor(c);
d=fmax(e,b);
f=islessequal(c,e);
a=tan(d);
e=fmax(b,f);
}
