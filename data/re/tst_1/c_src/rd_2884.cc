#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=islessequal(b,d);
c=fmin(b,b);
while(islessgreater(a,d)){
a=pow(a,b);
b=acos(a);
d=exp(a);
}
a=pow(c,e);
a=asin(f);
c=islessequal(f,g);
c=fdim(f,c);
g=isgreater(f,e);
if(isless(d,f)){
g=fdim(f,b);
a=fmin(b,g);
a=floor(f);
g=isgreaterequal(d,f);
f=islessgreater(b,b);
}
else{
d=sqrt(a);
g=log10(g);
d=isless(a,b);
}
c=isless(f,e);
c=isgreaterequal(f,d);
d=acos(a);
f=fmin(e,a);
}
