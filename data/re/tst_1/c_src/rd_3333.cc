#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fmax(b,a);
a=fmax(d,a);
while(isless(c,d)){
d=sqrt(e);
a=isgreater(d,b);
e=isless(e,d);
}
a=islessgreater(d,b);
e=sqrt(b);
c=isgreater(b,a);
a=log(d);
}
