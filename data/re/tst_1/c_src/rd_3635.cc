#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=pow(e,d);
b=fmax(d,d);
a=log(g);
b=floor(c);
if(islessgreater(f,g)){
d=isless(c,e);
b=isless(g,d);
g=sqrt(d);
}
b=atan2(b,c);
e=pow(b,e);
f=fmin(b,e);
g=sqrt(a);
c=fdim(d,c);
while(islessequal(g,d)){
d=fmax(f,b);
d=atan(d);
a=exp(f);
}
d=sqrt(d);
a=atan2(a,c);
if(islessgreater(d,a)){
d=islessequal(d,a);
d=log10(c);
d=sqrt(c);
}
b=isgreater(d,c);
d=tan(c);
d=islessequal(c,c);
}
