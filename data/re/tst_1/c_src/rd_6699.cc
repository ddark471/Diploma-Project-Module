#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fmax(d,e);
e=ceil(a);
e=floor(a);
while(islessequal(c,c)){
d=fmax(a,b);
e=exp(e);
a=atan2(c,f);
d=atan2(d,d);
}
b=isgreater(d,d);
d=islessgreater(e,e);
d=ceil(c);
c=atan(d);
d=isgreater(c,d);
}
