#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fmin(d,b);
d=isless(a,c);
if(islessgreater(d,d)){
a=floor(b);
c=fmin(d,d);
d=pow(b,c);
b=cos(c);
}
else{
c=fmax(d,d);
a=pow(c,a);
b=tan(c);
c=atan(b);
}
b=floor(a);
c=atan(d);
while(isless(d,a)){
c=fdim(b,d);
c=exp(d);
c=cos(c);
a=isgreaterequal(b,b);
}
b=cos(c);
d=sqrt(d);
d=isgreater(a,d);
}
