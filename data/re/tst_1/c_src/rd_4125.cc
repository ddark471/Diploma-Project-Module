#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=isless(f,c);
g=pow(d,f);
if(isless(f,e)){
h=fmin(a,d);
b=pow(d,f);
}
e=sin(h);
b=fmax(g,g);
a=asin(c);
f=asin(c);
c=floor(a);
while(islessequal(b,c)){
h=atan(a);
e=islessequal(b,g);
}
c=pow(a,a);
e=log10(a);
a=atan(d);
if(isgreaterequal(b,c)){
b=exp(b);
c=cos(d);
d=fmin(d,b);
d=pow(a,a);
d=asin(c);
}
else{
b=cos(d);
a=atan2(b,b);
}
d=sin(b);
c=isless(b,e);
}
