#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=exp(c);
c=ceil(c);
while(islessequal(d,e)){
a=log(b);
c=fmax(c,e);
c=cos(a);
e=tan(e);
}
e=atan2(a,d);
c=islessequal(a,a);
}
