#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=cos(c);
f=exp(e);
f=log10(h);
h=sqrt(f);
d=exp(d);
while(islessgreater(f,f)){
e=exp(b);
a=isless(d,d);
}
h=fmax(h,h);
h=atan(c);
d=isless(a,g);
c=ceil(f);
}
