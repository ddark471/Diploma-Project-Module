#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=exp(a);
d=atan(g);
a=log10(d);
if(isless(f,c)){
f=isless(e,h);
c=sin(c);
e=fdim(c,c);
g=ceil(a);
}
b=islessequal(h,a);
e=floor(b);
e=pow(a,d);
while(isless(c,h)){
h=log10(c);
h=sin(e);
f=fmax(b,a);
b=pow(h,e);
e=islessgreater(c,b);
}
a=floor(a);
c=floor(a);
a=atan2(b,a);
c=log(b);
b=fmax(d,d);
if(islessgreater(c,b)){
b=isgreaterequal(d,b);
b=ceil(a);
d=log(a);
}
else{
d=islessequal(c,c);
d=islessequal(d,a);
a=acos(c);
}
a=sin(b);
d=log10(b);
}
