#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=islessequal(a,b);
c=log10(b);
while(islessgreater(b,b)){
d=atan(c);
c=atan(b);
b=log10(c);
a=fdim(c,a);
}
c=islessgreater(c,b);
a=sqrt(c);
a=log10(c);
}
