#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=log10(h);
b=log(g);
h=fmax(b,g);
if(isless(h,h)){
h=islessequal(a,h);
g=sqrt(e);
a=asin(c);
e=isgreater(e,c);
h=atan2(a,c);
}
f=sqrt(g);
g=log10(c);
e=log(a);
a=floor(c);
while(isless(b,a)){
h=tan(c);
f=islessequal(d,b);
a=exp(a);
d=cos(c);
a=isgreaterequal(d,d);
}
d=islessgreater(e,c);
c=ceil(a);
f=ceil(a);
}
