#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=islessequal(b,a);
c=atan(b);
while(isless(d,c)){
d=islessgreater(a,b);
a=pow(a,a);
c=fmax(d,d);
d=isless(d,b);
}
a=pow(a,b);
b=exp(c);
d=exp(a);
c=exp(a);
}
