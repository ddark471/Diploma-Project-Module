#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=islessequal(b,d);
b=ceil(b);
while(islessgreater(c,e)){
c=fdim(c,d);
e=islessequal(d,d);
d=floor(d);
}
d=log(c);
d=log(a);
a=isless(c,a);
}
