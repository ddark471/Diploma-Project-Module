#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=ceil(b);
g=tan(f);
while(isless(f,g)){
e=exp(f);
c=pow(a,g);
}
f=acos(b);
h=isless(f,e);
f=tan(g);
f=atan2(f,c);
if(isless(g,h)){
g=asin(h);
b=cos(e);
}
b=fmax(b,e);
g=sqrt(e);
}
