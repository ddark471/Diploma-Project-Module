#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=isless(g,g);
h=exp(a);
e=sqrt(c);
while(isless(f,d)){
h=atan2(h,f);
a=tan(e);
b=floor(e);
b=fmax(b,h);
}
g=cos(c);
f=pow(e,a);
}
