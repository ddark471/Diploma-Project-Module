#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=isless(c,d);
d=isgreaterequal(c,d);
while(islessgreater(c,e)){
a=isgreater(b,b);
c=fdim(b,e);
}
b=log10(c);
e=sin(c);
e=floor(e);
d=fmax(e,a);
e=atan(e);
}
