#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=islessgreater(b,g);
d=fmax(b,c);
a=islessequal(d,g);
while(islessgreater(f,b)){
e=islessequal(b,g);
f=isgreater(g,f);
}
c=fmax(g,b);
a=isgreater(c,c);
f=atan(g);
}
