#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=exp(d);
c=fmax(d,c);
c=fdim(a,c);
c=fmax(d,d);
while(islessgreater(d,c)){
d=isless(d,c);
d=cos(d);
}
c=atan(a);
c=atan(g);
h=isgreater(c,e);
g=atan2(c,h);
h=sin(d);
}
