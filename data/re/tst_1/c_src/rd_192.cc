#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=pow(a,c);
a=islessgreater(c,b);
c=sqrt(b);
e=log10(a);
while(isless(d,e)){
c=pow(c,b);
d=log(b);
a=fmin(a,d);
}
c=asin(f);
c=atan2(c,b);
d=pow(e,e);
a=cos(b);
}
