#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=exp(d);
a=islessequal(b,d);
c=log10(c);
b=islessequal(c,c);
d=tan(a);
while(islessgreater(b,a)){
a=log(b);
d=floor(a);
}
d=fmax(b,b);
a=isgreater(b,b);
c=isless(d,c);
a=fmax(a,d);
d=cos(d);
}
