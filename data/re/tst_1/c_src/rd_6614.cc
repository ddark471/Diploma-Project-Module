#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=fmin(e,a);
e=exp(c);
while(isless(g,c)){
b=cos(c);
g=asin(c);
a=fmax(a,c);
}
f=isless(d,b);
f=tan(d);
a=islessequal(a,f);
f=isless(a,a);
if(islessgreater(f,f)){
g=cos(d);
c=exp(g);
g=tan(a);
f=isless(g,g);
}
d=fdim(e,g);
f=isgreater(b,h);
c=sin(b);
b=tan(b);
h=islessequal(c,h);
}
