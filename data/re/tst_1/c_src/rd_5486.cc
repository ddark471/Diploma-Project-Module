#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=exp(c);
c=acos(b);
b=isless(a,d);
while(islessequal(d,a)){
b=floor(c);
a=fmax(c,b);
d=isgreaterequal(c,d);
}
c=atan(d);
c=pow(d,c);
if(islessequal(a,c)){
c=fmin(b,d);
c=floor(b);
a=islessequal(a,d);
}
c=atan(b);
d=log(d);
}
