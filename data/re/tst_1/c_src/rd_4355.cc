#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fmax(e,c);
e=fmin(b,d);
while(isless(c,c)){
d=asin(a);
c=isgreater(a,b);
}
e=fdim(a,d);
e=fmax(a,a);
d=islessequal(a,c);
}
