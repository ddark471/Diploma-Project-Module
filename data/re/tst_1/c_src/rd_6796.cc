#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fmax(b,d);
c=isgreater(h,e);
if(isless(a,a)){
a=islessequal(d,e);
f=cos(h);
c=tan(g);
f=log(a);
g=isgreater(b,h);
}
b=isless(h,b);
a=isgreater(h,g);
e=islessequal(d,h);
c=fmax(a,d);
while(isless(b,g)){
d=fmax(c,b);
h=sin(e);
}
g=exp(c);
a=pow(d,a);
a=isless(c,f);
if(islessgreater(c,c)){
c=pow(f,d);
c=log(a);
}
else{
e=exp(e);
c=islessgreater(g,c);
a=tan(a);
a=sqrt(d);
g=log10(a);
}
a=tan(a);
e=sqrt(c);
}
