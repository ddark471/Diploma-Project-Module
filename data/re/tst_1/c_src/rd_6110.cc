#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=sin(c);
d=sqrt(a);
while(isgreaterequal(a,d)){
d=exp(d);
e=asin(d);
d=pow(a,b);
}
c=islessgreater(b,e);
a=isless(d,b);
}
