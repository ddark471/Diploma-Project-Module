#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=isless(c,e);
c=islessequal(a,a);
f=isless(f,d);
while(islessgreater(e,c)){
d=isless(a,c);
c=isless(a,c);
}
d=isgreaterequal(c,f);
d=isgreater(b,b);
}
