#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=atan2(h,g);
f=fmax(d,b);
g=atan2(g,c);
if(isgreaterequal(b,e)){
g=ceil(g);
b=cos(a);
g=cos(h);
}
f=log10(h);
c=sqrt(h);
b=isless(h,c);
f=fmin(c,b);
a=isless(e,d);
while(isgreaterequal(a,f)){
d=log10(e);
d=fmin(c,d);
}
d=floor(b);
a=isgreaterequal(a,b);
}
