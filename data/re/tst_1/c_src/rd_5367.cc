#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=log10(f);
d=sin(f);
while(islessgreater(d,e)){
d=sqrt(e);
b=fmin(f,h);
}
d=atan2(b,d);
h=fmin(e,a);
}
