#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=exp(a);
h=ceil(g);
if(isless(f,a)){
d=fmin(h,g);
e=fmax(a,a);
}
g=atan2(a,f);
e=asin(f);
d=log10(a);
d=log(h);
f=ceil(e);
while(isless(f,d)){
b=sin(h);
f=tan(e);
}
f=cos(f);
c=sqrt(f);
g=cos(f);
a=acos(g);
}
