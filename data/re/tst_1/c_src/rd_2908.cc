#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=tan(c);
c=sqrt(c);
while(isgreaterequal(h,b)){
d=sin(b);
g=fdim(h,c);
c=islessgreater(a,a);
h=isgreater(e,d);
}
c=sin(d);
a=isgreater(e,c);
}
