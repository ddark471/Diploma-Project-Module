#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=log(b);
a=islessequal(a,b);
b=fmin(a,c);
if(islessequal(a,d)){
b=atan2(a,d);
a=fmax(g,a);
e=cos(c);
}
else{
f=pow(c,a);
b=fmin(c,e);
g=atan2(b,b);
a=fmax(b,b);
}
g=isgreaterequal(b,g);
d=atan2(b,a);
b=log10(e);
while(islessequal(d,f)){
d=ceil(a);
b=islessgreater(e,a);
c=islessgreater(g,f);
}
a=sin(f);
a=isless(a,b);
a=atan(h);
}
