#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=exp(d);
b=atan(d);
f=isless(e,f);
while(isgreaterequal(f,b)){
b=exp(g);
e=acos(g);
}
a=fmax(e,g);
g=log10(f);
}
