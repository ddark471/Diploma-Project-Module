#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=log10(b);
a=pow(d,c);
c=exp(c);
a=islessgreater(e,e);
d=log10(f);
while(islessequal(a,b)){
d=fmax(e,c);
e=isgreaterequal(c,b);
}
h=acos(f);
f=isgreater(h,b);
if(islessgreater(g,c)){
e=pow(g,g);
d=atan2(h,b);
c=exp(d);
h=log(b);
}
h=fmin(f,h);
e=isless(a,b);
}
