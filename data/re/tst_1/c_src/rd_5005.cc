#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=log10(a);
e=isless(c,a);
a=sqrt(d);
c=atan2(d,c);
d=exp(c);
while(isgreaterequal(c,b)){
a=floor(d);
c=log(b);
}
a=atan2(c,c);
c=floor(a);
}
