#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fmax(a,e);
c=atan(f);
c=isgreater(b,b);
e=isless(f,a);
c=fmin(d,d);
while(isgreaterequal(f,a)){
a=fmax(b,d);
b=pow(a,b);
e=pow(b,b);
}
f=isgreaterequal(f,d);
d=floor(f);
f=pow(f,e);
}
