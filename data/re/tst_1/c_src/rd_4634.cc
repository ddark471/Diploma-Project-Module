#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=log10(d);
d=ceil(a);
b=atan2(b,d);
b=atan(b);
b=fmax(a,c);
while(islessequal(b,d)){
c=pow(d,a);
a=atan2(c,c);
a=fmin(a,b);
c=exp(c);
}
c=isless(c,d);
a=isless(c,a);
}
