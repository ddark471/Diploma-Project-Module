#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=ceil(f);
e=pow(d,g);
a=isless(e,f);
while(isless(g,d)){
a=atan2(g,b);
h=islessequal(e,d);
b=isgreater(e,b);
}
b=fmin(b,a);
d=isgreater(d,c);
if(islessequal(d,b)){
c=atan2(b,a);
a=isless(b,b);
b=sin(c);
a=atan(b);
b=ceil(c);
}
d=cos(d);
c=islessequal(d,a);
d=isless(a,c);
c=asin(d);
b=floor(a);
}
