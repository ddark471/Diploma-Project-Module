#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=fmax(a,f);
b=exp(b);
while(isless(f,c)){
e=atan2(a,f);
a=cos(b);
c=islessequal(a,b);
e=isgreaterequal(f,b);
}
h=atan2(f,b);
g=fmin(b,d);
if(isgreaterequal(h,h)){
g=pow(c,d);
g=exp(e);
d=atan(g);
h=acos(d);
}
f=floor(a);
f=islessgreater(f,a);
c=fdim(e,a);
}
