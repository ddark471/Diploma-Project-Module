#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=pow(f,f);
d=isless(c,c);
c=sqrt(f);
while(islessequal(d,e)){
e=atan2(a,d);
d=isless(a,c);
}
e=isless(e,b);
f=islessequal(a,a);
f=sqrt(f);
c=tan(a);
c=log(e);
}
