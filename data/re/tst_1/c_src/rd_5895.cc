#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=sqrt(e);
h=islessequal(e,e);
b=fdim(a,a);
b=isgreater(g,d);
f=exp(g);
if(isgreaterequal(c,d)){
e=fmax(a,a);
d=fmax(b,g);
e=pow(h,h);
}
else{
a=tan(g);
h=islessequal(g,e);
h=pow(g,e);
a=isless(d,b);
}
g=islessgreater(g,a);
g=sin(c);
g=exp(b);
g=fmin(a,h);
while(islessgreater(f,f)){
d=islessgreater(d,a);
c=sin(a);
}
a=sqrt(e);
d=islessgreater(d,d);
e=pow(e,d);
e=fmax(e,b);
c=islessgreater(a,d);
}
