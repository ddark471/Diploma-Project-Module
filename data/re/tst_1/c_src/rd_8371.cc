#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=isless(d,a);
a=atan(f);
f=fmin(a,a);
while(islessequal(a,c)){
b=floor(e);
a=fmin(a,b);
c=fmax(d,e);
f=islessgreater(a,e);
}
a=fmin(a,b);
d=atan2(c,a);
e=floor(c);
d=islessequal(c,b);
}
