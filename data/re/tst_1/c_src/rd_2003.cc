#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=islessgreater(a,a);
a=atan2(e,b);
while(isless(b,a)){
f=fmax(d,f);
b=floor(c);
}
c=atan2(c,d);
a=isless(c,b);
d=islessgreater(a,d);
a=islessequal(c,a);
}
