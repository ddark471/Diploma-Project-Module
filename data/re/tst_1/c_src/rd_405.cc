#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=isgreater(b,d);
d=isgreater(d,a);
a=islessequal(d,b);
a=islessequal(d,a);
if(isless(b,a)){
d=isless(a,d);
c=log(a);
b=log(b);
a=log(b);
}
b=pow(c,b);
a=ceil(d);
d=fmin(b,a);
while(islessequal(c,d)){
c=islessequal(d,c);
a=sqrt(d);
a=ceil(b);
d=acos(d);
}
d=fmax(a,e);
c=isgreater(a,e);
}
