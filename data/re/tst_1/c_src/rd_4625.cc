#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=tan(f);
b=log10(d);
while(isless(e,a)){
b=isgreater(c,b);
e=islessequal(a,d);
}
f=islessgreater(f,d);
d=ceil(e);
g=log10(g);
}
