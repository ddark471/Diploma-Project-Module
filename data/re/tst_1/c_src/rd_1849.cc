#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=pow(g,c);
c=floor(f);
while(isgreaterequal(b,b)){
c=log10(b);
e=fmax(b,e);
g=asin(c);
}
f=cos(e);
d=atan2(b,c);
a=exp(a);
}
