#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=log(a);
a=fmax(c,b);
d=isgreater(b,c);
d=log10(a);
b=isless(d,a);
while(islessgreater(a,c)){
a=ceil(b);
c=islessgreater(d,d);
d=islessequal(c,b);
b=ceil(c);
a=fmin(c,b);
}
d=cos(a);
c=sin(e);
}
