#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=asin(h);
a=asin(d);
b=isless(h,a);
while(isless(b,b)){
c=atan(e);
f=fmin(e,a);
h=fmax(d,h);
f=islessequal(b,d);
d=asin(c);
}
g=ceil(c);
e=tan(f);
}
