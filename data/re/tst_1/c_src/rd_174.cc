#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=log(a);
a=isless(b,c);
d=atan2(b,b);
d=fmax(d,c);
if(isless(b,c)){
c=isless(d,a);
a=isgreater(c,c);
}
else{
c=log(c);
d=pow(b,d);
}
a=log(d);
c=atan(d);
d=fdim(c,a);
c=acos(b);
while(islessgreater(b,b)){
d=isgreater(c,c);
c=tan(d);
a=log(d);
c=log(c);
}
a=log(e);
e=isless(d,b);
c=cos(b);
}
