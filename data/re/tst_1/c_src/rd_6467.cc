#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=log(c);
h=fmax(a,f);
e=fdim(g,f);
d=fmax(g,c);
b=exp(g);
while(islessequal(h,h)){
f=isgreaterequal(f,c);
g=islessequal(b,e);
g=isgreater(g,a);
g=isgreater(b,g);
}
a=fdim(d,g);
a=fmin(c,a);
g=cos(g);
g=sqrt(d);
h=isgreaterequal(a,c);
}
