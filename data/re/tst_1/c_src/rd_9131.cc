#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=islessequal(d,c);
a=islessequal(c,b);
d=log10(a);
while(isless(b,a)){
c=pow(d,a);
a=ceil(b);
a=fdim(c,b);
c=islessgreater(d,b);
}
b=fmax(c,a);
c=isless(d,c);
a=cos(d);
}
