#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=pow(a,a);
d=pow(b,a);
while(islessequal(a,c)){
a=islessequal(d,a);
d=atan2(c,b);
}
c=islessgreater(a,a);
d=asin(b);
c=pow(a,a);
d=log10(a);
}
