#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=pow(d,d);
e=fmax(e,c);
e=fdim(a,b);
while(isgreaterequal(e,d)){
c=pow(a,b);
d=isless(e,c);
}
c=isless(b,a);
c=atan(d);
e=log(c);
e=isless(b,b);
}
