#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fmax(c,g);
d=isgreater(c,e);
while(isgreaterequal(f,f)){
d=log(b);
e=cos(d);
}
g=isless(e,e);
c=sin(e);
f=islessequal(b,c);
c=sqrt(e);
b=sin(c);
}
