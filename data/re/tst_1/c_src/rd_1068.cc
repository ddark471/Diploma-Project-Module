#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=sqrt(g);
c=fdim(g,g);
f=exp(d);
while(isgreaterequal(b,a)){
a=pow(g,d);
h=pow(d,b);
c=isgreater(a,h);
c=fmin(c,f);
}
d=isgreater(e,c);
c=floor(b);
b=isgreaterequal(b,c);
g=fmin(d,a);
g=cos(h);
}
