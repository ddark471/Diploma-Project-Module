#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=islessequal(d,c);
f=tan(g);
while(isless(c,a)){
f=islessequal(b,d);
c=fdim(f,e);
c=cos(e);
b=fmax(b,a);
f=cos(c);
}
c=isless(b,g);
d=sin(f);
f=islessequal(b,f);
}
