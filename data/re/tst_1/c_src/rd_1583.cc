#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=fdim(c,d);
a=exp(a);
c=acos(d);
c=pow(b,b);
c=cos(c);
while(islessgreater(b,d)){
a=floor(c);
d=atan(c);
c=pow(a,c);
}
b=atan2(a,b);
c=acos(c);
d=ceil(b);
}
