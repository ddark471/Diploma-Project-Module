#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=pow(f,a);
b=fmin(h,b);
if(islessequal(a,d)){
g=isless(e,e);
b=log10(d);
}
h=ceil(f);
f=isless(b,a);
f=cos(f);
while(isless(d,h)){
c=asin(a);
g=atan(d);
b=fmax(d,d);
b=isless(c,d);
}
f=acos(h);
c=isless(h,a);
c=fmax(f,e);
f=isgreater(c,c);
if(isless(g,b)){
f=isgreaterequal(e,f);
h=exp(c);
c=cos(h);
e=log(f);
}
a=isgreater(g,c);
d=isgreaterequal(h,b);
h=floor(c);
a=isgreaterequal(f,f);
}
