#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=fmin(g,h);
h=fmax(g,h);
if(isless(f,b)){
b=atan2(f,d);
e=cos(d);
h=cos(h);
}
g=islessgreater(a,e);
b=floor(b);
g=cos(a);
h=atan2(b,e);
while(isless(d,g)){
f=atan2(c,a);
a=fmin(f,d);
}
e=islessequal(c,c);
c=islessequal(d,d);
c=isgreaterequal(a,c);
a=pow(c,c);
d=acos(a);
}
