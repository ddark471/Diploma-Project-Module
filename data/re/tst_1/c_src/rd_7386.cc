#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=atan(a);
h=log(d);
if(islessgreater(c,h)){
a=log(c);
f=islessgreater(h,f);
}
b=sqrt(h);
d=islessgreater(f,h);
g=fmin(c,b);
d=fmax(a,a);
while(isless(g,g)){
d=islessequal(d,f);
h=log10(c);
b=islessgreater(c,d);
}
b=sqrt(c);
d=isgreater(c,b);
b=cos(b);
}
