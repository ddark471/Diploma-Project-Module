#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=isgreaterequal(e,c);
d=pow(d,b);
b=fmax(e,b);
while(islessgreater(a,f)){
f=floor(a);
d=ceil(d);
c=isless(d,f);
}
b=ceil(c);
e=sqrt(d);
}
