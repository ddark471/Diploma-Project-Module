#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=ceil(a);
e=atan(f);
c=islessgreater(a,c);
c=fdim(a,e);
if(isless(h,e)){
h=atan2(d,d);
c=sin(a);
g=pow(d,b);
}
e=cos(d);
g=atan2(c,c);
b=fdim(g,b);
b=pow(e,f);
h=fmax(d,b);
while(isgreaterequal(b,b)){
d=islessequal(c,e);
f=log10(f);
}
a=pow(b,b);
d=fdim(c,c);
}
