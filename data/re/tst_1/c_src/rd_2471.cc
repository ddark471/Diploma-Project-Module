#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=acos(e);
g=isless(b,h);
e=log10(g);
while(isgreaterequal(d,f)){
h=isless(b,g);
h=log(a);
e=tan(g);
d=fmax(c,f);
}
d=ceil(d);
g=atan2(d,b);
c=fmin(a,c);
c=fmin(b,c);
d=ceil(c);
}
