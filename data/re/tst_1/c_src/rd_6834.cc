#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=exp(b);
c=pow(a,c);
a=atan2(b,b);
while(isless(b,a)){
d=fdim(a,d);
a=log(d);
d=pow(d,a);
}
c=fdim(a,b);
c=asin(a);
a=fdim(e,e);
}
