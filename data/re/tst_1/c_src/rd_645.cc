#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=atan(c);
d=sin(c);
while(isless(a,a)){
a=atan2(c,a);
a=isless(d,c);
a=islessequal(d,d);
a=asin(b);
b=atan(d);
}
b=islessequal(c,d);
d=fmax(a,a);
a=floor(b);
b=isgreater(d,c);
d=pow(b,b);
}
