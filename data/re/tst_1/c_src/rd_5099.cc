#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=exp(f);
h=isgreaterequal(f,f);
g=asin(g);
while(isless(d,b)){
a=isless(h,g);
d=atan2(h,g);
}
c=log10(d);
a=isless(c,a);
}
