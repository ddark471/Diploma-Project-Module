#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fmax(e,a);
e=atan(f);
while(isgreaterequal(b,c)){
a=floor(c);
c=fdim(c,a);
e=fmax(a,c);
a=pow(a,b);
}
a=fmax(b,e);
f=isgreater(d,a);
}
