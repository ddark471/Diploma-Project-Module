#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=ceil(c);
c=sqrt(g);
c=isless(b,d);
c=exp(d);
c=atan(e);
if(islessequal(d,b)){
d=exp(e);
a=isgreater(e,f);
b=exp(d);
c=atan2(a,b);
d=floor(f);
}
else{
b=sin(a);
c=fmin(g,g);
g=exp(c);
e=fmax(c,a);
}
e=log10(h);
f=isgreater(a,f);
e=exp(f);
b=islessequal(e,b);
while(islessgreater(h,e)){
d=isgreater(d,h);
a=log10(a);
g=atan2(e,b);
}
c=ceil(b);
c=exp(e);
d=pow(c,f);
e=sin(a);
}
