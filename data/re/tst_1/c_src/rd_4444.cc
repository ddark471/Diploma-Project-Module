#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=fmax(b,a);
c=sin(d);
while(islessequal(a,a)){
b=exp(a);
a=cos(b);
}
d=pow(a,d);
b=sqrt(a);
d=fdim(d,d);
d=isless(a,c);
b=fmax(a,d);
}
