#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=floor(d);
d=exp(a);
c=isgreater(b,d);
d=isgreater(c,d);
while(isgreaterequal(b,c)){
d=isgreater(b,c);
a=isgreater(a,b);
}
c=isless(d,d);
d=ceil(b);
d=isgreater(b,b);
c=asin(d);
}
