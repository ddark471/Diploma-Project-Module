#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=cos(d);
e=asin(b);
while(islessequal(a,a)){
d=isless(a,b);
c=ceil(d);
}
a=fmax(d,c);
a=fmin(b,b);
e=fmax(e,c);
if(islessgreater(f,f)){
e=log10(f);
e=fmax(c,e);
a=log10(a);
}
a=tan(a);
d=exp(c);
e=fmax(c,c);
c=sin(c);
d=asin(e);
}
