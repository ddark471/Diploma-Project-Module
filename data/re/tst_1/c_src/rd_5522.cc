#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=pow(c,c);
b=cos(c);
d=tan(d);
d=islessgreater(d,c);
b=isless(c,b);
while(islessgreater(a,b)){
a=fmax(b,b);
b=fmax(d,d);
}
d=fmin(a,h);
a=sqrt(a);
d=isgreaterequal(c,d);
if(isgreaterequal(b,c)){
e=log10(a);
c=fdim(d,c);
e=atan2(c,b);
a=atan2(c,a);
h=exp(f);
}
c=log(f);
h=pow(c,h);
h=floor(b);
f=islessequal(a,e);
}
