#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=atan(g);
g=atan(g);
h=isgreaterequal(e,g);
while(islessequal(g,e)){
a=fmin(f,a);
g=isgreaterequal(b,h);
h=cos(d);
}
d=floor(d);
g=ceil(d);
e=isgreaterequal(d,g);
d=fmin(g,f);
g=isgreater(c,a);
}
