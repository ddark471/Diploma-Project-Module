#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=fmax(f,c);
c=fmax(g,b);
e=isgreater(c,f);
while(islessgreater(f,g)){
c=isgreater(d,f);
e=fdim(a,a);
d=exp(f);
}
c=isgreater(f,c);
c=asin(g);
g=sqrt(b);
}
