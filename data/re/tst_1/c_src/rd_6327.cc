#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=log(h);
g=pow(d,f);
f=isless(b,f);
h=isless(b,h);
f=islessequal(h,h);
while(isless(g,b)){
g=ceil(h);
a=pow(e,b);
h=isgreaterequal(a,b);
}
c=sqrt(c);
a=atan(b);
c=fmax(b,d);
if(islessequal(a,a)){
c=islessgreater(a,b);
a=log(b);
d=sqrt(a);
}
b=log10(a);
b=acos(b);
b=isless(a,c);
b=atan2(c,a);
}
