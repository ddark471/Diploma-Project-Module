#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fmin(c,c);
e=fmax(e,d);
c=cos(e);
while(islessgreater(c,a)){
f=islessequal(a,a);
b=sin(d);
}
d=cos(c);
a=pow(d,f);
e=acos(g);
c=ceil(a);
a=cos(a);
if(islessequal(b,g)){
c=tan(c);
f=islessgreater(c,c);
h=exp(d);
d=isless(h,c);
}
else{
c=pow(a,h);
d=asin(g);
h=isgreaterequal(b,e);
}
h=asin(b);
d=sqrt(h);
h=isgreater(g,c);
c=sin(e);
b=fmax(e,b);
}
