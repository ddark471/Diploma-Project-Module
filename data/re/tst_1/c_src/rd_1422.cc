#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=cos(d);
a=fdim(d,b);
while(isless(b,d)){
a=acos(b);
c=fmax(b,b);
c=log10(c);
c=sin(d);
c=floor(b);
}
d=sin(a);
d=fmax(c,c);
}
