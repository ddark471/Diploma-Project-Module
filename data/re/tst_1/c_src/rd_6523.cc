#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=fmin(a,c);
g=atan(d);
d=exp(g);
while(isless(c,b)){
c=ceil(g);
c=pow(b,c);
g=pow(b,a);
g=fdim(a,g);
}
f=islessgreater(g,e);
e=fmax(f,f);
c=log(d);
g=atan(b);
}
