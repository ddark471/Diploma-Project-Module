#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=atan(e);
b=fmax(e,c);
d=floor(d);
while(isless(c,a)){
b=exp(d);
c=tan(c);
e=log(a);
c=fmin(d,a);
}
a=sqrt(a);
d=isgreaterequal(a,b);
}
