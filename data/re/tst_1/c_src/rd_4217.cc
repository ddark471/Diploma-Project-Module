#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=log(g);
g=fdim(g,b);
h=isgreater(h,c);
c=isless(c,f);
while(isless(a,e)){
h=islessgreater(c,f);
g=fmax(h,g);
a=cos(h);
}
d=fmax(g,h);
c=cos(b);
}
