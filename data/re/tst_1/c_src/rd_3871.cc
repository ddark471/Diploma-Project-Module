#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=fmax(e,b);
e=atan(g);
e=isless(b,d);
e=floor(a);
a=log10(h);
while(isless(h,c)){
e=asin(c);
b=sqrt(h);
}
d=exp(c);
b=cos(e);
e=ceil(g);
}
