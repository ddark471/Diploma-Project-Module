#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=islessequal(d,c);
b=islessgreater(b,d);
while(islessgreater(a,b)){
c=exp(d);
c=cos(d);
a=cos(c);
}
b=isgreater(c,c);
c=islessgreater(c,b);
a=islessequal(c,c);
a=fmin(b,b);
}
