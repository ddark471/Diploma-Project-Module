#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=cos(b);
d=asin(b);
d=pow(d,c);
while(isless(d,a)){
c=fmax(d,b);
b=fmax(d,a);
}
a=isgreater(a,a);
b=exp(a);
d=islessgreater(d,a);
}
