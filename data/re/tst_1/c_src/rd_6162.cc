#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=isless(c,f);
c=sqrt(g);
e=islessequal(c,d);
f=atan2(b,a);
while(isless(a,g)){
e=floor(b);
b=log10(c);
c=log(c);
}
b=isgreater(a,c);
c=cos(b);
f=log(c);
d=fmax(e,b);
d=islessequal(b,e);
if(islessgreater(d,f)){
f=isgreaterequal(b,a);
d=isgreaterequal(d,f);
}
a=exp(a);
f=pow(a,b);
c=ceil(b);
}
