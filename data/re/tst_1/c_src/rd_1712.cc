#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=sin(d);
a=cos(b);
b=fmax(b,a);
if(isless(a,d)){
a=pow(d,d);
c=asin(a);
}
c=isgreaterequal(a,a);
d=fdim(c,a);
while(isgreaterequal(c,e)){
b=fmax(b,c);
e=atan(d);
}
c=islessgreater(g,h);
f=isless(h,b);
b=exp(b);
c=log(h);
f=log(e);
if(islessgreater(f,f)){
e=fmin(b,f);
c=isless(f,g);
}
else{
g=sin(a);
c=isless(f,g);
}
g=tan(a);
b=fdim(h,b);
h=islessequal(a,c);
g=fmin(e,g);
}
