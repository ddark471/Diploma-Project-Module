#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=ceil(c);
d=isgreater(b,d);
e=islessequal(e,a);
b=ceil(g);
d=islessgreater(h,d);
while(islessequal(f,b)){
g=pow(a,e);
d=cos(a);
f=exp(g);
}
b=isgreater(f,a);
b=tan(b);
c=islessgreater(a,g);
h=log(g);
}
