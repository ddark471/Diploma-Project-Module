#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=isless(g,d);
d=isgreater(b,a);
c=isgreaterequal(e,g);
while(islessgreater(a,g)){
a=log(b);
c=fmax(b,c);
}
d=pow(d,g);
d=tan(e);
f=floor(c);
e=atan(d);
a=isgreaterequal(g,g);
}
