#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=atan(b);
f=log(f);
if(isless(g,c)){
b=ceil(a);
c=atan2(e,g);
e=islessequal(e,f);
}
else{
g=islessequal(b,c);
f=islessgreater(c,c);
c=log10(f);
}
g=log(c);
c=log(b);
b=fmax(g,g);
a=acos(g);
while(islessgreater(d,f)){
g=fmax(a,d);
e=asin(a);
}
f=atan(f);
d=isgreater(e,b);
b=isless(g,a);
c=cos(b);
}
