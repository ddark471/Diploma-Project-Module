#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fdim(a,e);
c=fdim(c,c);
while(isless(c,e)){
a=pow(d,b);
b=fmax(e,a);
}
d=fmin(a,a);
b=ceil(a);
b=islessequal(e,b);
}
