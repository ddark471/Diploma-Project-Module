#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=isgreaterequal(a,a);
c=atan2(b,b);
c=atan(a);
c=cos(b);
a=cos(b);
while(isgreaterequal(d,a)){
a=pow(a,c);
a=cos(b);
d=islessgreater(b,b);
}
b=exp(b);
b=pow(b,b);
}
