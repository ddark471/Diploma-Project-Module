#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=asin(f);
a=fmax(h,c);
while(islessgreater(e,g)){
a=acos(f);
d=atan2(e,a);
b=fmin(e,a);
a=acos(f);
c=isless(c,a);
}
e=atan(b);
c=islessequal(a,c);
b=fmax(d,d);
g=fmax(b,e);
}
