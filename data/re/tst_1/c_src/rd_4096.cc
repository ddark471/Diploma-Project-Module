#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=cos(c);
c=isgreater(d,b);
d=tan(d);
c=sin(e);
while(isless(c,e)){
c=islessequal(d,d);
d=isless(a,c);
}
d=fmax(e,a);
d=sin(b);
}
