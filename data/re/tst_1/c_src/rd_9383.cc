#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=atan(c);
g=atan2(b,a);
e=cos(f);
while(isless(f,a)){
h=cos(b);
f=log(c);
h=fmax(c,h);
d=exp(c);
}
f=islessequal(e,c);
e=pow(d,g);
b=pow(c,f);
f=fmax(c,f);
}
