#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=exp(d);
b=fdim(g,d);
f=islessgreater(b,c);
c=fmin(b,c);
while(islessgreater(a,e)){
g=floor(f);
a=isgreater(d,a);
}
c=log10(d);
d=fdim(c,e);
}
