#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=isgreaterequal(e,d);
h=fmin(f,a);
b=fmin(e,f);
while(islessequal(a,f)){
b=isless(c,g);
c=log10(h);
h=islessequal(h,f);
b=floor(c);
a=log10(a);
}
e=ceil(h);
d=isgreaterequal(c,b);
}
