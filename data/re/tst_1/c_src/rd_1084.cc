#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=atan2(e,f);
b=islessequal(e,e);
if(isless(a,d)){
c=log10(f);
f=tan(c);
f=fmax(e,c);
d=cos(f);
}
d=fmin(c,f);
e=atan(f);
f=sqrt(b);
f=islessgreater(e,e);
d=atan(b);
while(islessgreater(b,e)){
d=fmax(e,d);
b=exp(a);
a=fdim(b,a);
}
c=asin(e);
a=fmin(a,d);
if(islessgreater(e,c)){
b=atan(e);
a=islessgreater(a,c);
c=asin(c);
}
c=isless(e,b);
c=log(c);
}
