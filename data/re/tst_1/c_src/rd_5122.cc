#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=isless(a,b);
c=ceil(c);
d=fmin(a,c);
while(isgreaterequal(c,b)){
d=acos(a);
a=sqrt(d);
}
b=isgreaterequal(d,b);
d=isgreater(b,b);
b=isgreater(d,c);
d=atan(c);
}
