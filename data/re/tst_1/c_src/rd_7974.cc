#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=log10(e);
d=atan(c);
while(islessgreater(g,h)){
f=fmin(e,g);
h=sqrt(a);
g=fmin(d,a);
h=fmax(c,e);
}
c=cos(g);
b=log10(f);
}
