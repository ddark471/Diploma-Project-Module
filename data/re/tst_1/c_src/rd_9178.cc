#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=isless(f,f);
f=isless(g,a);
f=pow(g,d);
while(isgreaterequal(d,c)){
g=islessequal(b,e);
d=isless(f,d);
c=atan2(d,e);
d=pow(g,g);
}
a=log(b);
a=ceil(c);
c=floor(f);
e=floor(d);
f=fmax(d,b);
}
