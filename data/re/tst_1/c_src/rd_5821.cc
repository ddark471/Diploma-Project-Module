#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=asin(f);
h=sin(f);
h=fmax(a,c);
a=isgreater(c,b);
while(islessequal(e,a)){
d=islessgreater(h,e);
b=fdim(f,h);
e=sqrt(d);
b=sin(d);
f=atan2(f,h);
}
d=floor(c);
e=fmax(e,c);
}
