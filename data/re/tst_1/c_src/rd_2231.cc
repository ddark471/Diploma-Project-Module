#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=islessequal(c,f);
f=isgreaterequal(e,a);
f=isgreater(e,b);
while(islessgreater(b,f)){
c=atan(f);
g=fmin(f,g);
e=fmax(h,c);
c=isgreaterequal(d,g);
}
d=fmin(a,c);
a=pow(e,d);
e=fmin(c,c);
b=pow(b,f);
a=isgreater(e,b);
}
