#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=asin(d);
c=log(a);
a=isgreater(b,c);
while(islessgreater(a,a)){
c=islessgreater(d,b);
d=fmax(d,c);
}
c=isgreaterequal(b,c);
c=fmin(c,d);
c=exp(b);
c=isgreaterequal(d,d);
}
