#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=ceil(d);
a=pow(f,e);
while(isgreaterequal(d,a)){
b=fmin(c,g);
c=atan(f);
g=exp(f);
}
g=isgreaterequal(c,b);
f=exp(b);
b=atan2(f,g);
}
