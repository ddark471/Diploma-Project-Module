#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=acos(d);
b=ceil(c);
d=tan(d);
d=atan2(e,d);
while(islessequal(c,e)){
d=fdim(b,a);
a=isgreater(e,b);
}
c=sqrt(a);
d=fmax(d,b);
}
