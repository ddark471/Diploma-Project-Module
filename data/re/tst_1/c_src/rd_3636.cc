#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=islessequal(f,c);
f=pow(a,b);
g=fmax(f,h);
g=isless(b,a);
if(islessgreater(h,c)){
d=fmax(h,e);
a=asin(c);
d=isless(e,f);
d=isgreater(e,e);
h=pow(g,c);
}
else{
c=fmin(g,b);
c=fmax(e,c);
}
g=fmin(c,b);
g=fmax(f,f);
d=sqrt(b);
e=atan2(b,a);
h=fdim(a,h);
while(isgreaterequal(h,d)){
b=log10(c);
e=atan(f);
c=fmax(a,d);
g=atan(c);
}
c=pow(g,c);
a=sqrt(c);
e=ceil(g);
e=fmax(g,b);
a=islessequal(a,e);
}
