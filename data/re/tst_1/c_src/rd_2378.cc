#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=log10(c);
d=fmax(e,c);
while(isgreaterequal(a,a)){
a=atan2(a,d);
e=floor(c);
e=isgreater(a,d);
}
d=isgreater(a,a);
d=isless(a,b);
c=isless(f,b);
}
