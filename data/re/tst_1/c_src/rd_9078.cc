#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=atan2(d,d);
a=log(c);
while(isgreaterequal(a,a)){
a=pow(c,b);
c=isgreaterequal(a,b);
}
c=sqrt(d);
d=tan(a);
c=fmin(d,c);
}
