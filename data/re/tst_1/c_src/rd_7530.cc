#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=isless(a,a);
f=exp(g);
if(isless(e,c)){
e=ceil(c);
f=sqrt(d);
}
else{
e=tan(b);
e=log(a);
}
c=isgreater(c,c);
a=pow(c,d);
d=isless(f,c);
c=isless(d,a);
a=tan(g);
while(islessgreater(b,f)){
g=tan(g);
e=isgreater(b,a);
}
b=sqrt(b);
a=isgreaterequal(b,f);
}
