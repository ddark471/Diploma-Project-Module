#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=islessequal(f,a);
c=fmax(f,c);
d=islessgreater(f,h);
while(islessequal(a,b)){
a=isless(f,d);
c=islessequal(g,h);
h=islessequal(d,f);
}
g=islessgreater(c,b);
c=pow(f,h);
b=islessequal(c,e);
b=sin(h);
d=pow(e,g);
}
