#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=atan(d);
d=islessgreater(b,a);
while(islessgreater(b,c)){
b=exp(b);
a=exp(c);
d=islessequal(a,b);
}
d=fmax(c,b);
a=log(a);
c=exp(b);
d=pow(c,d);
}
