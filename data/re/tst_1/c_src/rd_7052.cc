#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=atan(c);
d=atan(a);
while(isgreaterequal(b,d)){
c=atan2(d,b);
c=fdim(b,c);
d=pow(b,c);
d=isless(d,b);
}
b=ceil(d);
c=fmax(c,c);
b=islessgreater(b,b);
}
