#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fmax(b,a);
a=exp(d);
d=islessequal(b,a);
while(islessgreater(c,a)){
b=log(b);
b=fmax(d,d);
d=log(d);
d=exp(d);
a=log(d);
}
c=pow(b,c);
b=isgreaterequal(a,g);
}
