#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=atan2(f,c);
f=ceil(c);
g=tan(c);
while(isless(d,g)){
g=isgreaterequal(b,d);
g=log(f);
e=isgreater(e,d);
}
f=exp(f);
b=tan(f);
g=sqrt(f);
a=atan(b);
}
