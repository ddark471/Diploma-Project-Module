#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fmax(b,d);
d=asin(d);
while(isless(c,b)){
d=atan(b);
d=fmax(b,d);
c=islessgreater(d,d);
}
d=sqrt(a);
a=islessequal(d,e);
a=islessgreater(b,a);
if(isgreaterequal(b,b)){
c=cos(b);
c=sin(a);
e=fmin(b,c);
}
b=isgreater(e,b);
e=atan(a);
b=pow(b,d);
d=pow(a,a);
}
