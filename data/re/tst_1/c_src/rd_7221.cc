#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=exp(e);
h=fmin(a,c);
a=tan(b);
h=log10(e);
while(islessgreater(a,c)){
a=isless(h,g);
h=islessgreater(e,a);
a=exp(g);
e=fdim(f,a);
}
f=floor(c);
d=ceil(g);
}
