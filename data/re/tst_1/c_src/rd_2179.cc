#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fmax(c,a);
f=pow(e,f);
while(islessequal(a,c)){
b=islessgreater(e,a);
h=isless(g,g);
a=sqrt(b);
}
e=tan(g);
b=isgreater(b,f);
}
