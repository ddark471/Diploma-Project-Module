#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=sin(e);
c=cos(c);
while(isless(a,b)){
a=fmin(d,e);
b=tan(d);
d=cos(e);
}
d=isless(d,b);
c=isgreaterequal(c,b);
b=pow(a,d);
if(islessgreater(a,d)){
c=pow(c,c);
d=atan(c);
d=isgreater(c,c);
}
else{
a=atan2(c,c);
d=sqrt(d);
d=fmin(c,b);
}
d=exp(c);
a=isless(c,a);
a=isless(b,d);
}
