#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=atan(e);
d=exp(a);
f=atan(f);
e=fmax(c,b);
while(isless(d,b)){
e=log(f);
b=fmax(b,c);
f=isgreater(a,b);
}
a=sin(e);
d=fdim(a,d);
d=log(b);
d=isgreaterequal(f,c);
}
