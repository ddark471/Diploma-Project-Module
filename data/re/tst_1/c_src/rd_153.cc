#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=log10(g);
g=atan2(f,e);
c=isgreaterequal(c,e);
g=isless(g,b);
while(islessgreater(e,d)){
c=islessequal(e,f);
e=atan2(g,d);
e=pow(d,h);
}
g=atan(g);
h=ceil(c);
e=log10(g);
}
