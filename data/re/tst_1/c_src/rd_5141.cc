#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=exp(f);
a=fmax(e,e);
g=log(b);
while(isless(b,f)){
a=isgreaterequal(h,a);
c=pow(c,h);
g=floor(g);
}
g=exp(e);
a=acos(b);
g=fmax(a,g);
e=tan(e);
a=log10(e);
}
