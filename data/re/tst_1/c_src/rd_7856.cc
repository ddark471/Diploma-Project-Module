#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=exp(d);
d=atan2(d,d);
a=ceil(b);
c=isgreater(d,c);
b=exp(d);
while(islessgreater(d,c)){
d=ceil(c);
c=islessgreater(d,d);
d=sqrt(d);
a=fmax(a,d);
}
c=log(a);
a=exp(a);
}
