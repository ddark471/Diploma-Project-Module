#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=exp(f);
c=isless(a,e);
while(isless(e,b)){
b=isgreater(a,f);
b=fmin(a,c);
}
c=floor(a);
c=isless(c,b);
c=log10(b);
}
