#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=tan(d);
f=sin(f);
while(islessequal(c,c)){
c=acos(c);
d=floor(c);
}
f=pow(e,b);
c=exp(c);
e=exp(c);
b=exp(d);
f=isless(c,d);
}
