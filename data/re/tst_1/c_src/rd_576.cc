#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=atan(a);
g=log10(c);
f=fmax(d,b);
while(islessequal(e,c)){
f=islessequal(a,h);
c=sin(d);
g=exp(h);
c=fdim(d,f);
a=isgreater(g,g);
}
c=isless(c,b);
b=islessgreater(d,d);
b=fdim(a,c);
a=isgreaterequal(b,h);
h=exp(c);
}
