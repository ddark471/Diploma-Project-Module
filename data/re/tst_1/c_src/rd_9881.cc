#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=islessgreater(a,a);
d=isgreaterequal(b,a);
b=islessgreater(a,a);
d=islessequal(d,a);
while(isless(d,b)){
c=isless(d,c);
b=fmax(b,b);
c=islessgreater(c,a);
a=atan(d);
b=fmax(a,b);
}
d=atan2(a,a);
b=isless(a,a);
d=ceil(d);
}
