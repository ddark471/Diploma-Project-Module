#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=fmin(c,e);
d=log(a);
if(isless(e,a)){
e=ceil(d);
g=exp(b);
c=atan(f);
e=sin(b);
}
e=sqrt(a);
c=floor(f);
a=floor(h);
while(isgreaterequal(g,d)){
e=islessgreater(a,f);
h=sin(h);
}
d=isgreaterequal(d,d);
a=asin(a);
}
