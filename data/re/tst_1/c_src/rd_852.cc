#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=log10(e);
h=atan(c);
h=islessequal(a,b);
while(isgreaterequal(a,b)){
h=floor(a);
e=atan2(e,c);
d=fmax(d,a);
}
b=islessequal(h,a);
b=atan2(f,h);
c=tan(d);
}
