#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fmin(c,c);
d=sqrt(c);
while(isgreaterequal(a,d)){
b=islessgreater(d,b);
a=atan2(b,b);
a=isgreater(b,a);
b=pow(d,a);
c=fmax(a,b);
}
d=isgreaterequal(a,a);
d=floor(c);
b=ceil(d);
b=pow(a,c);
d=islessgreater(d,b);
}
