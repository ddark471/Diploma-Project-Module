#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=log(c);
c=ceil(f);
c=cos(c);
h=exp(d);
while(isgreaterequal(g,g)){
a=ceil(d);
f=ceil(a);
}
c=isgreater(b,c);
c=floor(b);
g=isgreater(h,h);
d=isgreater(a,a);
g=atan2(h,f);
}
