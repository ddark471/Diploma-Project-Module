#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=isless(h,g);
g=fmax(g,d);
f=pow(c,b);
e=fmin(d,h);
b=islessequal(b,a);
while(islessgreater(g,f)){
g=islessequal(b,b);
h=fmax(e,f);
d=isless(f,e);
a=pow(g,f);
}
b=log10(e);
f=islessgreater(e,a);
f=exp(b);
b=sin(b);
}
