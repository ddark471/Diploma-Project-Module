#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=fmax(c,d);
c=isless(h,c);
d=fmax(e,g);
while(isless(a,g)){
d=exp(f);
f=asin(a);
a=isgreater(e,g);
d=acos(g);
h=fdim(d,g);
}
e=sqrt(e);
g=fmax(a,d);
}
