#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=asin(a);
g=exp(a);
while(islessgreater(c,e)){
f=pow(g,c);
b=log10(g);
}
e=fmax(b,e);
c=exp(b);
g=isgreater(a,h);
c=islessequal(e,f);
g=fmin(d,d);
}
