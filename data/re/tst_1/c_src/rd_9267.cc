#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=pow(b,a);
d=exp(c);
while(isless(b,b)){
c=sqrt(a);
d=fdim(a,a);
b=islessgreater(a,b);
a=pow(d,a);
a=cos(c);
}
c=sqrt(c);
a=atan2(d,a);
}
