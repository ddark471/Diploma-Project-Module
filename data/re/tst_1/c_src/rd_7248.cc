#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=fdim(c,f);
d=islessequal(a,b);
e=fmin(b,a);
while(isless(h,h)){
h=tan(e);
c=fdim(c,e);
e=sin(h);
}
g=isgreater(h,g);
h=sin(b);
e=fmax(e,a);
c=tan(a);
}
