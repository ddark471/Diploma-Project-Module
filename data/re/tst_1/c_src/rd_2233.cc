#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=isless(d,c);
d=isless(d,d);
b=floor(a);
d=isless(d,d);
c=pow(c,c);
while(isless(c,c)){
c=isless(d,b);
c=isgreater(d,b);
a=exp(b);
c=tan(d);
}
c=log(b);
a=log(c);
a=islessequal(a,d);
c=log(c);
b=pow(b,a);
}
