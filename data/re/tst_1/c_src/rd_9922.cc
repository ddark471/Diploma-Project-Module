#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=pow(b,d);
d=isgreater(e,c);
while(isless(b,c)){
e=log10(a);
a=fmax(c,e);
a=cos(d);
e=atan(f);
a=fdim(a,e);
}
e=ceil(f);
c=fmin(c,e);
}
