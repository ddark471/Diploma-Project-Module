#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=pow(a,a);
b=cos(b);
c=fmax(d,d);
c=tan(d);
e=isgreaterequal(e,g);
while(islessequal(g,a)){
g=isless(a,g);
b=islessgreater(c,f);
g=isgreaterequal(g,b);
d=exp(c);
}
g=exp(c);
d=fmin(g,e);
}
