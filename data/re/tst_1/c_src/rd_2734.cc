#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=floor(c);
d=pow(a,a);
while(isgreaterequal(a,a)){
b=pow(a,a);
a=islessequal(a,b);
a=isgreater(d,b);
a=tan(b);
}
b=ceil(a);
a=atan(b);
c=ceil(d);
}
