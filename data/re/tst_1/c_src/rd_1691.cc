#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=log(g);
f=pow(b,c);
while(islessgreater(d,h)){
f=isless(b,c);
h=fdim(c,c);
d=ceil(h);
a=floor(d);
}
h=islessequal(c,a);
e=isgreaterequal(a,f);
e=cos(h);
g=log10(f);
}
