#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=cos(b);
b=isless(d,d);
d=fdim(d,d);
while(islessgreater(a,a)){
d=atan2(a,c);
b=fmax(d,c);
c=sin(b);
a=sin(d);
d=sin(d);
}
b=exp(d);
a=fmax(c,d);
d=ceil(c);
}
