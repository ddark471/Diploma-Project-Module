#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=isless(a,d);
g=isless(d,f);
while(isless(g,b)){
d=islessgreater(c,g);
g=log(h);
}
d=islessgreater(g,h);
c=isgreater(h,g);
b=log(g);
f=floor(c);
d=fdim(h,b);
}
