#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=atan(b);
e=isgreaterequal(b,h);
e=asin(g);
f=floor(d);
while(isgreaterequal(f,f)){
d=fmin(f,b);
a=fmax(e,h);
}
f=floor(g);
c=isgreaterequal(e,f);
b=ceil(g);
e=fdim(h,f);
if(islessequal(c,a)){
a=atan(b);
h=log(f);
d=atan2(g,c);
c=sqrt(b);
}
g=sin(c);
h=pow(f,g);
g=log(f);
}
