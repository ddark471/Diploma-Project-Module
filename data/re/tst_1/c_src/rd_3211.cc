#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=isless(d,d);
f=isless(b,b);
while(islessgreater(b,c)){
b=fmin(f,e);
e=fmin(c,e);
b=asin(d);
}
b=atan2(g,d);
g=floor(f);
if(isless(f,g)){
a=islessgreater(g,a);
b=floor(f);
f=exp(f);
c=floor(c);
}
b=floor(a);
g=isgreater(g,f);
a=fmax(a,a);
}
