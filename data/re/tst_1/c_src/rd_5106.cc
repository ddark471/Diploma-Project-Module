#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=atan(c);
d=isgreaterequal(d,b);
d=tan(b);
while(isless(b,a)){
d=ceil(c);
c=fmin(a,d);
}
b=cos(d);
a=pow(d,b);
c=isless(a,a);
c=isgreater(a,d);
}
