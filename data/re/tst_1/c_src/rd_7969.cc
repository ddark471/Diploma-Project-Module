#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=islessgreater(f,d);
h=fdim(c,h);
f=fmin(a,f);
f=islessequal(b,g);
d=log10(c);
while(islessequal(f,a)){
f=fmin(g,b);
f=fmax(h,c);
c=isless(f,g);
}
a=atan2(c,b);
f=cos(b);
c=isless(a,f);
h=fdim(h,h);
}
