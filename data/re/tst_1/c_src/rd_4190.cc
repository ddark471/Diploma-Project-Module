#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=tan(b);
b=pow(b,a);
while(isless(c,b)){
b=exp(a);
c=isgreater(d,a);
d=floor(b);
a=cos(c);
b=islessequal(d,d);
}
d=atan2(c,a);
d=log(a);
a=cos(a);
c=fmin(d,b);
d=isgreater(a,b);
}
