#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=cos(c);
c=isgreater(f,a);
a=fmax(a,b);
e=islessgreater(d,e);
d=islessequal(f,d);
while(islessgreater(b,b)){
d=fmax(d,b);
d=isgreater(e,d);
c=sqrt(e);
f=islessgreater(d,a);
}
a=atan(f);
d=exp(c);
h=fmin(c,h);
if(isgreaterequal(b,c)){
b=fmin(e,d);
c=fmin(c,f);
g=fmax(b,h);
e=log(c);
}
d=fmin(c,c);
b=cos(a);
a=isgreater(b,c);
g=cos(d);
}
