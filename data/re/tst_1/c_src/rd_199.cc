#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=log10(b);
c=floor(b);
d=floor(a);
a=islessequal(d,d);
d=cos(d);
while(islessequal(a,b)){
d=sin(b);
d=fmax(c,c);
d=pow(a,b);
}
c=fmax(c,a);
a=sqrt(c);
c=tan(a);
b=isgreaterequal(a,c);
b=fmax(d,c);
}
