#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=islessgreater(c,b);
a=fmax(c,d);
a=atan(b);
a=exp(d);
while(islessgreater(b,a)){
b=pow(a,b);
d=pow(b,b);
c=cos(b);
}
a=fmin(a,b);
c=fdim(a,d);
}
