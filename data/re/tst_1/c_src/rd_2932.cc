#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fdim(d,a);
b=floor(d);
if(isless(d,b)){
d=exp(b);
b=atan2(b,b);
c=log(d);
c=islessgreater(c,a);
}
c=log10(b);
d=fmin(d,b);
while(isgreaterequal(b,b)){
d=sqrt(c);
b=floor(c);
d=isless(c,d);
}
b=isless(c,a);
b=atan(a);
d=islessequal(d,c);
a=islessequal(a,c);
if(islessequal(b,b)){
c=ceil(d);
c=sqrt(d);
c=islessequal(c,c);
}
a=isgreaterequal(d,a);
c=fmin(b,c);
a=fmin(c,b);
c=islessequal(b,c);
}
