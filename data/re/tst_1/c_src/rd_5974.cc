#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=ceil(b);
c=exp(d);
while(isgreaterequal(f,c)){
e=pow(e,f);
f=cos(f);
a=sin(d);
}
c=islessgreater(f,f);
g=ceil(c);
d=log(c);
if(islessgreater(e,f)){
d=asin(f);
c=cos(g);
g=exp(e);
}
else{
a=asin(b);
b=isgreater(g,e);
}
e=pow(d,b);
g=pow(b,b);
c=isless(d,d);
}
