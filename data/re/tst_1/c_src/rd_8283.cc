#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=asin(b);
d=sqrt(f);
c=floor(a);
while(isgreaterequal(f,d)){
f=fdim(f,a);
f=isgreater(b,d);
d=fmax(e,c);
d=atan2(c,c);
d=islessgreater(e,a);
}
h=isgreater(c,d);
g=pow(g,a);
b=isless(b,a);
d=floor(c);
d=pow(h,d);
if(isgreaterequal(h,c)){
g=fdim(c,f);
d=islessgreater(f,g);
c=isgreater(c,e);
c=sqrt(d);
}
d=isgreater(e,d);
e=isless(d,c);
a=isless(g,b);
}
