#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=atan2(f,a);
b=pow(a,e);
c=fmin(h,e);
while(isgreaterequal(c,c)){
d=isgreaterequal(b,a);
c=fmin(h,d);
h=pow(h,f);
}
a=tan(d);
c=islessequal(d,d);
a=atan2(c,a);
if(islessgreater(a,d)){
b=acos(c);
d=isless(a,d);
d=islessequal(b,c);
b=acos(b);
d=atan2(b,b);
}
else{
c=fmin(c,a);
a=log(c);
}
b=islessequal(d,b);
d=fmin(c,c);
c=pow(a,b);
a=isgreater(d,c);
a=islessequal(c,c);
}
