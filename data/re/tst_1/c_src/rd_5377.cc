#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=exp(f);
f=log(e);
while(islessgreater(f,e)){
a=sqrt(d);
a=log10(f);
}
d=log(d);
d=islessequal(e,d);
f=fmax(e,b);
f=cos(e);
}
