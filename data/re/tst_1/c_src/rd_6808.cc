#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=atan2(c,c);
h=sqrt(d);
d=asin(c);
c=pow(f,f);
while(isgreaterequal(d,g)){
g=log10(h);
a=pow(a,a);
a=fmax(d,e);
a=log(b);
}
e=isless(g,c);
c=pow(a,e);
}
