#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=isgreaterequal(b,a);
c=floor(a);
while(islessequal(c,a)){
d=sqrt(b);
b=cos(c);
}
a=fmax(a,c);
a=asin(b);
}
