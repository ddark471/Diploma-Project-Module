#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=isgreater(a,b);
b=isless(c,a);
while(islessgreater(b,d)){
c=fdim(b,a);
a=tan(c);
}
d=isgreater(b,d);
c=fdim(c,b);
c=islessgreater(d,b);
c=atan(c);
}
