#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=ceil(f);
e=ceil(c);
while(isless(c,c)){
f=pow(e,e);
e=fdim(f,c);
c=isless(g,b);
}
g=fmin(h,e);
d=fmin(c,f);
f=pow(a,b);
g=acos(a);
e=sqrt(e);
if(isgreaterequal(h,h)){
b=fmin(d,f);
f=isless(g,a);
d=sin(b);
b=pow(b,d);
g=isless(b,f);
}
else{
d=sin(e);
d=exp(h);
}
d=pow(c,f);
a=exp(b);
f=fmax(g,f);
}
