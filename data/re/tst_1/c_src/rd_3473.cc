#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=fmax(f,f);
d=islessequal(c,c);
while(islessequal(b,e)){
f=asin(d);
d=log(d);
a=pow(a,d);
d=fmin(f,d);
c=isless(f,d);
}
a=fmax(e,d);
e=floor(d);
if(islessgreater(b,b)){
b=isgreater(b,f);
c=log10(c);
e=isgreater(e,a);
d=log(a);
}
c=fmax(d,f);
f=atan(c);
e=cos(f);
d=isless(e,e);
}
