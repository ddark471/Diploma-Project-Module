#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=atan(f);
e=atan(g);
f=exp(c);
e=fmax(f,b);
b=asin(e);
while(islessequal(a,e)){
a=isless(g,a);
a=cos(f);
}
f=islessequal(a,g);
g=sin(b);
g=log(b);
f=isgreater(f,c);
f=islessequal(e,f);
}
