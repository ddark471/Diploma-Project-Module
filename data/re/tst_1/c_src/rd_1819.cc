#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=cos(c);
a=atan2(c,a);
while(isless(d,a)){
c=pow(c,d);
d=log10(c);
d=sqrt(b);
}
b=isless(b,b);
c=cos(b);
c=ceil(d);
c=islessequal(d,d);
}
