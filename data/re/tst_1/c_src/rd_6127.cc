#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=atan(d);
d=islessequal(c,b);
c=fmax(c,e);
a=fmax(a,d);
d=isgreater(e,d);
while(islessequal(c,b)){
e=fmax(a,d);
a=pow(b,a);
c=sin(d);
c=sin(c);
e=sin(c);
}
c=exp(c);
c=fdim(c,a);
c=log(c);
a=sin(c);
b=pow(c,a);
if(isgreaterequal(b,b)){
d=isless(d,c);
c=fmax(d,b);
}
d=floor(c);
c=islessequal(b,b);
b=islessequal(c,c);
d=log(a);
a=cos(c);
}
