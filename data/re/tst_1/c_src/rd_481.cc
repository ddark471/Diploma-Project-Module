#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=pow(f,e);
f=fmax(a,h);
d=exp(a);
g=fmin(e,a);
while(isless(h,g)){
b=log(e);
h=cos(g);
g=isgreater(f,d);
g=acos(b);
f=sqrt(c);
}
f=isgreater(b,c);
a=log(d);
}
