#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=pow(b,b);
d=ceil(b);
a=floor(b);
while(islessequal(d,c)){
d=atan2(d,b);
b=islessgreater(c,d);
a=acos(c);
a=atan2(a,d);
d=sin(a);
}
d=exp(a);
d=fmin(a,b);
b=islessequal(c,a);
a=pow(c,d);
a=fmax(d,d);
}
