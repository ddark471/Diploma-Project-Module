#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fmax(d,b);
d=log10(d);
while(isgreaterequal(a,a)){
a=fmin(a,d);
a=isgreater(d,b);
d=fmax(b,a);
b=isgreaterequal(d,a);
}
g=islessgreater(f,e);
g=exp(c);
if(isless(d,f)){
f=isgreater(g,b);
b=tan(g);
h=cos(e);
}
else{
h=atan2(h,g);
c=fmin(f,b);
c=fdim(b,d);
}
c=fmin(f,g);
f=log(b);
}
