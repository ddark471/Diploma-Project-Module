#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=sin(c);
f=fmax(d,f);
g=pow(a,c);
while(islessequal(e,c)){
e=islessgreater(f,e);
f=exp(g);
g=islessequal(b,g);
}
c=islessgreater(c,e);
b=log10(b);
c=exp(b);
}
