#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=islessgreater(b,d);
d=isgreater(a,d);
while(isgreaterequal(a,b)){
d=exp(b);
d=acos(c);
a=atan2(b,c);
d=isgreaterequal(d,c);
}
c=floor(c);
a=fmin(d,a);
d=atan(c);
}
