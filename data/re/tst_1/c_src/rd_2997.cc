#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=asin(d);
h=atan2(b,c);
g=fmax(a,g);
f=log10(b);
while(islessequal(b,c)){
e=islessequal(b,g);
g=islessgreater(f,c);
g=ceil(d);
c=pow(g,e);
f=fmax(b,g);
}
b=isless(c,e);
c=floor(a);
}
