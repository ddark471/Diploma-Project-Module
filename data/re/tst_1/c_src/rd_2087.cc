#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=ceil(d);
g=sqrt(c);
b=isless(c,b);
while(islessgreater(f,c)){
b=isgreater(c,h);
e=sin(f);
b=log(e);
h=fmax(c,e);
}
f=asin(g);
g=fmin(f,g);
if(isless(a,b)){
e=exp(e);
f=pow(a,a);
h=fdim(c,g);
}
b=islessgreater(c,f);
a=acos(f);
h=isgreaterequal(d,c);
}
