#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=log10(c);
d=pow(c,c);
a=log10(a);
while(islessequal(a,c)){
d=fdim(c,b);
c=isless(c,c);
a=exp(a);
}
b=sqrt(c);
b=islessequal(b,b);
b=atan2(b,b);
c=fmin(d,a);
c=sqrt(b);
}
