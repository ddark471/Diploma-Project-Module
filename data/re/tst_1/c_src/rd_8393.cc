#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=tan(d);
c=atan(b);
b=log(a);
while(isless(c,c)){
c=fmin(d,b);
d=islessequal(a,c);
d=fmin(d,a);
c=fmin(b,a);
}
a=isgreater(d,c);
c=sin(c);
}
