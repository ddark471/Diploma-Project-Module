#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=isless(c,f);
e=ceil(f);
while(isgreaterequal(d,b)){
b=floor(c);
g=isless(g,f);
c=isless(e,e);
a=asin(c);
c=atan2(g,g);
}
d=log10(d);
a=islessgreater(a,b);
if(islessgreater(a,d)){
c=sin(a);
a=pow(c,c);
c=pow(a,c);
a=cos(d);
}
b=fmax(b,a);
c=isless(c,a);
c=acos(c);
c=islessgreater(b,c);
}
