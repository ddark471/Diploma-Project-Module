#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=log(a);
a=asin(g);
c=isgreater(f,a);
while(islessgreater(f,f)){
b=isgreater(f,b);
d=pow(f,c);
h=exp(c);
c=tan(f);
}
f=acos(f);
f=floor(h);
d=fmax(b,d);
}
