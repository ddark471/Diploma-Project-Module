#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=sqrt(a);
b=isless(b,b);
a=islessequal(c,c);
a=ceil(b);
while(islessgreater(b,c)){
a=sin(a);
d=sin(d);
b=isless(c,c);
c=atan(d);
}
d=fdim(b,c);
d=exp(c);
}
