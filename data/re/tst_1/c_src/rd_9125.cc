#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fdim(b,b);
d=atan2(c,a);
a=ceil(a);
while(isless(c,b)){
b=isgreater(h,h);
e=fmin(h,f);
h=islessgreater(d,a);
d=cos(b);
}
h=islessgreater(b,e);
d=ceil(b);
b=floor(d);
}
