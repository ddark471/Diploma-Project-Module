#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=fmin(g,d);
a=ceil(d);
while(islessequal(a,g)){
g=tan(e);
f=log(a);
}
b=log(f);
c=sqrt(g);
c=log10(d);
a=log(c);
}
