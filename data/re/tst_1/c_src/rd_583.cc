#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=cos(a);
c=isgreater(a,b);
while(islessequal(c,e)){
b=isgreater(a,d);
c=fdim(e,c);
}
c=pow(a,e);
a=fmin(a,a);
if(isgreaterequal(e,a)){
e=tan(a);
e=islessgreater(b,a);
d=floor(f);
}
else{
a=sqrt(d);
e=pow(f,d);
d=fmax(f,e);
}
d=atan2(b,c);
g=cos(d);
}
