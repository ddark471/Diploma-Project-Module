#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=ceil(e);
g=floor(a);
f=isgreater(e,b);
d=exp(f);
f=ceil(a);
while(isgreaterequal(c,b)){
d=fmax(g,b);
d=pow(e,b);
}
f=floor(a);
f=log10(a);
e=isless(d,e);
}
