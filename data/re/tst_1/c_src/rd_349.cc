#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=pow(f,a);
e=isgreaterequal(b,a);
if(islessequal(b,a)){
a=fdim(c,f);
a=atan2(d,a);
}
else{
e=asin(f);
b=fmin(c,d);
}
a=fdim(c,d);
c=isless(b,e);
d=isless(a,c);
while(isgreaterequal(a,f)){
a=log10(b);
e=atan(b);
e=log10(f);
}
g=sqrt(h);
h=fmax(h,g);
f=sin(d);
if(islessequal(f,e)){
h=sin(d);
f=ceil(g);
c=log10(h);
c=acos(g);
}
b=islessgreater(b,a);
e=islessequal(d,e);
h=pow(h,h);
}
