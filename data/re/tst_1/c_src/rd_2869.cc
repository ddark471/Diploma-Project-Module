#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=isgreater(c,e);
e=pow(b,f);
while(islessequal(d,c)){
c=islessgreater(a,d);
g=sqrt(d);
}
f=isless(f,g);
c=exp(g);
e=log10(a);
a=islessequal(e,a);
e=sqrt(f);
}
