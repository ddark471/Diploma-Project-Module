#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fdim(a,c);
d=floor(c);
b=atan(d);
d=fmin(c,c);
b=sin(b);
while(isless(a,d)){
a=fmin(c,b);
d=isless(a,c);
c=sqrt(a);
}
c=acos(c);
d=islessequal(c,b);
c=fmax(d,c);
}
