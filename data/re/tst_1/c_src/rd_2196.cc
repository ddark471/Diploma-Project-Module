#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fmax(b,e);
b=islessequal(d,d);
f=islessgreater(d,c);
d=islessequal(c,b);
c=sqrt(g);
while(islessgreater(e,d)){
a=log(f);
e=isgreaterequal(a,f);
f=fmin(c,c);
g=log(e);
}
c=fmax(c,c);
f=ceil(d);
}
