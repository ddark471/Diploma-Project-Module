#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=atan(d);
d=islessgreater(a,b);
while(islessequal(d,g)){
c=isless(c,c);
g=asin(a);
d=islessequal(c,d);
}
a=sqrt(d);
d=tan(c);
b=ceil(c);
if(islessequal(a,d)){
e=log(d);
c=sin(b);
c=fmax(c,b);
}
d=exp(d);
d=pow(e,c);
b=acos(e);
}
