#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=isgreater(e,a);
c=pow(f,b);
a=isgreaterequal(d,a);
f=fmax(c,f);
while(isless(b,e)){
d=sin(d);
d=exp(e);
c=isgreaterequal(b,f);
f=asin(f);
}
d=fmin(e,b);
d=floor(d);
f=sqrt(d);
}
