#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fmin(g,e);
e=pow(a,a);
g=pow(f,f);
c=sqrt(a);
while(islessequal(b,f)){
e=acos(c);
f=tan(c);
e=tan(b);
}
b=sin(g);
a=log(f);
}
