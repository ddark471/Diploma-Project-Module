#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=log(d);
b=fmin(d,d);
c=isgreater(g,b);
while(islessgreater(c,e)){
e=fmax(h,b);
c=fmax(d,f);
d=exp(b);
g=cos(f);
b=sin(h);
}
b=log(c);
b=fmin(f,g);
d=log(b);
}
