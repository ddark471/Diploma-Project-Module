#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=sin(b);
f=isless(g,c);
while(isless(h,d)){
e=isgreater(h,g);
c=fdim(h,c);
c=tan(e);
}
h=log(g);
f=fmax(d,c);
a=atan2(b,f);
}
