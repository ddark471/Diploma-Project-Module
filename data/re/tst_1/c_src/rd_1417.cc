#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=isgreater(f,h);
f=ceil(g);
g=sin(b);
while(islessequal(a,h)){
d=log10(e);
g=isless(g,b);
b=atan2(f,b);
}
d=pow(a,d);
a=isgreater(a,c);
g=ceil(h);
g=fmax(d,a);
c=exp(g);
}
