#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fmax(b,b);
f=isgreater(f,e);
while(islessgreater(e,c)){
f=log10(c);
b=islessgreater(b,g);
d=fdim(b,g);
}
a=log10(b);
d=tan(g);
}
