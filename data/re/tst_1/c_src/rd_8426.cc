#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=isgreaterequal(b,e);
c=exp(c);
a=sqrt(f);
d=exp(a);
c=log10(d);
while(isgreaterequal(a,c)){
a=fmin(c,a);
b=log10(d);
}
a=pow(b,d);
b=fmax(b,c);
}
