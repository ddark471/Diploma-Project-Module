#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=pow(a,c);
d=atan(e);
if(isless(a,c)){
c=fmax(e,f);
f=islessequal(c,d);
}
b=sin(a);
f=fmin(d,e);
e=islessequal(b,g);
while(isgreaterequal(b,h)){
b=atan2(f,e);
f=isgreaterequal(b,h);
g=log10(d);
}
b=sin(b);
e=fmax(h,f);
f=atan(c);
d=exp(h);
b=asin(f);
if(isless(f,h)){
g=sqrt(e);
h=islessequal(b,b);
f=atan(b);
g=acos(c);
}
b=atan2(b,a);
a=log(e);
f=islessgreater(g,h);
e=fdim(e,e);
g=log(h);
}
