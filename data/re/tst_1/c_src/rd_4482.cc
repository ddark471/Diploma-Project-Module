#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fmax(a,b);
d=log10(d);
c=atan2(b,a);
while(isless(a,b)){
d=log10(a);
d=atan(a);
b=sin(d);
a=isgreater(a,c);
}
c=tan(c);
d=islessequal(c,c);
c=pow(b,a);
b=islessequal(d,b);
}
