#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=isless(f,a);
d=isgreater(f,a);
c=fmax(a,c);
f=islessgreater(d,d);
if(isgreaterequal(c,d)){
f=exp(b);
a=atan(c);
d=islessequal(b,b);
}
f=isless(e,b);
f=fdim(c,d);
b=cos(a);
while(isless(b,a)){
b=fdim(d,a);
a=islessequal(c,a);
}
d=acos(c);
d=log(d);
}
