#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=sin(c);
d=islessgreater(d,a);
while(isgreaterequal(c,c)){
b=acos(c);
a=isless(b,a);
}
d=acos(b);
d=isgreater(a,c);
}
