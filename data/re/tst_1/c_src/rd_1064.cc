#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=floor(f);
f=sqrt(c);
while(isless(g,a)){
e=isgreaterequal(a,a);
g=islessgreater(d,c);
d=ceil(d);
c=log10(d);
}
b=cos(b);
c=atan2(e,e);
}
