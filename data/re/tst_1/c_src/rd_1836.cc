#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=isgreaterequal(e,a);
f=fmax(c,c);
if(islessequal(a,h)){
b=isless(e,c);
f=tan(h);
g=acos(d);
}
e=isgreaterequal(f,f);
c=islessgreater(h,a);
c=isgreaterequal(a,g);
while(islessgreater(f,f)){
b=sqrt(a);
c=atan2(e,a);
}
f=exp(e);
c=log(b);
}
