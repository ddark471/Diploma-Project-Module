#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fdim(b,a);
c=fmin(b,a);
d=cos(c);
c=sin(d);
b=pow(a,d);
while(islessequal(c,a)){
a=fdim(c,b);
c=isgreaterequal(d,d);
d=isgreaterequal(b,d);
b=fdim(c,b);
d=atan2(c,b);
}
b=log10(a);
d=isless(c,a);
}
