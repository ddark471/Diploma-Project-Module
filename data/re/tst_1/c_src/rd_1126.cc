#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=floor(a);
d=isgreater(a,c);
b=floor(a);
a=islessgreater(d,b);
while(isless(b,a)){
a=islessequal(b,b);
a=cos(c);
c=atan2(c,b);
}
h=fmin(e,a);
e=pow(d,a);
c=fmin(e,h);
e=log10(d);
c=log10(h);
if(islessgreater(d,f)){
b=isless(e,h);
g=isgreater(f,d);
e=isgreaterequal(d,f);
}
else{
c=isless(h,e);
g=isgreaterequal(f,b);
d=atan(g);
}
a=acos(b);
e=islessgreater(c,d);
f=exp(g);
a=isless(d,a);
a=pow(g,c);
}
