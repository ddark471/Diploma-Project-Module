#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=islessgreater(b,c);
b=cos(d);
a=fmin(c,d);
c=fmin(b,b);
c=sqrt(a);
while(isgreaterequal(a,a)){
d=log(d);
c=sqrt(b);
}
g=pow(e,h);
e=isgreater(h,e);
h=tan(g);
f=tan(h);
if(isless(g,a)){
a=sqrt(c);
g=log(b);
a=floor(f);
f=fmin(c,d);
}
g=sin(b);
f=ceil(a);
f=isgreater(h,f);
c=tan(e);
h=islessgreater(c,b);
}
