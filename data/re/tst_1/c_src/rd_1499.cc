#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=isless(a,a);
d=isgreater(b,b);
a=isgreaterequal(e,e);
while(isless(c,d)){
e=isgreater(e,a);
a=isgreaterequal(e,c);
a=exp(d);
b=isless(b,d);
}
b=pow(c,c);
b=fmin(g,h);
a=sqrt(h);
h=tan(h);
}
