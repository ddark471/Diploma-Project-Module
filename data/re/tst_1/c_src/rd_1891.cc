#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=log10(d);
f=cos(f);
f=pow(d,f);
e=fmax(e,a);
b=sqrt(f);
while(isless(d,c)){
b=tan(d);
c=atan(f);
d=asin(f);
d=cos(e);
}
c=asin(a);
d=islessequal(c,d);
if(isless(b,d)){
a=sqrt(a);
e=fmin(d,d);
}
a=islessequal(c,b);
a=log10(b);
}
