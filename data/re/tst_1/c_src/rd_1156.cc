#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=isless(e,b);
c=log(e);
while(islessgreater(b,b)){
d=islessgreater(a,a);
d=acos(c);
a=pow(d,b);
c=pow(c,a);
}
a=isless(a,b);
e=acos(c);
e=fmax(b,d);
}
