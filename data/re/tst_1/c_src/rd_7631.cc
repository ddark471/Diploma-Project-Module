#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=fmax(h,a);
h=cos(c);
c=atan2(b,g);
g=fmin(b,c);
e=exp(b);
while(islessgreater(g,h)){
b=log(h);
d=log10(b);
h=islessgreater(c,b);
g=sqrt(h);
}
c=log(c);
e=tan(c);
b=isgreater(g,a);
a=fmax(e,g);
}
