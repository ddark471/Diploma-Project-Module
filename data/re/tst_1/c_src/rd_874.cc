#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=atan2(f,g);
c=log10(e);
d=cos(d);
while(islessgreater(d,d)){
g=sqrt(d);
c=fmin(d,g);
e=log10(d);
}
c=islessgreater(d,g);
e=exp(e);
}
