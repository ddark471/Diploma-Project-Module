#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=pow(d,g);
g=isgreater(e,a);
while(islessgreater(b,b)){
g=fmax(b,f);
c=islessequal(e,c);
f=isgreater(g,g);
a=isgreater(a,e);
f=isless(a,a);
}
a=floor(d);
d=fmin(c,c);
if(islessgreater(c,a)){
b=isgreater(d,d);
b=fmin(d,a);
}
d=log(d);
b=fmin(b,c);
a=log10(b);
c=pow(a,b);
b=exp(a);
}
