#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=exp(c);
d=sin(c);
while(isless(d,e)){
c=atan(a);
d=islessgreater(f,g);
a=acos(f);
g=log10(f);
}
e=cos(a);
a=atan2(e,b);
b=ceil(g);
}
