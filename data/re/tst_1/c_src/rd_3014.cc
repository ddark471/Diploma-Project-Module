#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=isgreater(a,b);
a=atan2(d,d);
d=log10(b);
d=islessequal(e,b);
a=fmin(b,e);
while(islessequal(b,b)){
e=pow(c,a);
e=isless(d,d);
c=sqrt(e);
}
a=isgreater(d,e);
a=log10(d);
c=fmax(d,b);
d=islessequal(e,a);
e=ceil(d);
if(islessequal(a,e)){
d=isless(a,b);
c=sqrt(a);
d=isless(e,a);
c=fmin(a,b);
}
else{
d=isgreater(b,d);
b=fmax(d,e);
e=isless(a,d);
}
d=exp(d);
c=cos(d);
e=fdim(b,c);
e=log(e);
}
