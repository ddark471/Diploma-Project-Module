#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=atan2(b,d);
f=floor(b);
while(islessequal(c,d)){
d=sqrt(f);
c=exp(e);
d=fmax(c,g);
}
e=fmax(d,b);
a=log(b);
f=fmin(g,a);
b=atan2(d,b);
g=fmin(f,g);
}
