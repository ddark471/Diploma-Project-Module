#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=sin(a);
c=atan2(b,a);
d=ceil(b);
d=isless(d,d);
if(isless(b,a)){
d=isless(b,b);
b=asin(c);
}
d=islessequal(c,d);
b=fmax(c,d);
a=floor(a);
while(islessequal(a,b)){
d=asin(a);
c=isless(a,a);
}
b=ceil(b);
d=fdim(c,d);
c=pow(a,d);
d=pow(a,a);
c=isgreater(a,b);
}
