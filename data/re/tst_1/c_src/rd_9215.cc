#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=acos(b);
d=asin(b);
a=pow(b,c);
while(islessgreater(b,d)){
c=islessgreater(c,a);
a=fmax(a,c);
d=islessgreater(c,c);
}
f=islessgreater(b,d);
c=sqrt(c);
c=ceil(b);
g=log10(c);
}
