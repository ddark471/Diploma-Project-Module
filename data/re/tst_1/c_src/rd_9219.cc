#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=log10(d);
d=isgreater(b,b);
b=isgreaterequal(e,c);
b=pow(f,c);
if(islessequal(c,d)){
d=isgreater(b,e);
f=pow(c,d);
}
d=islessgreater(f,a);
a=islessgreater(c,e);
while(isless(b,b)){
c=sqrt(b);
a=floor(d);
b=exp(b);
c=exp(d);
}
d=sqrt(a);
a=exp(b);
if(isless(d,b)){
c=sqrt(d);
d=isless(d,a);
d=fdim(d,a);
c=floor(c);
}
b=ceil(d);
c=fmax(d,a);
d=sin(d);
}
