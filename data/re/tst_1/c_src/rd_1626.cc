#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=sqrt(d);
d=isgreater(c,e);
h=sqrt(a);
a=tan(c);
h=pow(f,d);
if(islessequal(f,h)){
e=exp(a);
b=isless(c,h);
f=sqrt(h);
g=log10(e);
c=atan2(h,g);
}
else{
h=fmax(c,c);
h=fmin(e,e);
f=exp(b);
}
b=isless(c,a);
g=fmax(b,d);
e=atan2(h,a);
while(isgreaterequal(c,e)){
d=sqrt(d);
c=asin(h);
g=pow(a,g);
}
a=isgreaterequal(b,e);
d=acos(a);
}
