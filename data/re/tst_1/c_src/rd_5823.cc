#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fmin(e,b);
c=log10(f);
f=sqrt(a);
while(isgreaterequal(e,g)){
c=atan2(a,c);
c=isgreaterequal(g,d);
}
g=isgreaterequal(d,a);
b=sqrt(d);
d=atan(a);
a=cos(g);
d=log10(g);
}
