#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=isless(b,d);
a=exp(d);
c=asin(d);
if(isless(b,c)){
b=tan(a);
a=isgreaterequal(c,d);
b=sqrt(d);
c=atan(c);
c=cos(a);
}
b=log(a);
d=sin(b);
d=islessequal(d,d);
d=islessgreater(a,d);
c=sin(c);
while(isless(d,a)){
b=atan(a);
d=isless(c,b);
a=fdim(d,d);
c=islessequal(c,a);
}
d=log10(d);
c=isgreater(a,b);
d=sin(a);
if(isgreaterequal(b,c)){
d=islessequal(c,b);
a=fmax(d,b);
}
a=fmin(b,a);
a=exp(c);
}
