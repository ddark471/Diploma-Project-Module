#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=isgreater(d,c);
a=asin(c);
c=log10(d);
while(islessequal(c,d)){
c=exp(d);
d=cos(a);
b=ceil(d);
b=asin(d);
}
d=pow(b,b);
a=isgreater(c,b);
}
