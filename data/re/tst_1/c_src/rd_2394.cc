#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=tan(g);
g=isless(g,b);
c=cos(g);
c=isless(g,f);
while(isgreaterequal(e,a)){
f=islessequal(f,b);
g=cos(f);
}
g=log(b);
a=fmin(g,a);
d=log10(e);
}
