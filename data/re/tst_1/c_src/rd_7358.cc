#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=islessequal(a,g);
e=fdim(g,b);
d=islessgreater(b,c);
c=asin(e);
f=islessequal(d,b);
while(isless(e,d)){
d=ceil(b);
g=islessequal(h,b);
d=log(g);
f=fmax(e,f);
}
a=exp(g);
g=ceil(g);
d=log10(g);
c=isless(a,d);
c=islessequal(h,d);
if(isless(h,d)){
h=log(c);
b=tan(d);
e=acos(b);
d=isgreater(b,g);
g=islessequal(a,a);
}
g=isgreaterequal(d,b);
c=exp(a);
}
