#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=sin(c);
d=sqrt(a);
while(isgreaterequal(a,e)){
e=fmax(f,d);
d=pow(a,e);
c=log10(d);
a=islessgreater(b,a);
}
e=atan(b);
e=fmax(e,a);
a=exp(a);
}
