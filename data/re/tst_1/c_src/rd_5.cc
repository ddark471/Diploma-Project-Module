#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=log10(d);
f=isless(f,b);
while(isgreaterequal(f,d)){
c=log10(f);
e=isgreater(e,g);
f=pow(f,d);
g=atan2(e,d);
}
e=fmax(e,c);
g=sqrt(e);
}
