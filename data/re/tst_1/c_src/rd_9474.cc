#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=isless(b,a);
d=sqrt(d);
while(islessgreater(c,d)){
c=asin(b);
a=islessequal(c,a);
}
b=cos(c);
d=pow(d,b);
c=isless(d,d);
b=islessgreater(b,c);
}
