#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=cos(d);
d=log(c);
while(isless(b,d)){
c=cos(a);
d=fmax(a,a);
e=sqrt(e);
}
d=isgreater(b,d);
a=isless(e,e);
c=atan2(b,c);
c=floor(e);
}
