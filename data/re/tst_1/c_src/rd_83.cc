#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=isgreaterequal(e,b);
c=isgreater(b,a);
f=islessgreater(g,e);
a=log(d);
while(isless(f,a)){
g=log10(e);
a=isless(h,a);
e=islessgreater(c,f);
c=pow(a,h);
d=log10(g);
}
a=floor(e);
e=fmin(b,b);
}
