#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fmax(c,c);
c=floor(a);
if(islessgreater(a,c)){
d=exp(b);
c=atan2(a,a);
}
b=ceil(c);
a=fmin(d,d);
d=fmin(d,b);
b=isless(b,b);
d=isgreaterequal(d,a);
while(islessgreater(a,c)){
a=isgreaterequal(d,c);
d=fdim(b,a);
c=islessequal(a,c);
d=tan(c);
}
f=pow(c,c);
g=islessequal(a,e);
a=floor(g);
d=fmax(b,a);
c=cos(e);
if(islessequal(f,f)){
g=isgreater(e,f);
c=log10(d);
}
else{
g=tan(g);
b=atan2(f,f);
c=islessequal(e,d);
f=islessequal(f,d);
}
d=cos(e);
a=asin(g);
c=atan(e);
a=sin(d);
}
