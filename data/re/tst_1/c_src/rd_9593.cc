#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=floor(c);
a=islessgreater(b,d);
while(isgreaterequal(a,c)){
b=sqrt(a);
c=floor(d);
}
b=fmax(c,a);
c=exp(a);
}
