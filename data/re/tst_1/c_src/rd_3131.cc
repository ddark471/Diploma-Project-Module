#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=isless(c,a);
d=pow(a,c);
b=fmin(c,e);
while(isless(e,a)){
a=fmax(d,b);
e=islessequal(c,d);
c=log(a);
c=islessgreater(e,b);
}
a=isgreater(b,d);
c=fmax(a,d);
}
