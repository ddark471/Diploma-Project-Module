#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fmax(b,a);
e=fdim(a,f);
e=tan(d);
a=fmax(a,b);
while(islessequal(f,c)){
c=sin(d);
g=acos(g);
b=log10(a);
}
d=atan(f);
g=acos(g);
f=isless(e,e);
c=sqrt(g);
}
