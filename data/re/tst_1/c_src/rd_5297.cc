#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=atan2(b,a);
b=sin(b);
d=isless(a,b);
while(isless(b,c)){
a=cos(a);
a=isless(c,b);
c=acos(c);
}
c=isgreater(a,d);
a=fdim(d,a);
d=sin(b);
}
