#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fmax(e,d);
e=log10(a);
while(islessgreater(b,d)){
b=islessequal(c,e);
d=atan2(e,b);
a=atan2(c,a);
a=fmax(a,d);
}
c=isgreater(a,d);
c=sqrt(a);
e=tan(b);
}
