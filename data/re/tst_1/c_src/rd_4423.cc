#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=isgreater(b,a);
d=ceil(d);
a=asin(c);
d=fmax(a,b);
a=fmax(a,d);
while(isgreaterequal(a,d)){
a=log(b);
a=isgreater(c,a);
}
b=floor(c);
c=isless(c,c);
}
