#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=log10(b);
d=pow(a,b);
while(islessequal(f,e)){
c=isgreater(g,a);
c=asin(b);
c=isgreater(c,d);
e=fdim(e,g);
b=fmax(a,b);
}
c=fdim(d,h);
a=log10(h);
f=sqrt(h);
d=isless(f,h);
if(isgreaterequal(g,d)){
g=exp(h);
d=acos(g);
f=isgreaterequal(f,f);
}
g=fdim(c,c);
h=fmin(a,h);
g=sin(d);
h=ceil(d);
}
