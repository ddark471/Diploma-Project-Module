#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=atan(d);
e=log(f);
f=isgreater(a,a);
a=isless(b,e);
c=cos(d);
while(islessequal(a,a)){
e=log(b);
c=isless(d,a);
}
f=exp(b);
d=atan(d);
}
