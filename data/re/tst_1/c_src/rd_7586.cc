#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=acos(a);
b=isless(d,d);
d=cos(d);
while(isgreaterequal(d,c)){
b=ceil(c);
b=sin(c);
b=sqrt(b);
a=atan2(b,a);
d=cos(a);
}
d=fmax(a,a);
c=atan2(a,c);
c=atan2(a,a);
b=fdim(d,c);
a=atan2(b,b);
if(islessgreater(c,b)){
c=cos(b);
d=fdim(c,c);
a=atan(a);
}
else{
b=log(a);
d=islessequal(a,b);
}
c=fmax(c,d);
c=islessgreater(c,b);
a=fmax(a,d);
}
