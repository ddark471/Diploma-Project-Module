#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=isgreaterequal(f,g);
h=islessequal(f,h);
h=tan(a);
while(islessequal(a,f)){
e=isless(b,g);
g=log(h);
e=ceil(h);
d=floor(d);
}
b=isgreaterequal(a,b);
d=sin(c);
a=exp(a);
if(islessequal(b,c)){
d=pow(a,a);
d=log(b);
d=islessequal(b,b);
b=sqrt(c);
}
else{
a=exp(c);
c=sin(b);
b=log10(b);
c=fmin(a,a);
}
d=log(b);
b=fmax(d,c);
}
