#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=sin(d);
d=fmax(a,d);
c=islessgreater(c,a);
while(isless(c,b)){
b=isless(b,d);
a=fmax(d,d);
}
b=atan2(c,d);
a=pow(d,d);
c=cos(b);
b=floor(b);
}
