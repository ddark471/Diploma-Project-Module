#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=atan2(e,b);
c=floor(e);
h=isgreater(d,g);
if(islessgreater(f,a)){
c=cos(g);
f=islessgreater(e,d);
}
else{
b=tan(d);
h=pow(g,d);
d=tan(g);
f=sin(c);
d=fmin(a,g);
}
h=fmax(b,h);
a=exp(a);
e=atan(f);
while(islessequal(d,f)){
c=cos(e);
c=log(f);
}
d=fmax(c,d);
b=islessgreater(a,b);
g=ceil(h);
a=cos(f);
e=acos(a);
}
