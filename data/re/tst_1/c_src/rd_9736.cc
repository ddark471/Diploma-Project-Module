#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=sin(d);
e=isless(c,b);
d=atan(b);
d=tan(c);
while(islessequal(e,e)){
e=floor(b);
c=ceil(f);
}
c=sqrt(c);
b=exp(f);
a=sin(b);
c=pow(f,d);
}
