#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=pow(g,b);
b=atan2(e,f);
b=sqrt(g);
a=ceil(e);
while(isgreaterequal(h,f)){
g=acos(a);
h=exp(g);
g=atan(b);
h=islessgreater(g,g);
a=log10(g);
}
e=islessequal(b,c);
c=log(f);
b=isgreater(f,a);
e=fmax(e,c);
e=isless(b,b);
}
