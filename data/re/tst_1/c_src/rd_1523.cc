#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=tan(a);
d=pow(a,a);
while(isgreaterequal(c,b)){
a=acos(d);
d=pow(d,c);
d=ceil(c);
}
f=sin(g);
e=isless(c,a);
d=isless(f,a);
g=islessgreater(e,c);
}
