#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fmax(d,a);
c=atan(b);
c=pow(c,c);
a=sin(a);
a=log(a);
if(islessequal(c,c)){
c=atan(c);
d=atan(b);
}
d=log10(c);
a=sin(c);
a=fmax(d,d);
b=fmax(b,c);
while(isless(b,c)){
c=pow(b,d);
a=isgreaterequal(c,c);
d=islessequal(c,c);
c=fmax(a,b);
}
f=sin(g);
b=ceil(c);
if(islessequal(f,b)){
b=tan(c);
c=atan(c);
d=ceil(d);
g=pow(c,c);
e=isgreater(g,g);
}
else{
h=fmin(c,f);
f=islessgreater(h,a);
}
c=log(b);
b=fdim(c,c);
}
