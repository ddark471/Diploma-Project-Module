#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=cos(b);
b=islessgreater(b,b);
while(isless(c,a)){
b=fmin(b,b);
c=log(d);
c=isgreater(a,b);
b=ceil(c);
}
a=islessequal(a,c);
d=exp(c);
}
