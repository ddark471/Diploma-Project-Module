#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=atan2(d,c);
f=islessequal(g,g);
h=ceil(c);
while(isless(a,d)){
a=fmin(h,f);
c=fdim(c,g);
e=tan(h);
}
g=atan2(f,e);
h=asin(c);
f=log(h);
f=atan(h);
}
