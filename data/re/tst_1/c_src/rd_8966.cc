#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=pow(c,d);
d=fmax(e,e);
d=log(b);
while(islessgreater(e,d)){
c=sin(d);
d=sin(d);
}
b=ceil(b);
b=floor(b);
}
