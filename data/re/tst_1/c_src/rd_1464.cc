#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=fmin(f,h);
h=log10(a);
f=isgreater(g,f);
c=pow(e,d);
while(isgreaterequal(g,e)){
e=acos(b);
h=cos(c);
b=fdim(e,c);
}
a=isgreater(b,b);
f=atan2(e,g);
f=atan(e);
if(islessequal(a,d)){
h=log10(b);
c=cos(c);
b=atan2(h,e);
g=log10(b);
a=pow(b,g);
}
else{
e=fmax(c,h);
h=asin(c);
f=isgreater(c,e);
h=isgreaterequal(f,a);
}
a=atan(h);
e=atan2(a,g);
}
