#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=islessequal(a,g);
f=asin(b);
while(islessequal(f,f)){
f=pow(f,c);
a=pow(e,g);
c=fmin(f,f);
e=pow(e,f);
g=exp(c);
}
a=isgreater(e,d);
f=fmin(c,f);
d=asin(f);
f=isgreaterequal(a,f);
}
