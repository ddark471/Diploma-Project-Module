#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=atan(e);
b=sqrt(a);
f=asin(b);
while(islessequal(f,e)){
d=floor(g);
b=isless(a,b);
d=islessequal(g,d);
c=fdim(d,g);
d=fdim(d,b);
}
d=pow(f,b);
e=fmax(a,b);
e=islessequal(e,f);
}
