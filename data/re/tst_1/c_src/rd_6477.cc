#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=tan(b);
e=isgreater(f,b);
b=pow(e,a);
while(islessequal(a,b)){
a=sqrt(a);
f=fmin(d,f);
}
d=isgreater(b,c);
a=sqrt(a);
c=ceil(f);
}
