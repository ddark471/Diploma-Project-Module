#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=asin(d);
d=fdim(d,a);
e=tan(d);
if(isless(b,c)){
e=fmax(e,a);
a=fmin(b,b);
}
else{
e=ceil(c);
c=floor(a);
}
c=isless(e,a);
d=fmin(e,b);
while(islessgreater(e,b)){
d=log10(d);
a=fmin(d,d);
a=log(b);
e=islessequal(c,a);
c=fdim(a,d);
}
d=log10(b);
g=fmax(c,c);
c=fmax(d,c);
g=islessequal(f,d);
e=ceil(g);
if(isless(g,c)){
c=floor(c);
e=isgreater(f,e);
f=isgreater(e,d);
a=log(b);
d=pow(e,e);
}
b=pow(d,d);
e=islessgreater(c,d);
f=fmax(a,g);
b=isless(a,a);
}
