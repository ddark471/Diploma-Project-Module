#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=asin(b);
f=islessequal(h,d);
g=sqrt(h);
b=fmin(d,c);
while(islessequal(a,a)){
e=atan(g);
h=atan(d);
d=islessequal(f,a);
}
g=cos(d);
b=log(h);
h=islessequal(c,f);
c=cos(d);
f=fmin(e,g);
}
