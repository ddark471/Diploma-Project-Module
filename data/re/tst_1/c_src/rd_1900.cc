#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=fdim(e,a);
b=fmax(c,f);
while(islessequal(d,c)){
d=pow(f,c);
b=isgreater(b,a);
}
f=fmin(a,f);
g=isgreaterequal(e,g);
g=pow(a,b);
}
