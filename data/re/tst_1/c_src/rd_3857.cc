#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=pow(g,d);
d=isgreater(a,e);
f=islessgreater(e,b);
if(isless(f,f)){
d=sqrt(b);
a=sqrt(e);
d=asin(b);
}
else{
d=islessgreater(e,a);
f=sqrt(g);
c=islessgreater(e,d);
}
b=log(c);
e=pow(c,d);
g=asin(d);
f=log(d);
while(islessgreater(d,e)){
b=islessequal(e,b);
g=log10(c);
}
c=fmax(c,b);
d=sin(b);
}
