#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=pow(b,d);
c=isless(e,a);
d=sin(a);
if(islessgreater(a,b)){
a=fmin(a,e);
e=isless(a,a);
}
a=log10(e);
e=fmin(c,b);
while(islessgreater(a,a)){
a=islessequal(b,b);
d=exp(c);
}
d=floor(a);
h=pow(d,f);
e=atan(d);
}
