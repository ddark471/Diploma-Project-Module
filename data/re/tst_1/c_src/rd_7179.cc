#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=log(e);
d=fdim(f,g);
a=fdim(h,a);
g=islessequal(b,b);
while(islessgreater(c,c)){
h=fmax(h,b);
b=pow(g,f);
f=log10(d);
d=log10(h);
}
f=isgreaterequal(e,h);
e=fmax(h,b);
}
