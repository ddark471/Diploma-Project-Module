#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=sqrt(c);
e=pow(f,c);
g=sqrt(f);
while(islessequal(c,f)){
h=cos(a);
g=log10(d);
c=pow(c,b);
}
g=floor(d);
e=isgreaterequal(e,a);
e=tan(f);
a=fmax(g,g);
}
