#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=fmin(g,a);
h=islessgreater(e,b);
g=isgreaterequal(a,g);
while(isgreaterequal(a,a)){
g=atan2(a,f);
b=atan2(e,c);
d=log(e);
}
e=isgreater(d,e);
d=isless(d,c);
if(islessequal(b,e)){
a=pow(d,c);
d=exp(e);
d=floor(b);
c=sin(c);
a=fmin(d,b);
}
else{
e=floor(a);
a=sqrt(d);
}
e=islessgreater(a,b);
c=fmin(c,d);
d=tan(a);
e=fmax(a,a);
a=pow(e,a);
}
