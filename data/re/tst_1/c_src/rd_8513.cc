#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=isgreaterequal(b,a);
a=cos(a);
d=log10(d);
a=fmin(d,a);
while(islessgreater(c,c)){
a=atan(c);
a=fmin(c,c);
}
b=fmax(c,a);
d=log(a);
}
