#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=isgreater(b,b);
c=isgreater(a,e);
h=tan(h);
if(islessgreater(e,f)){
a=islessequal(b,e);
c=fmin(c,c);
c=fdim(c,g);
}
else{
b=asin(d);
d=islessgreater(d,h);
c=atan(e);
}
c=atan2(g,d);
c=tan(b);
c=islessgreater(d,c);
while(islessgreater(g,h)){
g=fdim(d,b);
c=fmin(b,a);
e=tan(f);
}
d=pow(b,a);
d=fmax(a,b);
if(islessequal(b,c)){
b=log10(d);
c=log(c);
}
a=islessequal(c,d);
e=exp(d);
c=atan2(b,b);
a=fmax(a,c);
d=exp(c);
}
