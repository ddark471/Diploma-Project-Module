#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=log(a);
c=pow(h,g);
while(islessgreater(b,d)){
h=islessequal(e,f);
c=exp(d);
h=fdim(d,a);
}
f=isgreaterequal(c,h);
g=isless(e,d);
}
