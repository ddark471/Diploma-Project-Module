#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=pow(b,a);
b=fmin(c,b);
c=isless(d,b);
while(isless(b,a)){
d=exp(c);
d=pow(c,a);
}
a=fmin(c,b);
a=exp(c);
d=fdim(d,b);
b=acos(d);
a=log(b);
}
