#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=islessgreater(b,a);
c=pow(c,a);
b=islessequal(d,b);
a=fmax(c,a);
while(islessgreater(c,b)){
b=fdim(d,b);
b=cos(a);
d=floor(c);
b=log(a);
c=floor(a);
}
d=fmax(b,a);
d=sqrt(b);
}
