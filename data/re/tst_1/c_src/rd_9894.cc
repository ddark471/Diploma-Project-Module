#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=sin(b);
h=exp(d);
f=islessgreater(g,d);
a=islessgreater(g,h);
c=islessgreater(h,f);
while(islessequal(a,e)){
h=isgreaterequal(f,c);
a=islessequal(g,a);
a=cos(b);
}
a=sin(c);
c=pow(d,d);
d=islessequal(b,c);
e=fmin(a,c);
e=isless(e,b);
}
