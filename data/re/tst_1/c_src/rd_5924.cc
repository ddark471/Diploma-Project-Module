#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=asin(b);
d=sqrt(c);
a=fdim(c,a);
a=cos(d);
while(islessequal(c,d)){
c=atan2(d,b);
b=islessgreater(d,b);
}
d=log10(c);
a=log10(b);
if(isless(b,a)){
a=asin(c);
a=isless(d,c);
}
else{
d=exp(c);
d=sqrt(c);
b=isless(a,c);
c=isgreater(c,c);
}
c=cos(c);
c=exp(d);
d=isgreaterequal(b,a);
a=sin(d);
d=acos(d);
}
