#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=isgreater(d,b);
d=pow(b,c);
c=isgreater(b,a);
while(isgreaterequal(a,b)){
d=ceil(a);
d=exp(b);
}
d=cos(a);
a=sqrt(a);
d=isgreater(c,d);
a=islessequal(d,d);
}
