#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=atan2(b,g);
g=asin(g);
while(islessequal(g,e)){
c=cos(b);
f=atan(h);
c=atan2(d,e);
}
f=isgreaterequal(c,g);
d=isgreater(a,g);
h=fdim(b,d);
a=fdim(a,f);
e=fdim(h,f);
}
