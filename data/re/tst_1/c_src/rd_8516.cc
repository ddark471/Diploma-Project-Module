#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=isless(c,c);
h=isless(f,h);
a=fmin(h,g);
while(islessequal(b,b)){
g=atan(g);
c=exp(c);
a=floor(b);
}
g=cos(h);
f=isgreater(d,a);
e=isless(d,h);
f=exp(a);
c=pow(g,d);
}
