#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=atan2(c,c);
e=isless(c,c);
while(isless(e,e)){
c=isgreaterequal(b,a);
c=isless(b,d);
}
a=islessequal(c,b);
e=asin(c);
d=isgreaterequal(c,b);
}
