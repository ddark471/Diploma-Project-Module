#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=pow(d,b);
d=isgreater(a,b);
a=asin(a);
a=fdim(c,a);
while(islessgreater(b,a)){
c=fdim(a,b);
b=isgreaterequal(c,c);
}
d=pow(b,b);
c=ceil(b);
}
