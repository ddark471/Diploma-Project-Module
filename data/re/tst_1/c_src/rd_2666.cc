#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fmax(a,b);
b=islessequal(a,d);
c=log10(b);
while(isless(d,c)){
b=asin(d);
a=asin(a);
}
b=islessequal(a,d);
e=log10(c);
c=islessgreater(b,d);
d=sqrt(b);
}
