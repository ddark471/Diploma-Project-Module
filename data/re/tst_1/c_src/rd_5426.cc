#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=sqrt(c);
d=log10(c);
c=fmax(e,e);
while(islessequal(f,b)){
c=isgreaterequal(b,b);
b=tan(f);
}
a=fmax(a,d);
d=acos(c);
}
