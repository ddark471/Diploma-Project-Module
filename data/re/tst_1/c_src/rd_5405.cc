#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fmax(d,c);
a=isgreater(a,d);
c=fmin(b,a);
b=fmin(d,a);
c=log(b);
if(isless(c,c)){
c=isgreaterequal(b,b);
a=isgreater(b,d);
a=tan(d);
b=fmax(d,c);
}
else{
a=pow(a,a);
a=log(a);
}
a=fmax(a,b);
d=exp(c);
c=fmin(c,c);
c=cos(a);
while(islessequal(b,d)){
c=islessgreater(a,b);
d=pow(d,c);
d=islessgreater(a,d);
c=ceil(c);
a=log(d);
}
a=islessequal(d,c);
c=sin(d);
if(islessequal(a,a)){
a=fdim(c,a);
b=sin(a);
}
c=log(a);
c=ceil(a);
a=isless(b,c);
}
