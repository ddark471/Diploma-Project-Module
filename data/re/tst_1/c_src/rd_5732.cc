#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=log(b);
b=sin(g);
a=pow(e,b);
while(islessgreater(c,a)){
d=islessgreater(g,b);
e=log10(g);
g=islessgreater(d,f);
a=sin(f);
f=fdim(g,e);
}
e=fmin(f,g);
c=fmax(e,d);
}
