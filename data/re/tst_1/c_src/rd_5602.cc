#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=isless(e,e);
c=floor(b);
c=isless(b,b);
d=isgreater(c,b);
while(isgreaterequal(d,c)){
e=fmax(b,c);
c=islessgreater(a,a);
d=islessgreater(a,b);
a=pow(b,c);
}
e=isgreater(e,b);
d=fdim(c,b);
e=fmin(b,c);
e=pow(c,c);
}
