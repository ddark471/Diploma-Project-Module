#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=ceil(f);
b=isless(g,f);
e=fmax(e,a);
while(islessgreater(h,h)){
h=cos(f);
d=fmax(g,b);
d=islessgreater(f,f);
}
e=sqrt(g);
b=isgreaterequal(b,d);
e=isless(g,a);
if(islessgreater(d,c)){
f=fmax(d,b);
e=log10(g);
}
else{
g=isless(e,g);
e=isgreaterequal(a,g);
g=atan(e);
b=isgreater(a,e);
}
a=fmax(b,g);
b=cos(g);
g=atan2(d,g);
}
