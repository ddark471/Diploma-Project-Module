#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fmax(a,g);
d=ceil(c);
c=ceil(c);
e=fmin(g,c);
while(islessgreater(d,a)){
f=fmax(a,b);
a=tan(d);
}
a=sin(f);
f=isgreater(a,d);
c=isgreaterequal(f,c);
}
