#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=exp(b);
h=islessgreater(c,d);
e=fmin(h,d);
while(islessgreater(g,e)){
d=cos(c);
g=tan(b);
d=isgreaterequal(g,f);
e=fmax(h,c);
e=tan(e);
}
c=islessgreater(d,g);
d=fmin(e,a);
f=asin(f);
d=exp(g);
d=floor(f);
}
