#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=fmin(h,g);
h=islessgreater(c,f);
f=isless(h,f);
h=isgreaterequal(g,b);
if(isless(a,g)){
c=tan(h);
a=pow(h,f);
d=isgreater(h,d);
c=islessgreater(d,b);
}
else{
h=exp(a);
e=floor(b);
g=fmax(c,e);
}
b=log(f);
h=isless(a,g);
while(islessgreater(d,b)){
a=islessequal(a,c);
e=isless(b,g);
f=fmin(d,d);
d=atan(c);
}
d=floor(d);
a=isless(d,g);
h=floor(f);
g=isless(a,a);
}
