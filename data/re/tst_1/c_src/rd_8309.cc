#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=pow(e,a);
e=isgreater(e,h);
f=pow(g,e);
a=fmax(g,a);
c=islessequal(e,h);
while(isgreaterequal(a,e)){
f=fmin(e,a);
h=ceil(c);
b=pow(f,d);
b=exp(f);
g=sqrt(b);
}
b=cos(e);
e=pow(a,d);
a=atan2(c,a);
}
