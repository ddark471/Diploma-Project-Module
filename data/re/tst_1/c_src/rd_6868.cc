#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=sin(d);
a=sqrt(b);
b=cos(a);
while(isless(c,b)){
c=pow(c,b);
b=exp(b);
d=fmax(b,e);
a=isgreater(d,d);
}
c=floor(b);
e=fmax(c,b);
d=islessgreater(d,d);
}
