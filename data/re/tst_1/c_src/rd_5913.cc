#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=cos(b);
a=pow(a,d);
while(islessequal(d,b)){
b=pow(b,e);
e=log10(d);
}
b=isless(a,c);
c=isless(e,c);
d=fmin(b,b);
}
