#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=pow(g,d);
f=fmax(c,c);
while(islessgreater(b,c)){
f=exp(c);
a=log(f);
}
d=exp(d);
a=log(a);
if(isgreaterequal(a,c)){
b=log(b);
c=islessgreater(c,a);
a=exp(c);
a=isgreater(a,c);
d=isless(c,c);
}
c=cos(d);
a=fdim(a,b);
a=fdim(d,b);
d=ceil(d);
d=islessequal(c,a);
}
