#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=tan(c);
f=atan(f);
while(isgreaterequal(g,b)){
g=fmax(h,f);
d=fmax(c,b);
}
c=acos(f);
a=atan(e);
if(islessequal(c,e)){
c=fdim(g,c);
b=sin(c);
f=fmin(a,g);
a=acos(a);
}
f=sqrt(g);
b=fmax(h,e);
g=isgreater(f,a);
}
