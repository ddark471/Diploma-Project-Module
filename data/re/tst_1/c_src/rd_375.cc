#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=fmax(d,d);
a=islessgreater(a,d);
d=log(c);
c=fmax(c,d);
while(islessgreater(c,a)){
d=exp(a);
c=islessgreater(a,d);
}
a=islessgreater(b,a);
c=exp(d);
c=islessgreater(b,a);
a=islessgreater(b,b);
}
