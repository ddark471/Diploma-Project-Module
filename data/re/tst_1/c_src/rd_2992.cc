#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=sin(c);
d=log10(f);
while(isless(g,c)){
a=isgreaterequal(a,g);
e=isgreater(c,e);
d=pow(g,f);
}
e=sqrt(c);
g=acos(f);
a=atan2(c,c);
}
