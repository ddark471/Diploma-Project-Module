#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=log10(b);
f=atan(f);
b=sqrt(b);
a=ceil(b);
while(islessequal(d,e)){
b=fmax(e,c);
f=sqrt(c);
a=exp(d);
d=pow(b,b);
}
a=log(a);
f=islessequal(a,a);
c=fmin(f,e);
}
