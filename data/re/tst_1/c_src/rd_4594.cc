#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=isless(c,a);
a=islessgreater(c,a);
a=exp(b);
d=sin(d);
while(isgreaterequal(b,d)){
a=pow(b,c);
d=cos(d);
}
a=islessequal(a,b);
a=atan2(d,b);
}
