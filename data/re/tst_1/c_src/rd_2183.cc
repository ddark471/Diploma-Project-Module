#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=log10(b);
e=isgreaterequal(c,a);
c=exp(d);
while(isless(c,d)){
a=fmin(a,d);
a=atan2(c,d);
b=fmax(a,c);
}
c=exp(e);
c=fmin(c,a);
d=fmin(a,b);
c=isless(e,a);
}
