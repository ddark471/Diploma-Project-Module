#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=sin(b);
d=exp(c);
if(isgreaterequal(d,b)){
b=isgreater(b,a);
c=isless(b,b);
b=asin(a);
c=isless(a,a);
b=fdim(d,b);
}
d=atan(c);
a=ceil(c);
a=fmin(a,c);
b=isless(c,b);
d=atan2(a,c);
while(isless(d,d)){
d=isgreaterequal(a,b);
c=atan2(b,b);
d=islessgreater(d,a);
}
f=log10(a);
a=isgreater(c,c);
c=atan2(g,a);
a=fmin(g,f);
d=islessgreater(f,e);
if(isless(a,d)){
d=fmax(g,a);
g=islessequal(b,e);
}
else{
g=log(g);
c=isless(a,b);
d=sqrt(g);
d=atan2(a,f);
f=isgreaterequal(a,a);
}
g=islessgreater(b,g);
d=islessgreater(g,b);
c=sin(e);
}
