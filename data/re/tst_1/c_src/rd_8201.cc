#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=pow(c,a);
f=sqrt(h);
f=ceil(b);
d=fmax(h,c);
while(isgreaterequal(h,e)){
b=fmin(e,g);
c=log(a);
}
b=asin(d);
e=asin(d);
c=log10(c);
e=atan2(a,c);
}
