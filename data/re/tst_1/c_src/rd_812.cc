#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=isless(e,a);
f=isgreater(f,a);
d=sin(b);
if(islessgreater(f,e)){
e=asin(d);
e=ceil(c);
f=fmax(d,b);
}
e=ceil(a);
a=ceil(e);
while(isgreaterequal(d,b)){
a=sqrt(b);
f=sin(d);
}
a=ceil(c);
a=fmax(c,d);
if(isgreaterequal(c,a)){
d=fmin(b,a);
c=atan2(d,d);
}
a=tan(d);
d=islessgreater(a,d);
a=floor(d);
}
