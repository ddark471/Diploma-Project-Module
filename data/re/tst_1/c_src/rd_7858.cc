#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=fmin(d,h);
g=exp(c);
b=pow(b,a);
while(islessgreater(f,c)){
f=islessequal(g,f);
c=pow(e,e);
d=fmin(d,d);
b=floor(h);
a=sin(a);
}
h=tan(h);
d=pow(d,g);
c=exp(c);
c=islessequal(b,e);
c=log10(b);
}
