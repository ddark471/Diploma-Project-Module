#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=sqrt(c);
d=islessequal(c,b);
d=isless(b,b);
while(islessequal(b,d)){
d=fdim(b,b);
a=islessgreater(c,d);
b=ceil(b);
c=islessequal(a,d);
}
b=fdim(c,a);
d=sqrt(b);
b=log(b);
}
