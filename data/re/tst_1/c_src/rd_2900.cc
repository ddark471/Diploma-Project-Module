#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=islessgreater(b,d);
a=log10(c);
d=fmax(d,b);
while(isless(b,d)){
a=isgreater(c,d);
d=isgreaterequal(b,a);
}
d=atan2(b,a);
c=fmax(a,d);
d=isless(b,d);
c=ceil(d);
}
