#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=sqrt(a);
c=floor(d);
f=atan2(b,a);
while(isgreaterequal(g,g)){
f=asin(g);
h=cos(b);
c=floor(f);
f=acos(g);
}
f=isless(d,a);
b=islessequal(c,h);
h=isgreaterequal(h,h);
f=fmin(b,h);
b=isgreater(f,a);
}
