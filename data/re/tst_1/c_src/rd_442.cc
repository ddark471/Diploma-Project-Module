#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=exp(b);
b=atan2(d,a);
b=fmax(a,b);
a=fmax(d,b);
while(isless(d,c)){
b=fmax(a,b);
b=isless(c,d);
d=sin(a);
}
c=log(a);
c=atan(a);
b=ceil(d);
d=atan2(b,a);
}
