#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=atan2(d,b);
b=fmax(a,b);
b=exp(b);
while(isgreaterequal(d,a)){
g=log(g);
c=cos(d);
d=floor(c);
c=atan(c);
}
d=exp(c);
d=islessequal(d,f);
e=isless(f,e);
}
