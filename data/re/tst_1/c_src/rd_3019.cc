#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=pow(e,b);
b=atan2(c,f);
d=log10(a);
a=cos(c);
c=fmin(g,e);
while(isless(a,c)){
e=cos(g);
c=cos(f);
d=fmax(d,e);
g=fmax(a,d);
}
f=fmin(g,b);
f=acos(d);
c=fmin(c,b);
}
