#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fmin(c,d);
b=fmax(c,a);
d=exp(a);
d=isless(b,c);
while(islessequal(b,d)){
d=islessgreater(b,a);
b=atan2(d,a);
}
c=fmin(a,b);
d=atan2(d,d);
d=exp(a);
c=cos(d);
}
