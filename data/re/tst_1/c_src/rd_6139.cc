#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=ceil(g);
e=fmax(e,a);
d=log10(c);
f=isless(g,d);
c=islessequal(d,d);
while(isless(d,d)){
f=atan(a);
f=exp(f);
d=log(f);
d=exp(b);
}
e=fmax(e,f);
e=log10(g);
a=asin(b);
e=isless(f,a);
g=fdim(h,h);
}
