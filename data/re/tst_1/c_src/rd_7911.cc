#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=sin(f);
e=isless(f,b);
while(isless(f,f)){
a=atan2(e,b);
b=fmin(c,a);
}
b=fmax(f,d);
c=sin(a);
e=acos(c);
}
