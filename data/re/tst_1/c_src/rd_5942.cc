#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=isless(c,f);
a=ceil(b);
a=isgreater(a,a);
b=atan2(e,a);
f=islessequal(b,e);
if(isless(b,f)){
c=cos(c);
a=fmax(b,b);
f=fmax(b,d);
}
b=isless(c,d);
c=islessgreater(c,d);
b=log(b);
f=sin(f);
while(isgreaterequal(f,f)){
c=asin(c);
a=pow(f,e);
c=atan2(e,a);
e=islessequal(f,f);
e=islessequal(c,d);
}
c=islessequal(a,a);
d=fmin(f,c);
}
