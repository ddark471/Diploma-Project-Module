#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=tan(c);
c=ceil(d);
d=floor(d);
a=fmin(c,a);
while(isless(d,a)){
a=atan(a);
c=cos(a);
a=ceil(a);
b=fmax(d,c);
}
b=fmin(d,d);
b=ceil(b);
}
