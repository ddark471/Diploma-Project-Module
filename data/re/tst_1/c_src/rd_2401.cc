#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=cos(b);
d=islessequal(a,a);
c=atan(c);
b=fmin(d,a);
a=islessequal(a,b);
while(islessgreater(c,a)){
c=isless(d,e);
c=log10(d);
d=fdim(c,a);
}
d=isgreater(e,c);
e=pow(e,d);
d=ceil(b);
c=fmin(a,a);
a=fmin(e,d);
}
