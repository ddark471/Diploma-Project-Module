#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=ceil(b);
e=islessgreater(e,f);
a=log10(b);
if(isless(h,b)){
g=sqrt(h);
f=exp(h);
h=ceil(g);
e=log(f);
}
else{
f=atan2(b,f);
g=fmin(e,c);
a=pow(a,c);
e=islessequal(a,d);
e=isless(a,e);
}
h=exp(a);
g=tan(a);
while(islessequal(a,d)){
e=isless(a,c);
a=fmax(a,a);
}
b=islessgreater(b,a);
e=exp(d);
}
