#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=atan2(b,g);
f=ceil(d);
h=isless(g,h);
a=sqrt(a);
while(islessequal(h,c)){
g=fmin(c,f);
f=asin(e);
}
f=isgreater(d,f);
g=fmax(f,a);
if(islessgreater(h,g)){
d=cos(h);
a=fmax(h,a);
f=isgreaterequal(g,d);
g=islessequal(d,b);
c=isgreater(d,b);
}
else{
d=isgreater(e,c);
h=fdim(e,f);
a=asin(h);
h=acos(f);
}
b=sqrt(h);
c=sin(b);
d=ceil(a);
f=isgreater(h,e);
f=isgreaterequal(g,e);
}
