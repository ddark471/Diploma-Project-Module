#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=exp(g);
g=isgreater(e,b);
a=islessequal(c,c);
while(isless(c,d)){
g=ceil(d);
c=islessequal(c,e);
}
c=fmax(b,a);
b=pow(c,a);
if(isless(b,c)){
c=pow(c,a);
d=islessgreater(c,b);
d=acos(b);
a=islessgreater(c,d);
a=log(c);
}
else{
d=fmin(d,a);
d=ceil(d);
d=fmax(d,c);
c=log(b);
}
a=pow(b,d);
d=islessgreater(c,c);
d=islessequal(a,c);
}
