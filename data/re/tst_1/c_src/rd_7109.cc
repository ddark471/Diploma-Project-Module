#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=exp(a);
a=fmin(c,a);
d=atan2(a,d);
a=cos(d);
a=sin(c);
while(isgreaterequal(a,a)){
d=fmin(b,b);
d=asin(b);
d=exp(a);
c=fmax(d,b);
}
c=exp(b);
d=asin(c);
b=floor(d);
d=fmin(c,d);
c=asin(a);
}
