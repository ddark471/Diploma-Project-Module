#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=acos(d);
b=sin(b);
b=fmax(a,c);
a=islessgreater(b,b);
d=pow(b,c);
while(isless(d,d)){
c=fmax(d,a);
a=atan(d);
d=exp(d);
}
b=isless(c,b);
a=pow(c,d);
}
