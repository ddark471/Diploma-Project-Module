#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fmin(g,b);
d=sin(d);
c=fdim(a,a);
e=isgreater(c,f);
while(islessgreater(e,e)){
a=isless(d,b);
g=fmin(c,c);
a=fdim(f,a);
f=fmin(c,g);
f=fmin(g,g);
}
b=fmin(e,c);
d=pow(b,a);
b=sin(d);
c=fmin(a,b);
if(islessgreater(c,b)){
d=sin(b);
a=islessequal(e,c);
}
else{
b=atan2(e,b);
c=asin(e);
}
e=pow(e,e);
c=isgreater(d,e);
a=isless(b,e);
}
