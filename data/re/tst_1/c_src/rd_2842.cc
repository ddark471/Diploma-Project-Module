#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=islessequal(e,e);
d=fmin(g,g);
g=ceil(g);
c=isgreater(d,e);
if(islessequal(f,c)){
e=ceil(c);
a=atan2(a,c);
d=exp(f);
c=ceil(g);
d=fmin(c,f);
}
else{
d=isgreater(g,a);
e=tan(e);
}
c=islessgreater(f,a);
g=log(b);
g=log10(a);
while(islessequal(a,c)){
g=atan2(e,b);
a=sqrt(b);
}
d=tan(d);
a=fdim(a,b);
b=fmax(b,b);
if(isgreaterequal(b,d)){
a=cos(b);
d=islessgreater(c,c);
d=fmax(b,d);
}
b=islessgreater(d,b);
a=isgreaterequal(d,d);
}
