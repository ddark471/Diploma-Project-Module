#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=sqrt(d);
f=log(e);
e=log10(c);
while(islessgreater(b,a)){
f=isless(a,a);
g=fmax(c,e);
f=isless(d,d);
h=ceil(g);
e=sin(b);
}
g=pow(h,d);
f=cos(f);
c=atan2(f,d);
}
