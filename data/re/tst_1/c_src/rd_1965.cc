#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=sin(a);
a=log(b);
c=sin(c);
if(isgreaterequal(a,a)){
d=pow(d,b);
a=fdim(d,a);
d=exp(c);
d=log(c);
c=exp(c);
}
else{
b=sqrt(b);
d=atan2(b,b);
}
b=isless(b,d);
d=isgreaterequal(d,c);
a=islessgreater(c,b);
while(isless(d,b)){
b=pow(b,a);
c=islessequal(d,d);
}
c=isless(e,b);
a=sin(e);
c=pow(c,c);
b=sqrt(c);
if(islessgreater(c,b)){
a=fdim(d,f);
a=islessgreater(c,c);
d=fmax(a,f);
}
b=fmax(f,d);
b=atan2(b,d);
}
