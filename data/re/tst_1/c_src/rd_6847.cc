#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=isless(g,d);
e=isless(a,a);
while(isless(h,a)){
a=islessgreater(h,g);
e=ceil(e);
c=log10(d);
e=sin(d);
}
a=ceil(d);
a=fmax(a,e);
f=islessequal(c,d);
e=isless(a,c);
if(islessgreater(e,b)){
a=fmin(a,d);
e=asin(f);
d=isless(a,a);
}
a=asin(b);
a=log(b);
}
