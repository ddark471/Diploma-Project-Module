#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=log10(f);
c=isgreaterequal(h,e);
c=fdim(a,h);
while(isless(e,a)){
h=tan(b);
h=sqrt(h);
a=ceil(g);
e=cos(h);
g=pow(a,g);
}
b=floor(h);
a=log(b);
}
