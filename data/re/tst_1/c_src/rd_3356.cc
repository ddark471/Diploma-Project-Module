#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=islessgreater(a,g);
g=islessequal(a,f);
a=sin(a);
d=isless(a,b);
while(isless(a,e)){
f=islessequal(a,a);
c=fmin(e,e);
g=sqrt(e);
a=fmax(f,c);
c=asin(c);
}
b=fdim(a,d);
d=cos(c);
g=sin(d);
d=ceil(b);
}
