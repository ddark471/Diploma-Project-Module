#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=atan(a);
a=fmax(g,c);
c=isgreater(b,f);
d=sin(b);
while(islessequal(f,d)){
a=islessgreater(c,f);
e=fmax(b,d);
}
d=sin(d);
a=isless(c,c);
g=fdim(e,b);
}
