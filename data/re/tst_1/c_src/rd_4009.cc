#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=isgreaterequal(c,a);
b=isless(c,a);
while(islessequal(c,d)){
b=exp(c);
c=log10(c);
}
c=pow(d,c);
c=floor(c);
c=atan2(c,a);
b=pow(b,a);
}
