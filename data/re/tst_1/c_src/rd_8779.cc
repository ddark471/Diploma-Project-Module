#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=cos(e);
e=sqrt(e);
while(islessequal(f,d)){
g=isgreater(g,c);
c=pow(f,a);
e=isgreaterequal(e,b);
c=log10(c);
a=isless(b,d);
}
b=exp(a);
g=atan(g);
d=islessequal(e,g);
}
