#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=sqrt(b);
e=atan2(a,h);
b=pow(d,g);
d=tan(c);
while(islessequal(g,h)){
d=exp(h);
h=fmax(e,e);
g=log(h);
}
g=tan(b);
h=floor(f);
e=islessequal(b,e);
}
