#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=log10(c);
d=exp(a);
a=acos(c);
c=cos(a);
d=atan2(a,d);
while(islessequal(a,b)){
c=fmax(b,b);
a=atan(a);
b=atan(a);
}
c=isless(d,b);
b=ceil(b);
d=tan(d);
d=pow(c,a);
}
