#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=fmax(b,g);
c=exp(a);
e=fmin(f,h);
d=atan2(b,h);
if(islessgreater(c,e)){
e=log10(h);
f=islessequal(b,b);
d=log(a);
f=isgreaterequal(g,f);
}
else{
g=log(f);
d=floor(b);
f=atan2(f,a);
a=log10(c);
}
e=tan(b);
c=isgreater(g,h);
while(isless(a,g)){
g=cos(g);
f=fmin(f,b);
}
a=fmin(a,e);
b=islessequal(c,b);
if(isless(d,c)){
b=ceil(f);
f=fmax(e,f);
d=isgreater(a,a);
d=cos(e);
}
else{
f=ceil(f);
d=isgreater(d,b);
a=sqrt(c);
b=fmin(e,f);
d=isgreater(f,d);
}
a=cos(f);
b=cos(e);
}
