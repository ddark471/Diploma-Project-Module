#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=pow(b,b);
f=log10(f);
d=islessgreater(b,e);
a=log10(f);
c=asin(e);
while(islessequal(a,c)){
a=sqrt(d);
e=isgreaterequal(f,c);
d=acos(a);
c=fmax(d,d);
b=isless(c,b);
}
g=log10(b);
d=sqrt(f);
d=exp(d);
b=fmax(e,c);
f=isless(a,f);
}
