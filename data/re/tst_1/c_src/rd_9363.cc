#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=fmax(b,d);
a=cos(b);
e=acos(a);
while(islessgreater(f,a)){
c=islessequal(a,d);
a=atan2(c,d);
f=islessgreater(e,f);
}
d=isgreater(c,d);
b=acos(d);
d=atan(b);
}
