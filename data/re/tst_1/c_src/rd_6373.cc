#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=acos(f);
d=isgreater(b,c);
while(isgreaterequal(d,f)){
f=fmax(b,e);
b=pow(f,b);
}
e=log(h);
a=atan(h);
h=tan(f);
h=fdim(g,h);
if(isless(h,d)){
a=isless(f,d);
g=isgreaterequal(e,g);
c=log(f);
}
else{
d=atan(g);
a=atan(e);
e=isless(c,b);
h=isgreaterequal(e,c);
}
h=acos(f);
d=log10(h);
}
