#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=atan(b);
d=cos(c);
while(isgreaterequal(a,c)){
a=exp(d);
a=exp(c);
c=pow(a,c);
}
b=log10(a);
b=isgreater(d,d);
a=isgreaterequal(d,b);
c=log10(d);
}
