#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=sin(a);
b=asin(c);
c=ceil(d);
while(islessequal(c,d)){
a=atan(a);
d=sin(a);
}
d=exp(b);
d=floor(a);
}
