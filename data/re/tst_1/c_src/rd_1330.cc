#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=sin(c);
a=floor(g);
while(isless(c,a)){
b=isless(d,d);
d=fmax(d,b);
}
g=fdim(f,e);
d=asin(g);
}
