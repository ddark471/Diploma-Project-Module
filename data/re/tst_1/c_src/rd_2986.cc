#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=islessequal(h,f);
g=ceil(e);
while(islessgreater(f,g)){
a=isgreater(c,c);
d=ceil(h);
}
d=log(d);
h=isgreaterequal(g,c);
a=log(f);
f=islessequal(g,d);
d=ceil(b);
}
