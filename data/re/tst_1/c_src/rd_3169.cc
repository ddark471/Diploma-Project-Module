#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=exp(g);
g=log(d);
while(islessgreater(b,c)){
a=asin(d);
c=isgreaterequal(b,e);
g=islessgreater(b,c);
c=atan(f);
}
f=atan(e);
f=fmax(d,e);
d=isgreater(g,c);
}
