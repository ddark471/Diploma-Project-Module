#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=ceil(b);
d=isgreaterequal(c,b);
c=isgreater(b,b);
while(islessequal(c,b)){
c=isgreaterequal(a,d);
b=ceil(a);
b=fmin(c,c);
d=fmax(d,c);
c=tan(d);
}
c=sin(a);
d=islessequal(d,c);
c=floor(a);
}
