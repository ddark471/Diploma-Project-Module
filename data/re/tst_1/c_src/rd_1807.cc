#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=atan2(a,b);
c=atan(b);
while(isgreaterequal(a,b)){
b=atan(c);
d=isgreaterequal(c,c);
}
b=fdim(a,d);
b=pow(b,a);
}
