#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=tan(c);
e=islessequal(c,g);
e=islessgreater(b,e);
while(islessgreater(d,b)){
d=log10(e);
g=isless(e,f);
e=isless(d,h);
h=sqrt(h);
g=isgreaterequal(a,d);
}
h=floor(a);
e=isless(g,b);
a=fmax(e,g);
d=islessgreater(g,a);
h=isgreater(g,e);
}
