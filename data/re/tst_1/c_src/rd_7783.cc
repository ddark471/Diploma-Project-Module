#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=atan(g);
c=pow(e,a);
g=isless(c,f);
c=tan(c);
c=isgreaterequal(c,h);
if(islessequal(f,f)){
g=floor(b);
f=isgreater(f,c);
e=tan(a);
a=log10(g);
}
else{
h=atan(c);
d=log10(f);
e=tan(d);
c=fmax(b,g);
g=islessgreater(a,h);
}
d=fmin(h,d);
b=pow(b,g);
while(isless(h,a)){
f=fmin(f,b);
d=pow(c,d);
}
a=fmax(c,a);
c=tan(a);
b=acos(a);
if(islessequal(d,c)){
c=islessgreater(a,b);
a=floor(d);
}
a=log(c);
c=pow(b,c);
}
