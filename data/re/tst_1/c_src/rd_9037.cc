#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=atan(e);
f=exp(b);
e=isgreater(g,d);
while(isless(b,c)){
d=fmax(e,f);
a=islessequal(c,e);
}
b=ceil(f);
g=islessequal(g,d);
f=atan2(g,a);
}
