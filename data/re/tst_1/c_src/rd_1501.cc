#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=sqrt(e);
h=exp(d);
if(isless(b,b)){
a=isgreater(c,h);
e=sqrt(f);
e=cos(d);
}
else{
e=fdim(c,a);
h=isless(d,b);
c=ceil(a);
}
d=isgreater(c,e);
g=isgreaterequal(c,d);
d=asin(h);
c=sqrt(e);
while(islessequal(f,e)){
e=sqrt(c);
g=sqrt(f);
c=atan2(g,a);
c=fmax(h,h);
}
b=fmax(e,f);
b=sin(b);
b=fmax(b,c);
a=sin(d);
d=log10(e);
}
