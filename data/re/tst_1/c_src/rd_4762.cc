#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=log(a);
c=fdim(f,f);
while(isless(c,h)){
b=pow(a,b);
f=log(a);
b=islessequal(a,b);
d=fmax(d,a);
c=log(a);
}
d=log10(g);
f=isless(e,b);
}
