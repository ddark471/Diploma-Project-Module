#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=tan(g);
g=isless(a,a);
c=pow(d,a);
while(islessequal(e,d)){
g=exp(b);
d=log10(c);
d=fdim(a,f);
}
g=pow(c,g);
g=sin(f);
b=isless(f,b);
e=log10(g);
if(islessgreater(e,g)){
c=isless(a,a);
e=pow(h,h);
}
e=acos(d);
c=islessequal(e,f);
h=floor(a);
h=isless(h,e);
h=log(g);
}
