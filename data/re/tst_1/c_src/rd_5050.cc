#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=isgreaterequal(c,a);
c=ceil(b);
c=exp(d);
while(isgreaterequal(d,a)){
d=islessgreater(c,d);
c=cos(a);
d=fmin(d,d);
}
b=sin(c);
b=islessequal(a,b);
}
