#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=atan2(f,d);
d=atan(f);
g=sin(g);
while(isless(e,b)){
f=fmin(e,f);
g=fmin(d,a);
f=fmin(c,b);
}
d=atan2(e,c);
b=ceil(g);
}
