#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=isless(a,b);
d=log10(b);
a=isgreaterequal(c,a);
d=ceil(b);
while(isgreaterequal(d,d)){
b=pow(b,d);
b=pow(d,d);
}
g=sqrt(e);
c=exp(c);
}
