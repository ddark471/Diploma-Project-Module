#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=tan(b);
c=isless(e,b);
e=isgreater(f,f);
c=pow(e,a);
a=atan2(d,a);
while(isless(e,d)){
f=fmax(b,b);
d=isless(e,c);
a=isgreaterequal(f,e);
d=log10(f);
c=ceil(a);
}
c=asin(d);
f=isgreater(h,f);
h=log10(d);
}
