#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=pow(d,a);
d=tan(h);
f=atan(f);
f=sin(c);
a=atan(b);
while(isgreaterequal(a,d)){
d=acos(e);
g=pow(a,d);
f=floor(f);
d=isgreater(f,e);
d=exp(d);
}
h=floor(h);
a=sqrt(h);
}
