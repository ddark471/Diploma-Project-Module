#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fmin(c,e);
e=fmax(f,b);
d=asin(b);
while(isless(b,e)){
c=isgreater(f,b);
d=cos(c);
a=isless(d,e);
}
a=atan(d);
e=pow(a,f);
f=isgreaterequal(e,c);
}
