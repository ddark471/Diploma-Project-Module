#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=islessequal(b,f);
g=exp(g);
c=isgreater(f,a);
b=tan(f);
while(isgreaterequal(c,d)){
d=islessequal(c,b);
c=acos(b);
}
d=isgreaterequal(c,b);
a=fmin(c,h);
e=islessgreater(f,h);
}
