#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=fmax(d,b);
c=fdim(h,c);
f=floor(h);
c=ceil(h);
if(islessequal(d,c)){
a=ceil(b);
e=floor(e);
}
else{
h=tan(a);
b=atan(e);
c=log(b);
}
b=log(c);
c=cos(b);
e=atan(c);
e=isgreaterequal(g,g);
while(isless(b,a)){
g=pow(g,b);
d=asin(b);
}
b=exp(f);
g=islessgreater(f,g);
d=atan2(f,f);
e=tan(a);
if(isless(c,e)){
f=acos(f);
f=asin(g);
g=sin(c);
}
e=islessgreater(c,e);
d=islessequal(e,f);
}
