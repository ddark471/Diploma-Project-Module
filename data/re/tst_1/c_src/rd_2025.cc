#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=pow(a,a);
f=atan(c);
c=isgreaterequal(d,g);
while(isless(c,c)){
a=ceil(f);
b=cos(b);
e=isless(d,c);
g=fmax(f,a);
g=atan(b);
}
c=ceil(d);
a=sqrt(a);
d=isgreater(c,d);
c=isgreater(d,d);
b=log10(b);
}
