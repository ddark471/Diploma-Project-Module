#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=islessequal(a,b);
d=isgreaterequal(a,c);
b=floor(d);
c=exp(b);
b=fmax(c,d);
while(islessgreater(a,b)){
c=floor(d);
b=atan2(d,d);
}
d=fmax(c,a);
d=fdim(d,d);
}
