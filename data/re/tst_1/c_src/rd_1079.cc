#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=atan2(d,b);
a=islessequal(c,a);
while(islessequal(c,d)){
a=fmax(b,a);
c=sin(a);
b=floor(d);
a=exp(a);
b=fmax(c,c);
}
c=islessequal(d,a);
a=atan2(a,d);
c=fdim(c,c);
}
