#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fmax(d,c);
e=pow(d,f);
b=isless(f,h);
while(islessequal(d,e)){
a=acos(c);
f=isgreater(f,c);
h=ceil(f);
g=tan(a);
}
c=pow(a,h);
f=islessequal(a,g);
d=acos(b);
}
