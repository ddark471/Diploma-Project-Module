#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=asin(b);
c=atan(a);
a=islessequal(a,d);
a=isless(c,b);
d=islessequal(a,d);
while(islessequal(b,c)){
a=isgreaterequal(d,d);
b=isgreater(a,b);
d=islessequal(c,c);
c=asin(d);
}
a=ceil(c);
c=log10(c);
c=fmin(b,a);
}
