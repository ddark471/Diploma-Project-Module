#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=acos(d);
d=acos(c);
d=fmin(c,e);
if(islessgreater(a,c)){
e=fmin(g,b);
g=sin(e);
f=fdim(c,a);
c=fdim(a,d);
a=isgreater(f,b);
}
else{
e=isless(c,a);
e=isgreater(f,e);
f=isless(b,g);
}
b=fmin(a,e);
d=islessequal(a,g);
g=log10(d);
while(islessequal(d,b)){
c=islessgreater(e,b);
e=isgreater(b,d);
a=sqrt(d);
g=isgreaterequal(a,b);
}
d=fmax(b,g);
a=pow(b,d);
g=fmin(a,g);
a=pow(c,g);
}
