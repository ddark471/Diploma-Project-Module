#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=log10(f);
c=log(c);
while(isless(c,g)){
g=sin(d);
c=fdim(g,g);
d=fmax(e,f);
}
e=asin(h);
d=fdim(h,b);
a=atan2(c,e);
if(isless(d,g)){
e=fdim(e,g);
h=sin(c);
d=isgreater(f,c);
}
else{
f=fmax(h,f);
b=islessgreater(d,g);
}
a=islessequal(c,h);
d=acos(h);
c=acos(b);
h=exp(h);
e=log10(d);
}
