#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=islessgreater(c,b);
d=fmax(b,d);
e=pow(d,c);
a=log(e);
d=fmax(e,d);
while(islessequal(c,d)){
e=isgreater(b,b);
a=fmax(c,c);
}
d=fmax(a,e);
a=isgreaterequal(d,a);
c=atan2(d,c);
}
