#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=asin(e);
c=log10(a);
c=log10(c);
b=fmax(c,a);
if(islessequal(b,c)){
h=ceil(c);
h=fmin(d,c);
}
b=ceil(e);
f=fmax(b,f);
h=log10(b);
e=log10(c);
while(isless(c,b)){
g=fmax(e,b);
g=pow(b,d);
}
c=isgreaterequal(b,h);
b=acos(b);
}
