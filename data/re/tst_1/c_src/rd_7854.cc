#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=atan(b);
f=ceil(a);
g=asin(g);
f=isless(a,g);
while(isgreaterequal(a,c)){
a=exp(b);
f=sqrt(f);
a=log(c);
a=islessequal(d,f);
}
e=pow(d,d);
e=ceil(a);
}
