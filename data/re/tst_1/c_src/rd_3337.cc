#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=floor(b);
b=atan(a);
d=floor(d);
e=exp(b);
while(isless(b,d)){
e=fmin(b,b);
a=fmax(e,c);
e=islessequal(e,b);
d=ceil(c);
}
c=exp(c);
e=fdim(d,a);
}
