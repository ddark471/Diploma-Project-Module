#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=islessgreater(h,h);
f=log(c);
d=isless(g,g);
h=log10(c);
c=isgreater(e,f);
if(islessgreater(b,f)){
e=atan2(f,e);
b=tan(b);
h=floor(h);
}
b=isless(f,d);
a=fmax(b,d);
g=atan(g);
h=islessequal(d,h);
while(isgreaterequal(e,g)){
d=fdim(b,a);
e=pow(b,a);
h=floor(a);
h=fmin(a,c);
}
f=islessequal(c,g);
b=exp(d);
e=sqrt(e);
if(isgreaterequal(b,f)){
a=ceil(a);
g=cos(a);
}
a=asin(e);
d=pow(d,b);
g=atan2(g,a);
}
