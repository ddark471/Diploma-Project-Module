#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=pow(e,b);
d=isless(f,g);
while(islessequal(g,c)){
e=fmin(f,d);
c=atan2(b,g);
g=log10(c);
a=acos(e);
}
e=pow(b,c);
c=tan(a);
b=log10(b);
}
