#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=exp(a);
a=acos(f);
f=islessgreater(e,a);
g=exp(b);
while(isgreaterequal(d,e)){
g=islessequal(b,g);
c=isgreaterequal(b,a);
d=atan(h);
b=floor(f);
c=floor(g);
}
g=log(e);
f=fmin(c,g);
d=isgreater(g,g);
}
