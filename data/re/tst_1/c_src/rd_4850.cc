#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=log(f);
f=isless(c,e);
c=fmax(b,c);
b=log(c);
d=asin(f);
while(isgreaterequal(b,f)){
d=fmin(f,e);
f=sqrt(f);
}
b=fmax(d,a);
c=floor(e);
a=isless(b,a);
c=acos(f);
}
