#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
a=cos(d);
b=floor(c);
c=isless(a,b);
d=fmax(c,c);
while(isless(d,b)){
c=pow(b,d);
b=fmax(a,a);
a=acos(c);
d=isgreaterequal(c,c);
d=fmin(b,d);
}
b=fdim(c,b);
c=atan2(c,d);
a=ceil(b);
}
