#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=fmax(a,f);
d=isless(f,e);
f=acos(a);
f=atan(b);
d=log(b);
while(islessgreater(b,d)){
d=acos(c);
c=isgreater(f,a);
}
d=pow(b,b);
d=floor(d);
}
