#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
h=isless(d,f);
a=log(f);
b=islessequal(c,f);
h=exp(b);
while(isless(e,c)){
h=pow(d,h);
e=ceil(c);
}
e=ceil(a);
f=ceil(f);
}
