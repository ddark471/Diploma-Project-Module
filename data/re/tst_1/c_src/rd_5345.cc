#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=fdim(g,e);
g=ceil(a);
if(islessgreater(d,e)){
a=fdim(c,a);
a=fmin(a,d);
}
else{
h=ceil(g);
c=islessequal(b,d);
}
e=pow(e,g);
h=pow(d,f);
c=atan(d);
while(islessgreater(g,g)){
b=fdim(h,h);
h=isgreaterequal(e,f);
}
c=atan2(d,c);
b=atan2(a,a);
d=exp(a);
c=atan(c);
if(isless(d,a)){
c=floor(b);
c=isless(a,c);
a=tan(c);
d=floor(b);
}
d=isgreater(d,b);
d=fmax(d,a);
a=pow(a,d);
}
