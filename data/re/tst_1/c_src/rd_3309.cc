#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=floor(d);
c=log10(a);
a=log10(d);
while(islessequal(b,b)){
d=sqrt(d);
b=pow(a,d);
d=pow(a,b);
b=islessequal(b,a);
a=islessequal(d,b);
}
a=islessgreater(d,d);
c=fmax(d,a);
}
