#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=pow(b,b);
a=fmax(a,b);
c=fdim(b,b);
while(islessequal(d,c)){
a=tan(c);
a=fmax(a,d);
}
d=log(c);
a=fmax(a,c);
b=fmax(c,d);
b=fdim(b,d);
}
