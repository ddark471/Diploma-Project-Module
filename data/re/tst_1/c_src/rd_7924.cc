#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=atan2(e,c);
d=atan2(d,b);
d=exp(b);
while(isless(f,e)){
c=islessgreater(d,a);
d=asin(a);
}
e=sin(f);
b=ceil(b);
e=atan2(a,f);
a=islessequal(c,e);
}
