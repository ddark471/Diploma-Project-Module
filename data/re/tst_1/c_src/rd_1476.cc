#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=isless(f,g);
a=floor(e);
e=ceil(b);
while(isless(b,b)){
h=log10(h);
f=islessequal(e,b);
a=cos(e);
c=isless(h,h);
}
h=log(e);
e=islessequal(a,a);
a=pow(f,f);
d=cos(c);
}
