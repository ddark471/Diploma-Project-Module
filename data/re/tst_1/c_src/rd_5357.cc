#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=cos(a);
g=isgreater(a,g);
d=islessequal(f,a);
if(isless(a,a)){
c=fdim(e,c);
b=pow(f,f);
}
d=isgreater(d,b);
e=isless(a,g);
while(isless(b,f)){
b=log10(b);
f=sin(c);
a=log10(c);
b=ceil(c);
}
e=floor(e);
d=exp(c);
e=asin(b);
}
