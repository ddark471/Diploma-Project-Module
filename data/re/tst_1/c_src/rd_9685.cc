#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
d=atan2(c,c);
d=atan(c);
a=isgreater(a,b);
c=log10(b);
while(islessequal(d,d)){
a=isgreater(a,b);
d=islessequal(a,c);
b=log10(d);
}
g=fmin(d,c);
b=ceil(h);
h=log(d);
d=islessequal(g,d);
if(isgreaterequal(b,d)){
h=exp(f);
f=isgreater(f,e);
}
else{
d=islessgreater(d,h);
b=fmax(e,e);
e=fmax(d,f);
e=asin(g);
}
f=tan(e);
d=pow(f,h);
d=tan(a);
}
