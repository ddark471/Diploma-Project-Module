#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=pow(a,c);
d=atan(e);
f=isless(a,e);
while(islessgreater(c,c)){
f=sqrt(d);
d=islessequal(e,f);
f=fmin(c,e);
}
a=fmax(d,d);
c=pow(c,d);
}
