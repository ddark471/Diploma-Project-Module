#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
e=cos(b);
c=floor(h);
d=log10(d);
while(isgreaterequal(h,b)){
c=ceil(c);
h=isless(f,a);
b=sqrt(b);
a=exp(d);
}
d=pow(c,c);
d=isgreater(d,b);
}
