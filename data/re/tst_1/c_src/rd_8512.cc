#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
g=pow(b,f);
b=ceil(g);
while(isless(h,b)){
b=acos(c);
e=log(f);
f=atan2(d,e);
f=pow(a,a);
f=exp(c);
}
g=log10(d);
f=ceil(b);
}
