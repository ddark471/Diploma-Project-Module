#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
b=exp(h);
h=sqrt(e);
while(islessequal(f,e)){
b=islessequal(c,a);
f=fmax(b,b);
}
d=islessgreater(b,a);
d=isgreaterequal(c,d);
a=fmin(a,d);
b=floor(a);
a=fmin(b,a);
}
