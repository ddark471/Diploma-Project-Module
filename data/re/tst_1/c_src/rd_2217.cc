#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
f=islessgreater(d,f);
e=sqrt(c);
while(islessgreater(d,c)){
d=atan2(c,b);
g=isgreater(a,b);
d=floor(g);
a=islessgreater(d,b);
d=islessequal(e,g);
}
c=islessgreater(c,d);
f=floor(g);
d=tan(a);
g=islessequal(f,g);
d=fmax(d,f);
}
