#include <stdio.h>
#include <math.h>

int main(){
double a;
double b;
double c;
double d;
double e;
double f;
double g;
double h;
c=log(c);
c=isgreater(e,b);
d=log(c);
while(isless(b,b)){
a=fmax(e,a);
c=floor(c);
}
e=log10(a);
c=fmin(c,a);
d=log(c);
b=fmin(e,c);
}
